# Architecture Overview

This file is the always-loaded orientation. For per-abstraction detail, load the relevant reference file listed under [Detail References](#detail-references).

## Tech Stack
- **Frontend**: React 19 + TypeScript, built with Vite
- **State**: Zustand stores
- **Database**: Dexie.js over IndexedDB (local-first)
- **Spatial Canvas**: React Flow
- **Drag-and-Drop**: dnd-kit
- **Routing**: React Router v7

## Dependency Graph

```
main.tsx (entry point)
├── App.tsx                → Router, layout shell (clears `filter-store` on pathname change via `useEffect(location.pathname, …)`; first-mount path stashed in a ref so an initial hydrated filter survives)
├── styles/tokens.css      → CSS custom properties (design system: dark/light themes via [data-theme], tint scale, shadows, radii, z-index, spacing, typography)
├── views/                 → Route-level pages
│   ├── CanvasPage         → components/canvas/, stores
│   ├── ListView           → Unified list with sort-by grouping (Date/Scheduled/Deadline/People/Project/Status/Org), Save / Load dialogs over `ListDefinition` (SavedView retired in v39; every saved list is now a `ListDefinition`, optionally `favorited: true` to render in the Favorites chip bar), inline filter editor via the shared `ListFilterEditor`, rich-text clipboard export via `services/task-copy.copyTasksRich`
│   ├── CalendarView       → Month/week calendar grid, drag-to-reschedule, overdue highlights, recurring virtual instances
│   └── SettingsPage       → Theme toggle, manage buttons, task defaults, database location, import/export
│   (DashboardView retired — the horizon ribbon + selected-horizon list live on the canvas as the `horizons` widget kind via `FloatingHorizonsNode` / rails `HorizonsSlotContent`; `/dashboard` redirects to `/`. Post-code-review-2026-04-25 P8 the dormant `dashboardUserLists` / `notesPinnedToDashboard` settings fields + setters are gone from `settings-store`; the import-validation entries linger one release so older backups still validate, and `restore` strips both keys from IndexedDB on import.)
├── components/
│   ├── layout/            → Sidebar (view-nav icons at the top; `bottomIcons` container at the bottom holds a theme-mode ☀/☾ button above the ⚙ settings icon — reads `useSettingsStore().themeMode` + `useResolvedTheme`, writes `setThemeMode(resolvedTheme === 'dark' ? 'light' : 'dark')`), TopBar (filter bar + grouped search dropdown — `role=listbox` per-field `role=group` groups for Title/Notes/Project/Person/Org/Status/Tag, driven by `utils/filter.matchTodoText` with `TextMatchContext.tagNames` sourced from `useTagStore().assignedTagsMap`; arrow-key roving; inline "Show all {n}". Search rows are `TaskDraggable surface='search'` wrapped by a local TopBar-scoped `DndContext` + `DragOverlay` — drops onto the dashboard / canvas taskboard route through `useTaskboardStore.addAt` at the pointer index (`computeSearchDropIndex` bisects `[data-tbp-entry]` rects by Y); right-click opens `CanvasContextMenu` with the same item set as `TaskRow` — the menu + `ProjectPickerPopup` live on TopBar (not the row) so they outlive row unmounts, and right-click no longer blurs the search input so the dropdown stays visible under the menu), FileSyncBanner, BottomTabBar (mobile)
│   ├── task/              → TaskRow + MobileTaskRow (both enforce the no-tag-chip display rule — assigned tags never render on a row; manage tags from `TaskEditPopup` instead), notes-icon button opens `TaskNotePopover` via shared `NotesBody`; TaskList, TaskEditPopup (notes field uses shared `NotesBody`; interactive tag chip selector via `ChipSelector` — assign/unassign/create), TaskNotePopover
│   ├── canvas/            → Stats widgets (stats-widgets-2026-04-25 P1–P5): three new `SlotKind` values + `Floating*Node` + rails `*SlotContent` per kind under a "Stats ▸" hover flyout in `WidgetKindMenu` (alongside the existing `horizons`) — `status` (open-tasks-by-status stacked bar + legend, `selectStatusBreakdown`), `scoreboard` (defer / completion / lag cards with 12-week sparklines, `selectDisciplineMetrics`), `snoozeGraveyard` (top-5 most-rescheduled open todos, `selectMostDeferred`). Discipline + graveyard read from a new `todoEvents` Dexie table populated by `todo-repository` (insert/update/complete/delete); status reads live `todo-store` + `status-store`. CanvasView (now mounts `<CanvasToolbar>` as a sibling inside `.canvasWrapper`; the tab-pill canvas pop-out indicator is a `.popOutIndicator` overlay sized to `DEFAULT_FLOAT_WIDTH × DEFAULT_FLOAT_HEIGHT` and translated via `transform` to follow the pointer — replaces the prior full-viewport `[data-canvas-drop-active]::after` border, triage-2026-04-26 P1 / T2), CanvasToolbar (lower-left floating pill with Fit-all-to-view + Collapse / Expand all rails; replaces the rails-collapse button retired from `Sidebar.bottomIcons` — triage-2026-04-26 P3 / T5 / Q5=A), ProjectNode (⧉ copy → `copyTasksRich`; ⊟ group-by picker next to ↕ sort — popover with seven dimensions + None, including `'tag'` (lists-consistency P4), reads `GROUP_OPTIONS` from `utils/task-grouping.ts` (relocated in triage-2026-04-26 P4 / T6 so SettingsPage's Default-grouping `<select>` shares the same array), calls `project-store.updateProjectGrouping(projectId, groupBy)` which round-trips `Project.groupBy` through `projectRepository.updateGrouping` to Dexie), ListInsetNode (⧉ copy button in `WidgetHeader` meta → `copyTasksRich`; "Edit list" item on the title-click `WidgetKindMenu` fires `useUIStore.openListEditorDialog(defId)` (triage-2026-04-26 P5 / T7 / Q7=A; switched from `openListsEditor`) so `<StandaloneListEditor>` mounts the per-list editor dialog without `DashboardListsEditor` underneath), ListDefinitionBody (shared filter→buildDashboardLists body for inset + rail lens; threads `runtimeFilterValue: number[]` + `onRuntimeFilterChange` through when the def declares `runtimeFilter`), RuntimeFilterPicker (multi-value chip+typeahead picker mounted above the body of a def with `runtimeFilter` — person/org/project/status/tag options, OR-combined; lists-consistency P5 retired the prior native `<select>`; the dropdown panel `createPortal`s to `document.body` with placement computed off the input row's `getBoundingClientRect()` — mirrors `ProjectPickerPopup` / `WidgetKindMenu` so it escapes ancestor `overflow: hidden` clipping on float insets / rail lens slots / `/list` toolbar, triage-2026-04-25 P3), FloatingNoteNode / FloatingCalendarNode / FloatingHorizonsNode / TaskboardNode / ListInsetNode (the five floating canvas widgets; post-code-review-2026-04-25 P3 every node thin-wraps the shared `useFloatingWidget(opts)` hook for `headerProps` + dock/close/kind-change handlers + `<ResizeHandle>` for pointer-capture resize — the per-kind body is the only meaningful per-file surface; FloatingCalendarNode threads `orientation` + `weekOffset` via `floating-calendar-store.updateOrientation`/`updateWeekOffset`; FloatingHorizonsNode reads ribbon state from `settings.horizonSlots` + `selectedHorizon`), SortableTaskList (flat render when `project.groupBy == null` — single `SortableContext` over all rows; grouped render otherwise — `partitionByGroup(displayItems, groupBy, ctx)` from `utils/task-grouping`, then one project-scoped `SortableContext` per non-empty group wrapped in `<TaskGroup label count>`, ids derived via `blockContextId(projectId, blockKey)` from `utils/cross-group-drag`, with the ungrouped block rendered headerless above all named groups; insert-triggers stay inside each block, no trigger crosses a group boundary; **cross-group drops mutate the grouped field with replace semantics (lists-consistency P6)** — `useCanvasDnD.handleDragEnd` reads `active`/`over` container ids via `parseBlockContextId`, calls `resolveCrossGroupMutation(groupBy, sourceKey, targetKey, todoId)`, and dispatches `bulkSetStatus` (status — scalar set; `undefined` clears) / `unassignPerson+assignPerson` / `unassignOrg+assignOrg` / `unassignTag+assignTag` (people/org/tag — replace, with `null` on either side covering synthetic ungrouped buckets); date dimensions skip mutation since buckets are derived; multi-drag is skipped to keep semantics unambiguous), ProjectNavigator, alignment, shared/DraggableTaskRow, shared/TaskGroup (visual-only group header — optional 8 px circle swatch · small-caps label · count · thin rule; renders no header for the ungrouped block; v1 has no collapse, no drag, no per-group actions; the swatch (triage-2026-04-26 P2 / T4 / Q4=A) is rendered when `color` is set — status / people / org / tag headers thread the entity color through `getGroupColor` in `utils/task-grouping`, date-bucket / ungrouped headers leave it undefined) (thin wrapper over `TaskDraggable` with `surface='inset'` / `'lens'` — emits `inset-todo-<id>` / `lens-todo-<id>` + `data: { type: TASK_DRAG_KIND.task, todo }`; used by `ListInsetNode` and — via `ListDefinitionBody.renderRow` — `LensSlotContent` so floating + rail-docked list rows both ride the canvas `DndContext` and drop into any taskboard surface); rails/ (RailsFrame — 3×3 CSS grid (`[left-size] 1fr [right-size]` × `[top-size] 1fr [bottom-size]`) with `canvas-host` fixed at cell 2/2 and each rail placed via `computeRailGridArea(rails, side)` derived from the corner-ownership bag (`rails.corners: Partial<Record<Corner, 'h'|'v'>>` — `'h'` lets the top/bottom rail extend into NW/NE/SW/SE, `'v'` / absent keeps the legacy "verticals own all four corners" layout); corner ownership is still written by `DockOverlay` start/end drop sub-zones via `canvas-rails-store.setCornerOwner` / `clearCornerOwner`, but the per-corner toggle button is no longer rendered — layout is set at drop-time only, RailContainer, Slot — now `{ id, flex?, tabs: Tab[], activeTabId, orientation?, weekOffset? }` with every slot carrying ≥1 `Tab { id, type, listDefinitionId? }`; per-kind content choice lives on the active tab, not the slot, SlotHeader (single-tab slot chrome), TabStrip (multi-tab chrome rendered when `slot.tabs.length >= 2` — pill per tab + drag-reorder + `+` add-tab + per-pill ▾ caret for kind change; chrome row carries a slot-level `↗` pop-out button next to `⋯` — triage-2026-04-25 P4 moved the canonical pop-out here from `SlotMenu` / `WidgetKindMenu`), LensSlotContent (passes `DraggableTaskRow` through `ListDefinitionBody.renderRow` so rail lens rows drag into taskboard surfaces), CalendarSlotContent + CalendarStrip (7-day Monday-anchored, vertical/horizontal orientations, `RangeBar` week-nav + Today button; post-Phase 7 rows ride dnd-kit via `TaskDraggable surface='calendar-strip'` + day cells are `useDroppable` on `TASK_DROP_KIND.calendarDay` — reschedule dispatch flows through `dispatchTaskDrop` on the enclosing `DndContext`), calendar/EventRow + calendar-events.ts (`buildEntries` enriches bucket entries with people/orgs/status; `compact` mode used by horizontal columns), CalendarOrientationToggle (☰/☷ meta button mounted in `WidgetHeader` for slot + float), NotesSlotContent, TaskboardSlotContent, HorizonsSlotContent (renders `HorizonRibbon` + the selected-horizon's todo list using `DraggableTaskRow surface='lens'`; reads `settings.horizonSlots` / `selectedHorizon`; "Edit horizons…" opens `DashboardListsEditor filterIds={horizonDefIds}`), DraggableSlot — consumes `Slot.flex` via `--slot-flex` CSS var, SlotDivider (pointer-drag between sibling slots; snapshots every slot's measured px on pointer-down, writes pixel-valued weights via `canvas-rails-store.setSlotFlexBatch` so non-adjacent slots keep their size; min 80 px per slot, rAF-throttled), DockOverlay — renders per-empty-side drop strips split into `start` / `center` / `end` sub-zones (`start`/`end` claim the adjacent frame corner on drop, extending the sub-zone into the corresponding corner cell via `--left-size`/`--right-size`/`--top-size`/`--bottom-size`; `center` docks without claiming), SlotMenu — post-triage-2026-04-25 P4 "Split …" + add-tab only; pop-out moved to `TabStrip`'s chrome `↗` button; kind switching moved to `WidgetKindMenu`, rail-dnd — Phase 4A scaffolding + 4B lens wiring + 4C slot drag-dock + 4D 2-week calendar strip + 4E notes slot + 4F persistence through `settings.canvasRails` (extended with `widths`/`heights` bags for per-side rail sizes, clamped to [200, 600], and a `collapsed: Partial<Record<RailSide, boolean>>` bag persisted through the same settings pipeline); RailContainer mounts two pointer-driven controls on the canvas-facing edge, both rAF-throttled via the shared `useThrottledResize` hook: an invisible `RailEdgeStrip` that spans the full edge (drag-only, 6 px wide, `col-resize`/`row-resize` cursor) + a visible `RailEdgeHandle` button centered on the edge (20×56 px pill for vertical rails, 56×20 for horizontal — click toggles `canvas-rails-store.toggleRailCollapsed`, drag past a 3 px threshold resizes via `setRailSize`, `Enter`/`Space` toggle, arrow keys nudge by 20 px). When `collapsed[side] === true`, the rail renders as a ~28 px icon strip (`COLLAPSED_RAIL_PX`) with a kind-icon stub per slot, the edge strip is suppressed, `railSize(rails, side)` returns `COLLAPSED_RAIL_PX` so the frame grid shrinks to match, and the handle always treats pointer-up as a click (so a drag on a collapsed rail is a tap-to-expand instead of trying to pull it open mid-gesture); each `CollapsedSlotStub` registers the same `rails:slot:<id>` drop zone the expanded `DraggableSlot` would (`useDroppable` + `data-rails-drop-id`) so float / slot / tab drags can dock onto a collapsed rail via the small stub rect — triage-2026-04-25 P5; **the collapsed `<aside>` itself also carries `data-rails-drop-id={ kind: 'collapsed-side', side }`** (triage-2026-04-26 P1 / T3) so a release that lands inside the rail aside but misses every individual stub still resolves — `resolveFloatDockTarget` ranks slot zones above collapsed-side, so per-stub zones still take priority and the catch-all only fires on margin / between-stub / top-bottom padding, where the resolver bisects child stubs by axis distance and routes to the nearest stub via center-merge; stored widths/heights survive collapse so expand returns to the prior size; `TabStrip`'s chrome `↗` button invokes `popTabToCanvas(slot, activeTab.id)` (post-triage-2026-04-25 P4 the canonical pop-out path) which pops the active tab into the matching floating store and cascade-closes the source slot when its tab list empties; `popSlotToCanvas(slot)` is a thin wrapper that targets the active tab and is retained for tests / external callers — spawn position reads `settings.canvasViewport` upper-left + jitter. Both menu helpers delegate to the shared `popTabAtPosition(tab, canvasId, x, y, init?)` dispatcher (exported from `RailsFrame.tsx`) so the menu path and the Phase 5 float-dock drag path produce identical floating widgets. Tab reducers live on `canvas-rails-store` (`addTab`/`closeTab`/`activateTab`/`changeTabType`/`reorderTab`/`moveTabToSlot`/`detachTabToNewSlot`); `utils/rail-dnd` exports the matching pure reducers + the `tab-strip` drop-zone kind. **Float ↔ rail dock (bidirectional)**: floating widgets (note/calendar/inset/taskboard) can drag from the canvas onto a rail drop zone (`DockOverlay` empty-side strips, slot bodies, tab strips) and materialise as a docked tab/slot; conversely, a rail tab pill can drag into the canvas area and pop out as a floating widget. The float → rail path is React-Flow-driven (not dnd-kit): `CanvasView.handleNodesChange` publishes the dragging float to `ui-store.floatDrag` + tracks a window-level pointer, then on release calls `resolveFloatDockTarget(pointer, …)` (`utils/rail-dnd`) which hit-tests via `document.elementsFromPoint` + `data-rails-drop-id` attributes on `DockOverlay` sub-zones / `DraggableSlot` / `TabStrip` / `CollapsedSlotStub`; `CanvasPage.handleFloatDock` resolves the descriptor from the appropriate floating store (threading calendar `orientation`/`weekOffset` + lens `listDefinitionId` + taskboard `taskboardId`) and dispatches `canvas-rails-store.dockFloatIntoSlot` / `dockFloatAsNewSlot` — both reducers delete the source float row on success, mirroring `popTabAtPosition`'s deletion-on-pop. The window `pointermove` / `pointerup` / `pointercancel` / `blur` listeners attached at the leading edge of a float drag tear down via `requestAnimationFrame` rather than `queueMicrotask` (float-dock-bugs-2026-04-25 P1) so the React commit that flips `floatDrag` back to `null` (which unmounts `DockOverlay`'s drop strips) doesn't run until *after* `processBatch`'s release branch has hit-tested against the still-mounted DOM; `pointerRef.current` is intentionally NOT nulled in detach (same plan / same phase) because the cleanup can still race ahead of RF's `dragging:false` change on real Chromium and the release branch needs the captured release coords. The rail → canvas path (tab drag only — slot drags stay rail-only) is handled by `useRailsDragMonitor`'s `zone.kind === 'canvas'` branch against a full-viewport `useDroppable({ id: 'rails:canvas' })` that `CanvasView` registers behind React Flow; on release it maps pointer → flow coords via `pointerToFlowPosition` and dispatches `popTabAtPosition` + `closeTab`. `settings.canvasViewport` is null until React Flow's `onViewportChange` fires (it does not on initial render), so the branch falls back to identity (`{x:0,y:0,zoom:1}`) instead of bailing — triage-2026-04-25 P5 fixed a silent abort on a fresh canvas where the pop-out gesture appeared to do nothing. `<ReactFlow autoPanOnNodeDrag>` is gated on `useUIStore.floatDrag === null` (float-dock-bugs-2026-04-25 P2) so float-widget drags do *not* auto-pan the canvas — the gesture pulls the widget *toward* a rail (i.e. off-canvas), and the auto-pan rAF loop accumulates ~1.2k px of viewport drift over a typical ~700 ms center-zone dwell on an occupied slot. Project-node drags keep auto-pan because the gate inverts only when a float is in flight. Announcements ride `ui-store.floatAnnouncement` + the rails-monitor announcer (rendered as two parallel `aria-live=polite` regions by `RailsFrame`))
│   ├── taskboard/         → TaskboardPanel (dashboard/rail card; renders the **singleton** `Taskboard` — no nested `DndContext`, sortable entries ride the surrounding canvas/dashboard `DndContext` with `data.type='taskboard-task'`; drop outside any taskboard target → remove), TaskboardNode (canvas floating node keyed by `floatingId` — one per `FloatingTaskboard` row; all surfaces show the same entries)
│   ├── dashboard/         → HorizonRibbon (sortable rows + stacked-bar layout — N rows, each row = drag-handle + label button + stacked bar (scheduled vs. due segments) + count + × remove + footer "+ Add list" / "Edit horizons…"; replaces the 5-cell chart in stats-widgets-2026-04-25 P6; reads settings `horizonSlots: number[]` + `selectedHorizonDefId: number | null` + `listDefinitions`)
│   ├── overlays/          → CommandPalette, ReassignDialog, BulkConfirmDialog, UndoSnackbar, FilterSheet (mobile), ListDefinitionPickerPopup, QuickAddBar (keyboard-first task capture surface — single-row top-anchored bar at 120 px, no scrim, opened via `Ctrl+Space` / FAB / `command-registry.ts:openQuickAdd`; redesign-2026-04-26 trimmed it to chips/unmatched/hint rows below a 18 px title input + outlined `Details ⇥` button + muted `↵ create · esc close` cluster — no `+` glyph, no filled-accent Create button, no in-bar expanded panel, no Notes textarea, no footer "Open full editor →" link. Drives the live `parseInput` → `resolveInput` pipeline against the person / org / project / status / tag stores on every keystroke, renders read-only chips for every metadata field the resolver produces (`StatusChip` / `PersonChip` / `OrgChip` / `ProjectChip` / `TagChip` / `DateChip` / `RecurrenceChip` — auto-derived chips have no `×`; clear by editing the title text). Inline tokens covered by the empty-state hint row: `@person` · `/project` · `#tag` · `:status` · `!deadline (mon, fri…)` · natural dates (`tomorrow, fri 3pm`) · recurrence (`every week`). Autocomplete popup reuses `useNlpAutocomplete` (the same hook `InsertTrigger` consumes) and is portal-anchored to `document.body` with the `RuntimeFilterPicker` flip-above-when-clipping + clamp-on-right-edge math; tracks scroll (capture phase) + resize while open. **Tab and the visible `Details` button both hand off to `TaskEditPopup` create-mode** via the same `onOpenFullEditor` path: `App.tsx`'s wrapper stashes `{ rawTitle }` on `ui-store.quickAddDraft` (notes field dropped in the redesign — `QuickAddDraft` no longer carries it), closes the bar without clearing the draft, and dispatches `openCreatePopup`; `TaskEditPopup` reads the seed on mount and `closeEditPopup` clears it. Submit calls `useTodoStore.add` then `applyNlpMetadata` (`services/nlp-task-creator.ts`) for people / orgs / scheduled / deadline / recurrence / tags, mirroring `CanvasPage.handleAddTask` so `Ctrl+Space` lands tasks identically across surfaces. Title input carries `data-shortcut-scope="none"` so `use-keyboard-shortcuts.ts:34-44` skips global shortcuts while typing.)
│   ├── settings/          → PeopleEditor, OrgEditor, StatusEditor, TagEditor, ThemeColorsEditor, KeyboardShortcutsModal, DashboardListsEditor (modal-on-modal: list rows live in the outer "Lists" modal, the per-row edit form lives in `ListEditorDialog` portaled to `document.body` at `--z-dialog-overlay` (1002) above the Lists modal at `--z-dialog` (1001); row gear / name click both open the editor; Name + Filter + Sort + Grouping + Prompt all live in the editor body draft (row inline-rename + the `editing` / `startEdit` / `saveEdit` triple retired in lists-editor-modal P2); "Add to favorites" header button fires immediately, no Save needed; Esc / scrim / × / Cancel route through `guardDirty` → `showBulkConfirmation`)
│   └── shared/            → Chip, SectionHeader, ChipSelector, ColorInput, StatusIcon, AvatarStack, WidgetHeader (unified slot/widget chrome: kind icon via `utils/slot-kind.KIND_ICON`, drag handle, collapse, title, meta, pop-out ⇱ / dock ↙ / more ⋯ / close ×; `floating` prop switches to hover-revealed buttons with `nopan nodrag`; `onTitleClick` renders title as a ▾-caret button that fires anchor coords — carrier for the P3 kind selector; consumed by rails `SlotHeader` + `FloatingNoteNode` + `FloatingCalendarNode` + `ListInsetNode` + `TaskboardNode`), WidgetKindMenu (shared title-click popover — kind radios + optional "Edit list" item (lens-only; lists-consistency P1, callback wired by `SlotRenderer` + `ListInsetNode` to `useUIStore.openListEditorDialog(defId)` post-triage-2026-04-26 P5 / T7 / Q7=A so `<StandaloneListEditor>` mounts the per-list editor dialog directly without `DashboardListsEditor` underneath) + optional "Change list…" secondary (lens only; taskboard has no secondary because the board is a singleton); the secondary row opens an inline hover flyout rendered in-place via `ListDefinitionPickerBody` — single click on a list picks it and closes the whole menu, no separate popup + no per-row Pin/Add button; keyboard: ArrowRight opens / ArrowLeft / Esc closes; wired through `pickListForLens` (rails: `canvas-rails-store.updateSlot`; floats: `list-inset-store.update`). Add-tab / change-kind-to-lens / split-with-list flows still use `ListDefinitionPickerPopup` because they need a seed list before creating the widget; floats use `utils/float-kind-switch.convertFloatingKind` to delete the source float + create a new kind at the same rect), ListEditorDialog + ListEditorBody (shared body extracted to `components/shared/` in triage-2026-04-26 P5 / T7 — Name + Filter + Sort + Grouping + Prompt unified single-`<select>` controls; consumed by both `DashboardListsEditor`'s lists-manager flow and `<StandaloneListEditor>`'s manager-less flow), StandaloneListEditor (mounted once on `CanvasPage`; subscribes to `useUIStore.listEditorDialogId` and dispatches `<ListEditorDialog>` over `<ListEditorBody>` — owns its own draft / dirty state via `defsEqual` from `utils/list-def-equal.ts` and routes Cancel / Esc / scrim through `showBulkConfirmation` Discard prompt), notes/NotesEditor (CM6 wrapper + HTML→Markdown paste via `htmlToMarkdown`) + notes/NotesBody (shared editor + Alt-T-to-task via CM keymap + `→ ✓` toolbar button; no footer chrome; platform-aware shortcut labels via `utils/platform.formatShortcut`) + notes/NotesToolbar (Bold/Italic/H1/H2/Bullet/Checkbox + ⧉ copy-rich; dispatches CM commands via viewRef; parent owns copy handler that flushes note-store first), selection.module.css, dropdown.module.css
├── stores/                → Zustand (canvas, canvas-rails, todo, project, person, org, status, tag, list-inset, floating-note, floating-calendar, floating-taskboard, floating-horizons, floating-status, floating-scoreboard, floating-snooze-graveyard, todo-event, taskboard, ui, filter, undo, list-definition, settings, file-storage, note — note-store owns only the single global "outside-tasks" note; floating-note-store + floating-calendar-store + floating-taskboard-store + floating-horizons-store own per-canvas placement widgets that view their respective global content (note / shared calendar filter / singleton `Taskboard` / horizon ribbon state); taskboard-store holds a singleton `board` (`ensureLoaded()` seeds an empty row on first call; every entry op targets the single row — no `taskboardId` param); tag-store mirrors the org-store shape — `tags: Tag[]` + `assignedTagsMap: Map<number, Tag[]>` + CRUD + `assignTag`/`unassignTag`, case-insensitive uniqueness on insert/update; list-definition-store now carries `favorited: boolean` + optional `maxTasks` / `limitMode` on every def — `SavedView` was folded in by the v39 migration (`saved-view-store` retired), and ListView's Save/Load + Favorites chip bar are driven by this store; settings-store gained `defaultProjectGroupBy: ProjectGroupBy \| null` (default `'tag'`) read by `CanvasPage.handleAddProject` to seed `Project.groupBy` on every new project — triage-2026-04-26 P4 / T6; ui-store gained `listEditorDialogId: number \| null` + `openListEditorDialog` / `closeListEditorDialog` driving the manager-less per-list editor dialog mounted by `<StandaloneListEditor>` — disjoint from the existing `listsEditorOpen` manager flow, triage-2026-04-26 P5 / T7)
├── data/                  → Dexie repositories + migrations + restore + audit. Stats-widgets-2026-04-25 introduces `todoEvents` (v42; `++id, todoId, type, timestamp`) — append-only history log with a one-time backfill of `created` + `completed` events from existing todos, emit-on-change wiring inside `todo-repository` for `scheduledDate` / `dueDate` / `statusId` / `isCompleted`, and cascade-delete on todo removal — plus `floatingStatus` / `floatingScoreboard` / `floatingSnoozeGraveyard` placement tables (v43 / v44 / v45)
├── models/                → TypeScript interfaces
├── hooks/                 → Custom React hooks
├── utils/                 → Shared pure utilities (sort-order — `bySortOrder` comparator (sortOrder asc, id tiebreaker), dates, effective-date, filter — `toggleItem` + `matchTodoText(todo, query, ctx)`/`TextMatchField` for multi-field text search used by TopBar + `filter-store.matchesFilter`, platform — `isMacLike()` + `formatShortcut('Mod-…'/'Alt-…')` for ⌘/Ctrl/⌥/Alt-aware shortcut labels, slot-kind — `KIND_ICON` / `KIND_LABEL` maps, float-kind-switch — `convertFloatingKind` deletes a float row and recreates it at the target kind+rect, used by all four floating nodes on title-menu kind change, task-dnd/ — unified DnD vocabulary + dispatch: `kinds.ts` (`TASK_DRAG_KIND = { task, taskboardTask }`, `TASK_DROP_KIND = { task, project, taskboard, taskboardTask, calendarDay, listSection }`, `isTaskDragKind`), `ids.ts` (per-surface `taskDragId(surface, todoId, extras?)` for every `TaskSurfaceKey` value — `canvas-project` / `inset` / `lens` / `list` / `taskboard-panel` / `taskboard-float` / `calendar-view` / `calendar-strip` / `search` — plus `projectDropId` / `taskboardFloatDropId` / `calendarDayDropId` and `parseTaskboardEntryId` for splitting `tbp-<todoId>` / `tb-<floatingId>-<todoId>`), `collision.ts` (`buildTaskCollision(rules, fallback?)` returning a dnd-kit `CollisionDetection` from a declarative rule table — each rule scopes droppables and picks `pointerWithin` / `closestCenter` / `rectIntersection`; F12 rails-vs-task routing lives in `CanvasPage`'s rule table), `dispatch.ts` (`dispatchTaskDrop(event, { taskboard, calendar?, multiDragIds? })` — the single entry point for taskboard add / reorder / move-to-end / remove-on-drop-off + calendar reschedule; returns `false` for non-taskboard / non-calendar drops so the caller's route-specific handler can run), `search-drop.ts` (`computeSearchDropIndex(pointerY, entryRects)` — pure bisection helper used by TopBar's search-result drag-end handler to pick an insertion index over a taskboard panel))
└── services/              → NLP (parser emits `#tag` tokens; `nlp-resolver.resolveTags` does case-insensitive lookup-or-create against `tag-store` so `#Foo` and `#foo` collapse to one tag and unknown slugs create a registry row with `DEFAULT_ENTITY_COLOR`), command registry, file storage, undoable, backup scheduler, dashboard-lists interpreter (`buildDashboardLists` accepts `ctx.runtimeFilterValues: ReadonlyMap<defId, number[]>` and merges each pick list into its def's predicate as an OR over the declared `runtimeFilter.field` (`person`/`org`/`project`/`status`/`tag` — lists-consistency P5 widened to multi-value + added `tag`) via `applyRuntimeFilter` before membership runs; an empty array is a no-op, while a missing key returns `{ todos: [], runtimeFilterUnset: true }` so surfaces render a "Pick a {label}…" placeholder), horizons (`startOfWeek` + `classifyByDateSource(todo, today, weekStartsOn): 'scheduled' | 'due'` driving the new stacked-bar HorizonRibbon — the legacy bin-geometry helpers retired in stats-widgets-2026-04-25 P6), stats/ (`status-breakdown.selectStatusBreakdown(todos, statuses)` for the Status widget — open = `!isCompleted`, returns one entry per configured status in order + appends a synthetic 'No status' bucket only when statusId-null open rows exist; `discipline.selectDisciplineMetrics({ events, now, weekStartsOn, weeks=12 })` for the Scoreboard widget — three metrics in stable order (`defer` / `completion` / `lag`); `snooze.selectMostDeferred({ events, todos, limit=5 })` for the Snooze Graveyard widget — counts only future-shift `scheduled` events, joins against open todos, decorates with `oldestScheduled`; `buckets.weeklyBuckets(now, weeks, weekStartsOn)` is the shared 12-bucket helper), notes-export (Markdown ⇄ HTML clipboard helpers: `mdToHtml`, `htmlToMarkdown`, `copyNotesRich`), task-copy (`copyTasksRich(sections, ctx)` / `buildTasksHtml` / `buildTasksPlain` — mirrors `copyNotesRich`: walks flat todo sections in display order + writes `text/html` + `text/plain` via `ClipboardItem`, falls back to plain-text `writeText`. Sections are caller-pre-filtered/grouped; consumed by `ProjectNode`, `ListView`, and `ListInsetNode`'s ⧉ button)
```

## Data Flow

1. **App startup**: Dexie opens IndexedDB → `useCanvasStore.ensureDefault()` → `fileStorageService.initialize()` reconnects to saved file handle and loads file data into IndexedDB. Settings store loads theme mode and applies `data-theme` to `<html>`; only user-customized color overrides are set as inline styles. Expired completed tasks purged if `completedRetentionDays` configured. `backupScheduler.start()` begins periodic auto-snapshots.
2. **Normal use**: User acts in UI → Zustand stores call repository methods → Dexie persists to IndexedDB. If file storage is connected, Dexie hooks debounce-save all changes to the JSON file on disk. Cross-cutting task capture rides `QuickAddBar` (overlays/) — `Ctrl+Space` / FAB / `command-registry.openQuickAdd` flips `ui-store.quickAddOpen` and the bar's parser-driven flow runs `useTodoStore.add` + `applyNlpMetadata` on submit; the older `TaskEditPopup` create modal stays reachable only via the bar's Details / Tab handoff and the canvas right-click "New Task" menu.
3. **Canvas**: Single canvas only; `useCanvasStore.ensureDefault()` creates/selects it at startup.
4. **View switching**: Sidebar icons navigate between Canvas, List, Calendar, Settings via React Router. The horizon ribbon + selected-horizon list is a canvas widget (`horizons` kind) — drag onto any rail or drop it as a floating node. Post-stats-widgets-2026-04-25 P6 the ribbon is a user-editable ordered list of N rows (`settings.horizonSlots: number[]` + `selectedHorizonDefId: number | null`) seeded on first run with the same five default lists (This week / Next week / Rest of month / Later / Someday); each row renders as a labeled stacked bar (scheduled segment + due segment via `classifyByDateSource`). List view groups all todos by a user-selected sort-by attribute.
5. **Person assignment**: Many-to-many via `todoPeople` join; `usePersonStore` maintains an `assignedPeopleMap` cache.
6. **Person-org membership**: Many-to-many via `personOrgs` join; a person can belong to multiple orgs.

## Testing

Two layers, picked by what's being verified:

- **vitest (`npm test`)** — unit + JSDOM component tests under `src/test/**`. Authoritative for pure logic, models, repositories, store reducers, services, utility functions, and component rendering / event-handler dispatch that doesn't depend on real layout, focus, or async DOM timing. Coverage via `vitest run --coverage` (v8). `e2e/**` is excluded from the vitest sweep so Playwright specs don't get pulled in.
- **Playwright (`npm run test:e2e`)** — Chromium-only real-browser flows under `e2e/**`, configured by `playwright.config.ts` (fixed port 5173 via `--strictPort`, `reuseExistingServer`, `trace: 'retain-on-failure'`, desktop 1280×800). Runs against `npm run dev`. Specs today: `smoke`, `canvas-enter-chain`, `focus-trace` (diagnostic), `canvas-rail-dock`, `canvas-widget-kind-menu`. Test fixtures + CDP-trusted gestures live under `e2e/fixtures/seed.ts` (`seedCanvas`, `dragWidgetTo`, rail / WKM locators). CI is out of scope; tests are local-only.

**JSDOM is not authoritative** for: focus handoffs (the Enter-chain bug took three attempts because JSDOM tests passed while the real-browser bug persisted — see `docs/plans/features/real-browser-testing/`), `ResizeObserver`-driven layout, drag-and-drop hit-testing on rails / floats / taskboard / `data-rails-drop-id` / `data-tbp-entry` geometry, popover anchor placement / viewport flips, async-store-driven re-render timing, and canvas positional behavior (project drag + cascade shift, calendar reschedule, rail dock). Behaviors in those categories require a Playwright spec — JSDOM passing is not enough signal.

**Diagnosis vs. regression.** A Chrome DevTools MCP server (`mcp__chrome-devtools__*`) is wired up as a *spike* tool — drive an interaction live in real Chromium, capture `document.activeElement` / console / network / DOM, then codify the regression in Playwright once the assertions are known. CLAUDE.md "Tools — Chrome DevTools MCP" has the workflow and the don't-substitute-for-unit-tests guardrails.

## Detail References

Load the relevant file when you need per-abstraction detail. Each is a table of `Abstraction | Location | Purpose` rows.

| File | Covers |
|------|--------|
| [architecture/models-and-data.md](architecture/models-and-data.md) | Models (`src/models/`), repositories, migrations, validation, restore (`src/data/`) |
| [architecture/state.md](architecture/state.md) | Zustand stores, filter semantics, store helpers (`src/stores/`) |
| [architecture/components.md](architecture/components.md) | Views, task/canvas/overlay/shared/layout/settings/taskboard components, ListView bucket builders |
| [architecture/services-and-utils.md](architecture/services-and-utils.md) | Services (dashboard-lists interpreter, NLP, recurrence, file storage, exports), hooks, pure utils |

---

**Update this file (and the matching reference) whenever you add new modules or change the dependency graph.**
