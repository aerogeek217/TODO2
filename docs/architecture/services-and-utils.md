# Services, Hooks & Utils

Detail reference for `src/services/` (non-UI logic), `src/hooks/` (custom React hooks), and `src/utils/` (pure utilities). Load when touching NLP, recurrence, file storage, dashboard list interpretation, drag-and-drop logic, keyboard shortcuts, or date helpers.

## Services

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| buildDashboardLists, interpretMembership, interpretSort, interpretGrouping, applyRuntimeFilter, WARNING_WINDOW_DAYS | services/dashboard-lists.ts | Pure interpreter over `ListDefinition`. Membership kinds: today (≤ today OR deadline within `warningWindowDays`, default WARNING_WINDOW_DAYS=3), upcoming (has date, not in today), deadlines (has `dueDate`, intentional overlap with today), someday (no dates), custom (injected `ctx.evalPredicate` consumes a `TodoPredicate`). **Sort + grouping switch on flat `TodoSortBy` / `TodoGroupBy` literals post-v46** (ui-consistency-2026-04-25 P4 — the prior discriminated-union dispatch retired): `interpretSort` maps `'manual'` → sortOrder, `'date'` / `'scheduled'` / `'deadline'` → date-asc compares (chronological with `effectiveDate` / `resolvedScheduled` / `dueDate`), categorical literals (`'people'` / `'org'` / `'project'` / `'status'`) fall back to sortOrder; `interpretGrouping` maps `'none'` → ungrouped, `'date'` / `'scheduled'` / `'deadline'` → relative bucketing (today / week / month / later via `bucketByDate`), `'tag'` → `bucketByTag` (reads `ctx.assignedTagsMap` → registry rows for bucket label + color; a todo with multiple tag assignments appears in every matching bucket), `'people'` / `'org'` / `'project'` / `'status'` → many-to-many or single-key bucketing. Applies `showCompleted` + `showHiddenStatuses` gates. Date-based sort + bucket compare by each todo's own date with id as a stable tiebreaker. Runtime filter: when a def declares `runtimeFilter`, the interpreter reads the caller's picks from `ctx.runtimeFilterValues: ReadonlyMap<defId, number[]>` and `applyRuntimeFilter(predicate, spec, values: number[])` merges them into the predicate as an OR over the declared field (`person`/`org`/`project`/`status`/`tag` — lists-consistency P5 widened to multi-value and added `tag`) before membership runs, replacing any prior id filter on that field while preserving other clauses. An empty array is a no-op (predicate passes through unchanged — matches "user cleared all chips" UX). Missing pick → list returns `{ todos: [], runtimeFilterUnset: true }` so surfaces render a "Pick a {label}…" placeholder instead of an empty state. `applyRuntimeFilter` is also called directly by `ListView` (post-list-bugs P3) — `/list` doesn't route through the interpreter, so it round-trips its `filter-store` criteria through `criteriaToPredicate` → `applyRuntimeFilter(pred, spec, values)` → `predicateToCriteria` before `applyFilter`, short-circuiting to an unfiltered list when no value is picked or the array is empty. **Code-review-2026-04-25 P2**: the legacy `weekBoundaries(today)` helper (which silently hardcoded Sunday-first weeks) is gone — every relative-date bucketer (`bucketByEffective` / `bucketByDeadline` / `bucketByScheduled`) now thin-wraps `utils/bucket-by-date.bucketByDate` against `dateBucketBoundaries(today, weekStartsOn)`, and the people/tag bucketers route through `utils/bucket-by-many.bucketByMany` (org keeps an inline walk for the inferred-via-person path). `interpretGrouping` threads `weekStartsOn` from `ctx`; the dead `default:` arm in `compareBySortBy` was replaced with an explicit `'name'` exhaustiveness case |
| buildEntries, dayKey, CalendarEntry, BuildEntriesOptions | services/calendar-entries.ts | Shared bucket-and-enrich helper for every calendar surface (CalendarView grid + canvas rails calendar strip — extracted in code-review-2026-04-25 P2 from the prior duplicate at `components/canvas/rails/calendar/calendar-events.ts`). `buildEntries(todos, days, options)` walks each todo, places it on `resolvedScheduled ?? deadline` (matching CalendarView's drag-to-reschedule semantic — falling back to `effectiveDate` would clamp the card back to `min(scheduled, deadline)`), generates virtual recurring instances within the window via `generateRecurringInstances`, and enriches every entry with people / orgs / status. `BuildEntriesOptions.sortMode` picks within-day ordering: `'effective'` (`CalendarView` grid — `effectiveDate` ascending, `sortOrder` tiebreak) vs `'sortOrder'` (canvas calendar strip — pure `sortOrder` ascending). `dayKey(d)` is the canonical bucket key (UTC midnight ISO). The thin re-export at `components/canvas/rails/calendar/calendar-events.ts` keeps the canvas-strip import path stable |
| buildExportData | services/export-import.ts | Reads all DB tables (incl. `listDefinitions`) in parallel; shared by file-storage, settings export, and backup snapshots |
| buildMarkdownExport | services/export-import.ts | Builds markdown representation of all tasks grouped by project; flat list per project (no parent/child nesting); shows `[status.name]` for meaningful statuses (icon or hideByDefault); uses buildExportData |
| fileStorageService | services/file-storage.ts | File System Access API sync (file ↔ IndexedDB); uses onAfterImport callback for store refresh; `onConfirmMigration` callback pauses import of legacy-format files pending user confirmation. Post-code-review-2026-04-25 P11 the previously-silent `.catch(() => {})` arms on `snapshotBeforeDestructive` + `clearFileHandle` log a `console.warn` so a failure mode no longer disappears |
| backupScheduler | services/backup-scheduler.ts | Auto-snapshot every 24h, pre-destructive snapshots, prune to 10 max; started in App.tsx. Post-code-review-2026-04-25 P11 the interval body gates on `document.visibilityState === 'visible'` so a backgrounded tab doesn't accumulate snapshots; an `import.meta.hot?.dispose(() => backupScheduler.stop())` guard tears the timer down on Vite HMR so dev sessions don't fan out duplicate intervals across reloads |
| checkMigrationNeeded | services/migration-check.ts | Checks IndexedDB version via `indexedDB.databases()` before Dexie opens; converts IDB version (Dexie multiplies by 10) to Dexie version for comparison; returns `MigrationInfo` if data-modifying upgrade is pending |
| detectLegacyFormat | services/migration-check.ts | Inspects raw parsed JSON for legacy fields (`isStarred`/`isAssigned` booleans, `starred` list insets, `priority`/`isHardDeadline` todo fields, `high-priority`/priority-`attributeFilter` list insets); returns `LegacyImportInfo` with per-category counts and human-readable descriptions |
| exportCurrentDatabase | services/migration-check.ts | Reads all tables from raw IndexedDB at a specified version (without triggering Dexie upgrade); returns JSON string |
| undoable | services/undoable.ts | Helper to register an action as undoable; skips when undo store is mid-undo/redo |
| task-placement | services/task-placement.ts | Pure functions for task ordering: computeInsertionSort, placeTaskAt, placeMultipleAt, normalizeSortOrders, shouldNormalize |
| pasteTasksAt | services/clipboard.ts | Paste cut tasks at a target position using placeMultipleAt + applyMutations; clears clipboard after paste. Post-code-review-2026-04-25 P11 lazy-imports its store deps (`useUIStore` / `useTodoStore` / `task-placement`) so this service module can be loaded by lower-layer callers (utils/data) without dragging the Zustand store graph into their bundle. Mirrors the lazy-import pattern in `command-registry`, `nlp-task-creator` (`useTagStore`), and `float-kind-switch` |
| drop-resolver | services/drop-resolver.ts | Pure drop target resolution: resolveDropTarget (DropResolution), resolveDropPreview (preview indicators) |
| parseInput | services/natural-language-parser.ts | Parses raw text for NLP tokens: `@name` or `@"First Last"` (`MENTION_PATTERN` — emits `'person'`-typed tokens; `nlp-resolver.ts` decides person vs org), `/project`, `#tag` (whitespace-anchored; group-1 separator stays in the remaining title; slug is `normalizeTag`-folded in first-seen order), date keywords (→ `scheduledDate`), deadline syntax `by <date>` / `!<day>` (→ `dueDate`, fuzzy windows resolve to end-of-window), recurrence (`every week`, `every quarter`, `repeat daily`); returns ParsedInput with cleaned title, persons[], projects[], tags[] (string slugs — `resolveTags` turns them into registry ids downstream), scheduledDate, dueDate, recurrence |
| makeRecurrenceRule | services/recurrence.ts | Build a RecurrenceRule, capturing originalDayOfMonth for monthly/quarterly/yearly |
| computeNextDueDate | services/recurrence.ts | Advances a due date by one recurrence interval, skipping past dates |
| recurrenceAnchor, advanceRecurring | services/recurrence.ts | `recurrenceAnchor(todo)` picks `dueDate` first, falls back to a precise `scheduledDate` (returns null for fuzzy-only). `advanceRecurring(todo)` computes the next-occurrence field update — either `{dueDate}` or `{scheduledDate: {kind:'date', value}}` — used by `todo-store.toggleComplete` / `bulkSetCompleted` and CalendarView virtual instances. **Day-of-month integrity (post-code-review-2026-04-25 P11):** monthly/quarterly/yearly read `originalDayOfMonth` from the rule (captured at `makeRecurrenceRule`), so a Jan-31 monthly that lands on Feb-28 elevates back to Mar-31; a Feb-29 yearly that lands on Feb-28 in year N+1 elevates back to Feb-29 in the next leap year. **DST:** `next.setHours(0, 0, 0, 0)` re-anchors to local midnight after every interval bump so spring-forward / fall-back can't leak a 23:00 / 01:00 timestamp into stored rows |
| generateRecurringInstances | services/recurrence.ts | Generates all recurring dates within a date range for calendar display |
| resolveInput | services/nlp-resolver.ts | Matches parsed person/project/org names against known entities (case-insensitive exact/prefix/initials/first-name); person-first for @tokens, unmatched fall through to org matching; returns personIds[], orgIds[], projectId, unmatched names, plus the pass-through `tags: string[]` from `ParsedInput` (converted to ids by `resolveTags`) |
| resolveTags | services/nlp-resolver.ts | Async resolve-or-create: for each `#foo` slug, look up the existing tag case-insensitively in `ctx.tagStore.tags` and return its id; on a miss, `tagStore.add(name, DEFAULT_ENTITY_COLOR)` and use the new id. Returns ids in input order, deduped. A local `createdByLower` cache coalesces duplicate new slugs within the same call so `#Foo #foo` creates one tag, not two. Repository-level concurrency (two near-simultaneous NLP calls racing to create the same slug) is handled by `tagRepository.getOrCreate` — the single-transaction upsert serialises the check-and-insert so only one row lands, and `tagStore.add` is idempotent on a case-insensitive match |
| parseTaskInput | services/nlp-task-creator.ts | Combines parseInput + resolveInput; `applyNlpMetadata` assigns parsed metadata (people, orgs) inside `runNlpMetadataTransaction`, then calls `resolveTags` against `useTagStore` and assigns each resolved tag id via `tag-store.assignTag` — tag writes live outside the transaction because the registry rows are normalized and don't need atomic ordering with person/org assignment. Post-quickadd-bar P4 `applyNlpMetadata` is the shared submit-side fan-out for both the per-project `InsertTrigger` / `CanvasPage.handleAddTask` flow and the cross-cutting `QuickAddBar` capture surface (`AppQuickAddBar` in `App.tsx`); the QuickAddBar call site passes the parser-derived `ResolvedInput` through unmodified, including `statusId` from the `:status` token (post-quickadd-bar P6). The submit helper is intentionally left inline at each call site rather than extracted to `quickadd-submit.ts` — the shape is small and the two surfaces have minor differences (lens default project resolution, draft seeding) |
| ensureDefaultProject | services/ensure-default-project.ts | Resolve a project id to attach a newly-created task to on a given canvas (triage-2026-04-27 P6 / Q7=A). Returns the first project on the canvas if any exist (handles "user removed the previous default" edge); otherwise calls `useProjectStore.getState().add('Inbox', canvasId, 0, 0, settings.defaultProjectGroupBy)` to materialize an Inbox project at (0, 0), then `useSettingsStore.getState().setDefaultProjectId(id)` so subsequent submits reuse it. Mirrors the orphan-project pattern `useProjectStore.remove` already uses. Consumed by `AppQuickAddBar.handleSubmit` so a blank-canvas Quick Add lands on a visible project; pre-fix the call passed `canvasId=undefined` unconditionally and the row was invisible everywhere except IndexedDB |
| convertFloatingKind | services/float-kind-switch.ts | Deletes the source float record and creates a fresh float of the target kind at the same `{x, y, width, height}`. Resolves `listDefinitionId` (lens) from caller seed or the list-definition store; taskboard targets need no resolver because the board is a singleton. Aborts and leaves the source intact when no list-def is available for a lens target. Used by every floating widget's title-menu kind-change path. Post-code-review-2026-04-25 P3 the body is registry-driven: source removal + target creation route through `floatKindBySlotKind(kind)` + `floatKindByDragKind(kind)` from `utils/float-kind-registry`, so every per-kind switch now lives in one table |
| FLOAT_KIND_REGISTRY, FloatKindEntry, floatKindBySlotKind, floatKindByDragKind, floatKindForNodeId, isFloatNodeId | utils/float-kind-registry.ts | Single source of truth for every floating canvas widget kind (introduced in code-review-2026-04-25 P3). Five entries keyed by `slotKind` (`lens` / `notes` / `calendar` / `taskboard` / `horizons`) carry the matching `floatDragKind` (the `ui-store.floatDrag` discriminator), `domPrefix` (the React Flow node id prefix — `inset-` / `note-` / `calendar-` / `taskboard-` / `horizons-`), `label` (a11y), `defaultRect` (spawn / pop-out / convert defaults), and four reducer-shaped operations: `remove(id)`, `addFloat({canvasId, x, y, listDefinitionId?})`, `setSize(id, w, h)`, `buildDescriptor(floatId) → FloatDescriptor \| null`. Consumers: `CanvasView.handleNodesChange` (prefix decode via `floatKindForNodeId`); `CanvasPage.handleFloatDock` (descriptor build); `services/float-kind-switch.convertFloatingKind` (cross-kind delete + add). Adding a sixth widget kind: append a new entry here, define the matching floating-* store, wire the corresponding pop-in / dock paths — every other consumer iterates this list with no per-kind branches |
| saveFileHandle, loadFileHandle, clearFileHandle, saveLastPickerHandle, loadLastPickerHandle | services/file-handle-idb.ts | File System Access API handle persistence in a dedicated `todo2-meta` IndexedDB (separate from the main Dexie DB so legacy-import "clear all tables" won't drop the handle). Handles stored under namespaced keys (`todo2-primary`, `todo2-last-directory`); read paths transparently migrate legacy un-namespaced keys forward on first access. Post-code-review-2026-04-25 P11 the cached IDB connection registers an `onversionchange` handler that closes the cache so a parallel tab's schema upgrade doesn't leave us holding a stale connection |
| startOfWeek, classifyByDateSource | services/horizons.ts | `startOfWeek(today, weekStartsOn)` is the canonical week-start helper consumed by `weeklyBuckets` (`services/stats/buckets.ts`), `dateBucketBoundaries`, and any other relative-week math. `classifyByDateSource(todo, today, weekStartsOn): 'scheduled' | 'due'` (stats-widgets-2026-04-25 P6) reports which date drove a todo's horizon placement — mirrors `effectiveDate` priority (scheduled wins regardless of past/future, then due, then 'scheduled' fallback for someday rows); drives the per-row stacked-bar split in the rewritten `HorizonRibbon`. The legacy `HORIZON_KEYS` / `HORIZON_GRAIN` / `horizonBinRanges` / `horizonBins` / `horizonSomedayCount` / `BinStats` exports retired in P6 alongside the 5-cell chart |
| stats/status-breakdown (selectStatusBreakdown, StatusBreakdownEntry) | services/stats/status-breakdown.ts | Pure selector for the `status` widget (stats-widgets-2026-04-25 Phase 2). Open = `!isCompleted` (D4's `status.kind !== 'done'` clause is a no-op until `Status.kind` lands; see P2 handoff). Returns one entry per configured status in `statuses` order — `{id, label, icon, color, count}` — including zero-count statuses so the legend lists every configured status. Appends a synthetic `{id: null, label: 'No status', icon: 'circle', color: 'var(--color-text-muted)', count}` bucket only when statusId-null open todos exist. Skips `Status` rows with no id defensively |
| stats/discipline (selectDisciplineMetrics, ScoreMetric, DisciplineMetricsInput) | services/stats/discipline.ts | Pure selector for the `scoreboard` widget (stats-widgets-2026-04-25 Phase 4). Walks `events` once into `scheduledCountByTodo` / `firstScheduledByTodo` / `earliestCompletedByTodo` maps; iterates `weeklyBuckets(now, weeks ?? 12, weekStartsOn)` to compute three metrics per bucket: **defer** (avg `scheduled` event count per todo completed in this week), **completion** (% of cohort whose first `scheduled.toValue` lands in this week AND who completed by week end), **lag** (avg `(completed.timestamp - first scheduled.timestamp)` in days for completions in this week). Empty / missing buckets emit 0 (UI's delta formatter treats 0 cohort as "no signal"). Fuzzy `scheduled.toValue` (`'fuzzy:<token>'`) doesn't contribute to the completion cohort — we don't store the as-of date so we can't resolve the token retroactively. Returns three `ScoreMetric` objects in stable `defer / completion / lag` order with `lowerIsBetter` + `format` carried for the UI's delta arrow + label |
| stats/snooze (selectMostDeferred, SnoozedTask, MostDeferredInput) | services/stats/snooze.ts | Pure selector for the `snoozeGraveyard` widget (stats-widgets-2026-04-25 Phase 5). Counts only future-shift `scheduled` events (`typeof both === 'string'`, neither `fuzzy:`-prefixed, `to > from`); groups by `todoId`; joins against open todos (drops completed via `!isCompleted` and orphans whose todo no longer exists); sorts by `count` desc with `todoId` asc as a stable tiebreaker; takes top N (default 5); decorates each row with `oldestScheduled = parseToValueDate(firstScheduled.toValue)` (`null` when first scheduled was fuzzy / unset, in which case the UI omits the "since" label). Non-`scheduled` event types are ignored entirely |
| stats/buckets (weeklyBuckets, WeekBucket) | services/stats/buckets.ts | `weeklyBuckets(now, weeks, weekStartsOn): WeekBucket[]` builds N consecutive non-overlapping weekly windows ending in the week containing `now`. Returned chronologically — `[oldest, …, current week]`. Honors `weekStartsOn` so adjacent week ranges never overlap. Used by the discipline scoreboard for its 12-week window (stats-widgets-2026-04-25 Phase 4); reuses `startOfWeek` from `services/horizons.ts` |
| notes-export (mdToHtml, htmlToMarkdown, copyNotesRich, sanitizeHref) | services/notes-export.ts | Markdown ⇄ HTML clipboard helpers for the notes toolbar's rich-copy action. `sanitizeHref` uses an allowlist (http/https/mailto/tel + schemeless) to neutralize `javascript:` / `data:` / `vbscript:` / `file:` / `blob:` URLs in both directions; Phase 0 security hotfix |
| task-copy (copyTasksRich, buildTasksHtml, buildTasksPlain, CopyTaskSection, CopyTaskContext) | services/task-copy.ts | Rich-text clipboard export for a flat todo list — parallels `copyNotesRich`. Caller supplies one or more `CopyTaskSection`s (flat view = one unlabeled section; grouped view = one section per heading) and a `CopyTaskContext` with `assignedPeopleMap` + `statusMap`. Renders each todo as one line of plain text (`[ ] title [Status] (sched: …) (deadline …) @people`) + one flat `<ul><li>☐ …</li></ul>` (with `<h2>` per-section headings); writes both `text/html` + `text/plain` via `ClipboardItem`, falling back to `navigator.clipboard.writeText(plain)` when `ClipboardItem` is absent / rejects. Consumed by `ProjectNode`, `ListView`, and `ListInsetNode`'s ⧉ button; the caller pre-filters completed / hidden rows — the helper just renders what it's given |

## Hooks

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| useIsMobile | hooks/use-is-mobile.ts | Reactive mobile detection hook (640px breakpoint via matchMedia + useSyncExternalStore) |
| useResolvedTheme | hooks/use-resolved-theme.ts | Reactive resolved theme hook ('light' \| 'dark'); combines Zustand themeMode with OS prefers-color-scheme via useSyncExternalStore |
| useKeyboardShortcuts | hooks/use-keyboard-shortcuts.ts | Global keyboard shortcut handler: undo/redo, task navigation (Arrow/Home/End), task actions (Enter/Space/Delete/Insert), movement (Ctrl+Arrow/Tab), chord navigation (G then C/L/A/S), filter focus (F), select all (Ctrl+A), keyboard shortcuts modal (?) |
| useBulkActions | hooks/use-bulk-actions.ts | Hook wrapping mutations with multi-select awareness (toggleComplete, remove, setStatus, setScheduled, setDeadline, setProject, quickAssign/Unassign person/org); called via `useTaskRowActions` from `TaskRow` + `MobileTaskRow` |
| useTaskRowActions, buildTaskRowMenuItems | hooks/use-task-row-actions.ts | Shared row action surface for `TaskRow` + `MobileTaskRow`. `useTaskRowActions({todo, ghost?, onTaskboard?})` returns `{ bulk, handleToggleComplete, handleDelete }` — `handleDelete` becomes `taskboard-store.removeEntry` when `onTaskboard` is true (mirrors the Taskboard "Delete" → "Remove from Taskboard" relabel), otherwise routes through `bulk.remove`. `buildTaskRowMenuItems({todo, onBoard, onTaskboard?, onOpenDetail?, onMoveToProject?, onComplete, onDelete})` is the pure menu-builder consumed by all three context-menu surfaces (TaskRow right-click, MobileTaskRow long-press, TopBar search-results context menu via `top-bar-search-menu.buildSearchContextMenuItems`); pure (no store reads) so tests cover the menu shape without mounting a row. Extracted in code-review-2026-04-25 P6 |
| useTaskEditCallbacks | hooks/use-task-edit-callbacks.ts | Shared TaskEditPopup wiring: onCreate (NLP + metadata), editProps (assignments, actions), entityCreators — used by CanvasPage, DashboardView, ListView, CalendarView |
| useCanvasDnD | hooks/use-canvas-dnd.ts | DnD state, edge panning, drag handlers (including handleDragCancel), drop execution; shared resetDragState cleans up on Escape/focus-loss |
| useInlineEdit | hooks/use-inline-edit.ts | Inline title editing: state, focus, save/cancel, 250ms click-to-edit timer |
| useClickOutside | hooks/use-click-outside.ts | Click-outside detection hook for closing dropdowns/menus |
| usePopoverAnchor, PopoverAnchor, PopoverPlacement, PopoverStyle | hooks/use-popover-anchor.ts | Shared lifecycle hook for popover panels: anchor positioning + flip + clamp + scroll/resize/Escape/outside-click dismissal. Two anchor flavours via discriminated union: `{kind:'ref', ref}` reads `getBoundingClientRect()` from a trigger element each compute pass (used by element-trigger popovers like `RuntimeFilterPicker`); `{kind:'point', x, y}` pins to viewport coords (used by right-click / contextmenu popovers — `CanvasContextMenu`, `ProjectPickerPopup`, `ListDefinitionPickerPopup`, `WidgetKindMenu`, `FilteredListPopup`, `SlotMenu`). Inputs: `anchor` / `open` / `placement?='bottom-start'` / `offset?=4` / `closeOnScroll?=true` / `closeOnResize?=true` / `closeOnEscape?=true` / `closeOnOutsideClick?=true` / `onClose` / `extraInsideRefs?` (other elements that count as "inside" — used by `WidgetKindMenu` for its nested Stats / Change-list flyouts and by `RuntimeFilterPicker` for its label+chips wrapper). Outputs: `panelRef` (callback ref to set on the panel root) + `style` (`{position:'fixed', left, top, maxWidth, maxHeight}`) + `placementUsed` (final placement after any flip). Caller owns the panel + its `createPortal(...)` wrapper. Behavior: initial position lands in `useLayoutEffect` (before paint, no flicker); `bottom-*` flips to `top-*` when the panel would clip the viewport bottom; `*-start` flips to `*-end` for the right edge; capture-phase scroll listener catches ancestor scrolls (e.g. inset bodies); `closeOnScroll: false` + ref anchor → reposition mode (panel tracks the trigger), `closeOnScroll: false` + point anchor → static (the click coords are static); 8 px viewport margin all sides; inputs captured in refs internally so the hook stays stable across renders even when callers pass an inline `anchor` object literal. Built ui-consistency P1 |
| useNlpAutocomplete | hooks/use-nlp-autocomplete.ts | Hook for `@` / `/` / `#` autocomplete in input fields: tracks trigger position (whitespace-anchored), filters people/orgs/projects/tags (case-insensitive prefix match for `#`), handles arrow/Tab/Enter/Escape navigation. Accepting a tag suggestion completes the `#slug` text; the existing NLP pipeline + `resolveTags` then turns it into a registry id on save. A missing match on `#` surfaces a "create #new-tag" affordance so unknown slugs still create a row |
| useRightEdgeFlip | hooks/use-right-edge-flip.ts | Shared `useLayoutEffect` panel-flip hook for dropdown panels that anchor to a chip's left edge by default. Returns `[panelRef, align]` (`'start' \| 'end'`); on the next layout tick after `open` flips `true`, measures `panel.getBoundingClientRect().right > window.innerWidth - 8` and switches `align` to `'end'` so the consumer renders `data-align="end"` (`topBar.dropdownPanel` flips to `right: 0; left: auto`). Consumed by every chip-row picker in `ListFilterEditor` (Project / People / Org / Tags / Status / Date); bugs-ui P1 extracted it from the per-picker `useLayoutEffect` originally added to `DateRangeDropdown` in list-bugs P2 so right-edge overflow is handled in one place |
| useFloatingWidget | hooks/use-floating-widget.ts | Shared chrome plumbing for the five floating canvas widgets (note / calendar / horizons / taskboard / list-inset). One call returns `{ headerProps, kindAnchor, setKindAnchor, handleClose, handleDock, handleChangeKind }` — `headerProps` spreads directly into `<WidgetHeader>` (close / dock / title-click). `handleDock` calls `useCanvasRailsStore.createAndDockSlot(kind, dockSeed)` then deletes the float; `handleChangeKind` dispatches `services/float-kind-switch.convertFloatingKind`. `kindAnchor` state (anchor coords for `<WidgetKindMenu>`) is exposed for the caller to render itself, since lens variants thread `pickListForLens` / `onEditList` / `secondaryLabel` while the simpler kinds don't. Extracted in code-review-2026-04-25 P3 from ~50 LOC of duplicate handler code in each floating node |
| useDefaultRails | hooks/use-default-rails.ts | Hydrate-on-first-mount + persist-back hook owned by `RailsFrame`. Reads `settings.canvasRails` (from `useSettingsStore`) and dispatches one of two independent effects: **persisted hydration** runs as soon as `canvasRails` carries a non-empty rail (no list-defs gate — saved slots already carry their `listDefinitionId` references); **default seed** only fires when there's no persisted layout AND `listDefinitions.length > 0`, then seeds a fresh right-side `thisweek` lens slot using the canonical horizon list def. Splitting the two paths (real-browser-testing 6.5.1) prevents a slow IDB read on `listDefinitions` from blocking persisted hydration — pre-fix, a real user with persisted rails but a not-yet-loaded list-def store would render an empty canvas frame for one tick. Subsequent `rails` updates are persisted back through `setCanvasRails`. Extracted in code-review-2026-04-25 P5 from RailsFrame's inline effect |
| useRailsDragMonitor | hooks/use-rails-drag-monitor.ts | Wires `@dnd-kit/core.useDndMonitor` to the canvas-rails store. On drag start of a rail slot/tab (`active.data.current.type === RAILS_DRAG_TYPE`), publishes the active drag and announces "Dragging <kind>" via the rail-monitor announcer. On drag end, decodes the dropped zone via `decodeRailsDropId`, routes to the matching reducer (`dropSlotToSide` / `splitDropSlot` / `reorderTab` / `moveTabToSlot` / `detachTabToNewSlot`), wires corner ownership via `applyEmptySideCorners`, and — for tab releases over the canvas (`zone.kind === 'canvas'`) — pops the tab out via `popTabAtPosition` (using `pointerToFlowPosition` + `settings.canvasViewport` with identity-fallback per triage-2026-04-25 P5). Returns `{ draggingSlot, announcement }` for the parent to render `DockOverlay` + the `aria-live` region. The `window` `pointermove` listener is tracked in a typed `cleanupRef` with an unmount-time effect, so a provider teardown mid-drag doesn't leak the listener when dnd-kit swallows the end/cancel. Extracted in code-review-2026-04-25 P5 (~230 LOC out of RailsFrame) |
| useFloatDragLifecycle | hooks/use-float-drag-lifecycle.ts | Owns drag-session lifecycle for canvas float widgets (note / calendar / lens / taskboard / horizons) and project nodes alike. Tracks which nodes are mid-drag (`draggingIds: MutableRefObject<Set<string>>`), fires position-persist callbacks (`onNoteDragStop` / `onCalendarDragStop` / `onInsetDragStop` / `onTaskboardDragStop` / `onHorizonsDragStop` / `onNodeDragStop`) on release, runs the float-dock hit-test (Phase 4 float-dock — `resolveFloatDockTarget` against `data-rails-drop-id` hotspots), publishes `ui-store.floatDrag` for `DockOverlay` rendering, and toggles the window-level `pointermove` / `pointerup` / `pointercancel` / `blur` listeners around the leading/trailing edges of a float drag. Returns `{ draggingIds, droppedPositions, processBatch }` for `CanvasView.handleNodesChange` to sequence with `useCascadeShifts`. A `wasMultiDragRef` flag gates the hit-test to single-drag releases. Extracted in code-review-2026-04-25 P5 from the inline drag bookkeeping in `CanvasView.handleNodesChange`. **Cleanup is rAF-deferred, not microtask-deferred** (float-dock-bugs-2026-04-25 P1): clearing `floatDrag` synchronously off `pointerup` would unmount `DockOverlay`'s drop strips before `processBatch`'s release branch runs its `elementsFromPoint` hit-test, so float drops onto an empty rail side silently position-persisted instead of docking. `requestAnimationFrame` fires after React commits the next render, so `processBatch`'s trailing-edge cleanup nulls `pointerCleanupRef.current` first and the rAF callback short-circuits; the ~16 ms ceiling caps how long a swallowed release leaves `floatDrag` stuck. The detach branch deliberately does NOT null `pointerRef.current` — the cleanup can still race ahead of RF's `dragging:false` change, and the release branch needs the captured release coords to dispatch the dock |
| useCascadeShifts | hooks/use-cascade-shifts.ts | Owns the cascade-shift bookkeeping previously inlined in `CanvasView.handleNodesChange`. Returns `{ detectAndCacheDimChanges, processSetNodesUpdate, persistCascadeShifts }` — three pure transforms the caller threads through its `setNodes` updater. `detectAndCacheDimChanges` runs *before* `applyNodeChanges` so deltas are computed against pre-change heights. `processSetNodesUpdate` picks one of three branches (single-node drag → snap-align via `findAlignmentsScoped`; idle + dim deltas → `computeCascadeShifts` + cascade-persist queue; otherwise pass-through). `persistCascadeShifts` writes to `droppedPositions` for visual sync, then debounces the store callback so a stream of intermediate height changes only commits the final positions. Extracted in code-review-2026-04-25 P5 |
| popTabAtPosition, popTabToCanvas, popSlotToCanvas, computePopOutFlowPosition | services/rail-pop-out.ts | Shared pop-out dispatcher for the rail → canvas direction. `popTabAtPosition(tab, canvasId, x, y, init?)` is the pure dispatcher that switches on `tab.type` and routes to the matching floating-* store's `add` (calendar threads `init.orientation` + `init.weekOffset`); returns `false` for no-op cases (lens tab without a list def). `popTabToCanvas(slot, tabId)` looks up the tab and calls `popTabAtPosition` with `computePopOutFlowPosition()` (upper-left of viewport + jitter — uses `settings.canvasViewport` upper-left or 50/50 fallback). `popSlotToCanvas(slot)` is a thin wrapper over `popTabToCanvas` that targets the slot's active tab. Extracted in code-review-2026-04-25 P5 from `RailsFrame.tsx`. Both call paths converge here: the title-bar `↗` button (`TabStrip.onPopOut`) uses `popTabToCanvas` with menu coords; `useRailsDragMonitor`'s tab-drag canvas branch uses `popTabAtPosition` with `pointerToFlowPosition` coords |
| describeDropZone, findSlotKind, findTabLabel, computeTabInsertIdx | utils/rail-dnd-monitor-helpers.ts | Pure helpers shared by `useRailsDragMonitor`, `TabStrip`, and `float-dock-announce`. `findSlotKind(rails, slotId)` returns the active-tab kind for a slot (or null); `findTabLabel(rails, slotId, tabId)` is the same lookup for a specific tab. `computeTabInsertIdx(stripEl, pointerX, sourceTabId?)` walks `[data-tab-id]` pill midpoints under a tab-strip element and picks the insertion index — when `sourceTabId` is provided (intra-strip reorder), that pill is excluded from the survivor set so the index reflects the post-removal layout. `describeDropZone(zone, rails)` builds the `aria-live` announcement text ("Dropped in <kind> slot" / "Dropped in <kind> tab strip" / "Dropped in <side> rail" / "Dropped in canvas") — phrasing matches `describeFloatDockTarget` so rail-tab and float-dock announcements read consistently. Extracted in code-review-2026-04-25 P5 from inline helpers in `RailsFrame` |
| applyEmptySideCorners, CornerOwnerDispatchers | utils/rail-corner-claim.ts | Reconciler for rail-corner ownership on empty-side drops. Calls `computeEmptySideCornerClaim` (in `float-dock-announce`) for the start/end claim case and `pinchBothCorners` for the no-claim case, then dispatches `setCornerOwner` / `clearCornerOwner` (clearing when target owner equals the default `'v'` so the persisted bag stays minimal). Used by `useRailsDragMonitor` to wire corner state on tab/slot drops. Extracted in code-review-2026-04-25 P5 from `RailsFrame.applyEmptySideCorners`; reconciles with `computeEmptySideCornerClaim` so the rail-tab path and the float-dock path share one corner-axis resolver |
| REACT_FLOW_NODE_SELECTOR | utils/react-flow-dom.ts | Constant `'.react-flow__node'` used as `closest()` target by `<ResizeHandle>`'s `bodySelector` lookup + similar DOM-walking. Hoisted in code-review-2026-04-25 P5 from inline string literals scattered across the floating nodes |

## Utils

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| resolveFuzzy, resolveScheduled, effectiveDate, isScheduledExpired, isScheduledPast, isDeadlinePast, scheduledLabel, scheduledValuesEqual, daysUntil, dateIntensity, resolveRelativeToken, resolveDateAnchor, WeekStart | utils/effective-date.ts | Unified scheduling helpers: resolve `ScheduledValue` fuzzy tokens to concrete end-of-window dates, compute `min(scheduled, deadline)`, label chips, structural equality for ScheduledValue. `daysUntil(date, today)` returns whole-day diff; `dateIntensity(days)` returns a 0.15..1 proximity factor used by task-row chips to fade toward `--color-text-muted` for distant dates. `isScheduledExpired` is fuzzy-only; `isScheduledPast` broadens to fuzzy-expired OR precise past; `isDeadlinePast` covers `dueDate < today`. Every sort/filter/group consumer reads `effectiveDate`. **Code-review-2026-04-25 P2** retired the module-level `configuredWeekStart` singleton + `setConfiguredWeekStart` / `getConfiguredWeekStart`: `weekStartsOn: WeekStart` (0=Sunday, 1=Monday) is now a **required** parameter on every helper that depends on week math (`resolveFuzzy` / `resolveRelativeToken` / `resolveDateAnchor` / `resolveScheduled` / `effectiveDate` / `isScheduledExpired` / `isScheduledPast`). UI consumers read it from `useSettingsStore`; `filter-store` reads via `getState()`; `task-grouping.GroupingContext` carries it. `isDeadlinePast` is the only week-independent helper — it stays parameterless |
| bucketByDate, dateBucketBoundaries, DateBucketKey, DateBucket, BucketByDateResult | utils/bucket-by-date.ts | Generalised relative-date window bucketer (introduced in code-review-2026-04-25 P2). `bucketByDate(todos, getDate, today, weekStartsOn, windows)` walks `todos` once, places each onto the first matching window in the caller-supplied list, and returns `{ buckets, noDate }` — `noDate` collects todos for which `getDate` returned null *and* todos for which no window matched (a missing terminal `later`/`beyond` shows up as drift instead of silently dropping). `DateBucketKey` covers `overdue` / `today` / `tomorrow` / `thisWeek` / `nextWeek` / `thisMonth` / `laterMonth` / `nextMonth` / `later` / `beyond`. `dateBucketBoundaries(today, weekStartsOn)` is the single source of truth for the inclusive boundary timestamps every bucketer uses — built on `services/horizons.startOfWeek` so the horizon ribbon and dashboard lists stay in lockstep. Consumed by `services/dashboard-lists`'s three relative bucketers (`bucketByEffective` / `bucketByDeadline` / `bucketByScheduled`) and `utils/task-grouping`'s 4-bucket project-grouping path |
| bucketByMany, RegistryEntity, ManyBucket, BucketByManyResult, BucketByManyOptions | utils/bucket-by-many.ts | Generic many-to-many bucketer for people / org / tag (introduced in code-review-2026-04-25 P2). `bucketByMany(todos, entities, assignedMap, options?)` walks `todos` once: a todo with N entries in `assignedMap` lands in N buckets (deduped on `entity.id`), todos with no assignment go into `unassigned`. Buckets emit in `entities` order by default; `options.compare` overrides (used for alphabetic tag sorting). Skips entries whose entity has been deleted from the registry (treats as unassigned-equivalent). Consumed by `services/dashboard-lists.bucketByPeople` / `bucketByTag` and `utils/task-grouping`'s many-to-many group dimensions; org keeps an inline walk because its inferred-via-person path doesn't fit the generic shape |
| MS_PER_DAY, startOfDay, startOfToday, isSameDay, formatDate, formatRelativeTime, toDateInputValue | utils/date.ts | Centralized date utilities: day normalization, formatting, constants |
| bySortOrder | utils/sort-order.ts | Shared sort comparator: sortOrder ascending, with id as a stable tiebreaker so equal-sortOrder tasks render in deterministic order |
| generateInitials | utils/person.ts | Generates 1-3 character uppercase initials from a name |
| resolvePersonColor | utils/person-color.ts | Returns the first assigned org's color for a person (`personId + personOrgMap + orgs`), or `undefined` when no org is assigned / has a color. Replaces the retired `Person.color` field (dropped in Dexie v31); used by `AvatarStack`, `TaskEditMetadata`, `ListView`, and NLP autocomplete pipelines |
| toggleItem, matchTodoText, TextMatchContext, TextMatchField, TEXT_MATCH_FIELDS | utils/filter.ts | `toggleItem` toggles an item in a null-or-Set filter (null = all shown, Set = explicit selection). `matchTodoText(todo, query, ctx)` is the canonical multi-field text matcher used by TopBar search + `filter-store.matchesFilter` — hits title/notes from the todo itself and project/person/org/status/tag from `TextMatchContext` when the caller supplies them. Callers resolve `tagNames` from `useTagStore().assignedTagsMap.get(todoId)?.map(t => t.name)` (the registry), never from the retired inline `todo.tags` field |
| DATE_FIELD_LABELS, DATE_FIELD_LABELS_SHORT | utils/filter-labels.ts | Per-`DateField` human labels (`date` / `scheduled` / `deadline` / `created` / `modified`). The full forms ("Effective Date" / "Scheduled" / …) drive desktop chip dropdowns; the short forms ("Effective" / "Sched." / …) drive mobile-density chip-bar callouts. Hoisted in code-review-2026-04-25 P4 from per-call-site copies in `TopBar` + `ListFilterEditor`; consumed by `<FilterChipBar>` |
| normalizeTag, TAG_SLUG_RE, TAG_MAX_LEN | utils/tags.ts | Pure slug normalizer for `#foo` tokens: trims, lowercases, validates against `/^[a-z0-9_-]+$/`, caps at 64 chars. Returns `null` for anything that can't round-trip through the NLP. Shared by the natural-language parser + import validation; the normalized slug feeds `nlp-resolver.resolveTags` which maps it to a registry id |
| getFilterDefaults | utils/filter-defaults.ts | Extract task creation defaults (people, orgs, status, project) from active filter criteria; strips sentinel 0 values; single-selection rule for status + project |
| supplementWithFilterDefaults | utils/filter-defaults.ts | Supplement resolved NLP output with filter-inferred defaults (person/org/project); mutates resolved in place (void return) |
| isMacLike, formatShortcut | utils/platform.ts | Platform detection + keyboard-shortcut label formatting. `isMacLike()` prefers `navigator.userAgentData.platform` and falls back to `navigator.platform`. `formatShortcut('Mod-…'/'Alt-…'/'Shift-…')` renders CodeMirror-style specs as `⌘`/`⇧`/`⌥` on Mac-like platforms and `Ctrl+`/`Shift+`/`Alt+` elsewhere; used by NotesBody footer + toolbar and any UI that surfaces a shortcut hint |
| KIND_ICON, KIND_LABEL | utils/slot-kind.ts | Per-`SlotKind` icon-glyph + label maps shared by `WidgetHeader`, `WidgetKindMenu`, `SlotHeader`, `TabStrip`, and floating nodes so every surface renders the same kind affordance. **Triage-2026-04-27 P1 / Item 5** retitled `KIND_LABEL` to title-cased values: `lens: 'List'` / `notes: 'Notes'` / `calendar: 'Calendar'` / `taskboard: 'Taskboard'` / `horizons: 'Horizons'` / `status: 'Status'` / `scoreboard: 'Discipline'` / `snoozeGraveyard: 'Snooze graveyard'`. `TabStrip.tabLabel` now reads from the map directly (`return KIND_LABEL[tab.type]` with the lens special case) instead of the prior per-kind `if/else` ladder that fell through to `'Calendar'` for any unhandled kind, so stats kinds (`status` / `scoreboard` / `snoozeGraveyard`) render the right tab label. The `KIND_DISPLAY` lookup in `RailContainer.useSlotStubLabel` (collapsed-rail stubs) carries an identical title-cased table |
| TASK_DRAG_KIND, TASK_DROP_KIND, isTaskDragKind | utils/task-dnd/kinds.ts | Canonical `data.type` strings for every task-shaped drag payload + drop target. `TASK_DRAG_KIND = { task, taskboardTask }` collapsed the three pre-unification aliases (`task` / `list-task` / `dashboard-task`) into a single `'task'` kind; `'taskboard-task'` stays separate because its drop-off-target behavior (remove from board) differs. `TASK_DROP_KIND = { task, project, taskboard, taskboardTask, calendarDay, listSection }` is what drop handlers match on via `event.over.data.current.type`. `isTaskDragKind(v)` narrows a `data.type` value to the drag-payload subset |
| taskDragId, isTaskDragId, parseTaskboardEntryId, projectDropId, taskboardFloatDropId, calendarDayDropId, TASKBOARD_SINGLETON_DROP_ID | utils/task-dnd/ids.ts | Central id factories for every draggable-task surface. `taskDragId(surface, todoId, extras?)` emits the per-surface drag id — `canvas-project → todo-<id>` / `inset → inset-todo-<id>` / `lens → lens-todo-<id>` / `list → list-todo-<id>` / `dashboard → dashboard-<listKey>-<id>` / `taskboard-panel → tbp-<id>` / `taskboard-float → tb-<floatingId>-<id>` / `calendar-view → calview-todo-<id>` / `calendar-strip → calstrip-todo-<id>`; throws when required extras are missing. `parseTaskboardEntryId(id)` replaces the fragile `Number(id.split('-').pop())` sniff (F9 in dnd-audit) with an explicit `tbp-<todoId>` / `tb-<floatingId>-<todoId>` parser used by `dispatchTaskDrop`. `projectDropId`/`taskboardFloatDropId`/`calendarDayDropId(scope, dateMs)` emit the matching drop-target ids; `TASKBOARD_SINGLETON_DROP_ID` is the hard-coded dashboard/rail singleton droppable id |
| buildTaskCollision, CollisionActive | utils/task-dnd/collision.ts | Shared `CollisionDetection` factory. Takes a declarative `TaskCollisionRule[]` (each rule = `{ when, accept, algorithm: 'pointerWithin' \| 'closestCenter' \| 'rectIntersection' }`) — the first `when`-match wins and its `accept` filter scopes the droppable set before the algorithm runs. Consumed by `CanvasPage` (rails vs. non-rails — F12 isolation: rails drags match `rails:*` zones, everything else gets the non-rails zones), `DashboardView` (top-row / user-list / taskboard), and `CalendarView` (day cells only). Falls back to `closestCenter` when no rule matches or `active` is null. **`CollisionActive.data` mirrors dnd-kit's `DataRef<Data>` shape** — the live payload lives at `data.current`, so rule predicates must read `active.data.current?.type` (not a flat `.type`); a flat-`.type` predicate always returns `false` and the rule silently falls through to the next one. Fixed in float-dock Phase 0 after all three callers (`CanvasPage`, `DashboardView` × 2) were reading the wrong path and stripping their intended droppables out of scope |
| dispatchTaskDrop, TaskboardOps, CalendarOps | utils/task-dnd/dispatch.ts | Shared drop dispatcher consumed by `use-canvas-dnd.ts` + `DashboardView.handleDragEnd` + `CalendarView`. Owns the four taskboard/calendar branches: (1) `calendarDay` target → `calendar.reschedule(todoId, date)` — checked first so taskboard entries dragged onto the mini-calendar reschedule instead of removing from the board; (2) `taskboard-task` drag onto a `taskboard-task` target → `reorder(from, to)`; (3) `taskboard-task` drag onto the panel container → move to end; (4) `taskboard-task` drag with `over=null` (or non-taskboard target) → `removeEntry`; plus external `task`-kind drag onto the board → `addAt` / `addMultipleAt` at the index implied by dnd-kit's sortable data. Returns `false` when the drop isn't taskboard/calendar-shaped so the caller's route-specific handler (e.g. canvas project placement) can run. `TaskboardOps` is the narrow singleton-board surface `useTaskboardStore.getState()` implements directly; `CalendarOps` is a single `reschedule` callback |
| computeSearchDropIndex | utils/task-dnd/search-drop.ts | Pure bisection helper used by `TopBar`'s search-result drag-end handler to pick an insertion index over a taskboard panel. Walks `[data-tbp-entry]` rects by Y and returns the first index whose midpoint sits below `pointerY`, falling through to `rects.length` (append) past the last entry or when the panel is empty. Lives next to the rest of the task-DnD vocabulary so tests don't have to import from a layout component to exercise the math |
| DEBUG_DND, dndLog | utils/debug-flags.ts | URL-flag debug switch for the rails / float / taskboard / calendar drag-drop subsystem (ui-consistency P5). `DEBUG_DND = new URLSearchParams(window.location.search).has('debug-dnd')` is read once at module load (mirrors `DEBUG_FOCUS` / `?debug-focus=1` in `InsertTrigger.tsx`); `dndLog(label, payload?)` is a gated `console.warn('[debug-dnd]', label, payload)` that no-ops when the flag is absent. Wrap sites: `task-dnd/dispatch.ts` (8 branches — no-active-todo / calendar reschedule / taskboard reorder + move-to-end + remove-on-drop-off / external add / unhandled), `task-dnd/collision.ts` (3 branches — no-active fallback / rule-matched with index + algorithm + droppable counts / no-rule-matched fallback), `rail-dnd.ts:resolveFloatDockTarget` (8 branches — miss / empty-side / tab-strip / collapsed-side center-merge / collapsed-side no-stub / canvas-fallthrough / slot.no-orientation / slot), `hooks/use-rails-drag-monitor.ts` (`onDragStart` non-rails-drag bail + slot/tab kind; `onDragEnd` non-rails-drag / no-over / unknown-drop-id / resolved zone, plus the five canvas-branch silent aborts and the slot-drag fallback). Stats widgets (`FloatingStatusNode` / `FloatingScoreboardNode` / `FloatingSnoozeGraveyardNode`) dock via the `useFloatingWidget` click path, not dnd-kit drag events, so no new wrap points are needed for them |
| bucketByTag, UNTAGGED_BUCKET_KEY, UNTAGGED_BUCKET_LABEL, BucketByTagResult | utils/bucket-by-tag.ts | Shared tag-bucketing helper consumed by both `ListView.buildTagSections` (list grouping) and `dashboard-lists.bucketByTag` (custom-list interpreter). A todo with N assigned tags lands in N per-tag buckets (deduped on `tag.id`); untagged todos go into the trailing `untagged` collection. Tagged buckets sort alphabetically by `tag.name` via `localeCompare`; each caller maps the returned shape into its own section/group type and uses `UNTAGGED_BUCKET_KEY` / `UNTAGGED_BUCKET_LABEL` so the "no tag" group renders identically across surfaces |
| getGroupKey, getGroupLabel, getGroupColor, partitionByGroup, GroupingContext, PartitionGroup, PartitionResult, GROUP_OPTIONS | utils/task-grouping.ts | Pure project-task grouping vocabulary used by `SortableTaskList`'s grouped render path (`Project.groupBy`). `getGroupKey(todo, groupBy, ctx)` returns `null` (no value → ungrouped block), a single `string` (status / date bucket), or `string[]` (people / org / **tag** many-to-many — task appears in every assigned bucket, mirroring `ListView`; lists-consistency P4 added `'tag'`); date / scheduled / deadline collapse to four bucket keys (`'overdue' \| 'today' \| 'week' \| 'later'`) that match `ListView.buildBucketSections` so the same dimension reads identically across views. `getGroupLabel(key, groupBy, ctx)` resolves human labels (status name from `ctx.statuses`, person/org/tag name from `ctx.assignedPeopleMap` / `assignedOrgsMap` / `assignedTagsMap`, date keys → "Overdue" / "Today" / "This Week" / "Later"). `getGroupColor(key, groupBy, ctx)` (triage-2026-04-26 P2 / T4 / Q4=A) returns the entity color used to drive `<TaskGroup>`'s 8 px swatch — status colors from `ctx.statuses`, org colors from `ctx.assignedOrgsMap`, tag colors from `ctx.assignedTagsMap`, person colors via `resolvePersonColor(personId, ctx.personOrgMap, ctx.orgs)` with `UNAFFILIATED_PERSON_COLOR` fallback, date / scheduled / deadline buckets and unknown keys return `undefined`. `partitionByGroup(todos, groupBy, ctx)` walks once and returns `{ ungrouped, groups }` — group order is status by `sortOrder`, date buckets by overdue→later, people/org/tag alphabetically by label; within a bucket todo order is preserved from the input. `GroupingContext` carries `assignedPeopleMap`, `assignedOrgsMap`, `assignedTagsMap`, `statuses`, `orgs`, `personOrgMap` (last two added in triage-2026-04-26 P2 so person color resolution doesn't need extra threads at the call site), `today`, and `weekStartsOn: WeekStart` (added in code-review-2026-04-25 P2 so date-bucket grouping respects the persisted setting; threaded in by `SortableTaskList` from `useSettingsStore`). `GROUP_OPTIONS` (relocated to this file in triage-2026-04-26 P4 / T6 from `components/canvas/ProjectNode.tsx`; ProjectNode keeps a re-export for the existing project-node-sort test) is the canonical `[ {value, label} ]` array of seven group-by dimensions plus None — consumed by both ProjectNode's ⊟ picker and SettingsPage's "Default grouping for new projects" `<select>` |
| UNGROUPED_GROUP_KEY, blockContextId, parseBlockContextId, resolveCrossGroupMutation, CrossGroupMutation | utils/cross-group-drag.ts | Cross-group drag vocabulary used by `SortableTaskList` + `useCanvasDnD.handleDragEnd` (lists-consistency P6). `UNGROUPED_GROUP_KEY = '__ungrouped'` is the sentinel block key for the synthetic headerless ungrouped block. `blockContextId(projectId, blockKey) → 'p<projectId>:<blockKey>'` derives a project-scoped `SortableContext` id so two ProjectNodes on the same canvas don't emit colliding container ids; `parseBlockContextId(id)` is the inverse (returns `null` for any other surface's container id, including dnd-kit's auto-generated ids). `resolveCrossGroupMutation(groupBy, sourceKey, targetKey, todoId)` is the pure helper that the canvas drag-end branch dispatches: returns a `CrossGroupMutation` describing the field write — `{ kind: 'status', todoId, statusId }` (scalar set; `undefined` when target is the synthetic ungrouped bucket); `{ kind: 'people' \| 'org' \| 'tag', todoId, removeId, addId }` (replace semantics — null on either side covers drags from / to the synthetic empty bucket). Returns `null` for date dimensions (`date` / `scheduled` / `deadline` are derived; Q5), same-key drops, missing keys, or unparseable bucket keys |
| RAILS_DRAG_TYPE, RailsDragData, RailsDropZone, SplitZone, TabDropTarget, pointerToSplitZone, encodeRailsDropId, decodeRailsDropId, isRailsDropId, applyDropToSide, applySplitDrop, applyCenterSwap, applySplitButton, applyReorderTab, applyMoveTabToSlot, applyDetachTabToNewSlot, extractTab | utils/rail-dnd.ts | Pure reducers + drop-id codec for rail drag-and-drop. `RailsDragData` discriminates on `kind: 'slot' \| 'tab'`; `tab` drags carry `sourceSlotId` + `tabId`. `RailsDropZone` covers the existing side/quadrant/split kinds, a `tab-strip` zone (pill-reorder / merge-into-slot), a `'collapsed-side'` catch-all (triage-2026-04-26 P1 / T3) registered once on each collapsed `RailContainer.<aside>` so a release that lands inside the rail aside but misses every individual stub still resolves, and a `'canvas'` catch-all registered once by `CanvasView` for the rail-tab → canvas float-pop-out gesture (see `pointerToFlowPosition` below). `applyReorderTab` + `applyMoveTabToSlot` handle tab-strip drops; `applyDetachTabToNewSlot` extracts a tab into a fresh single-tab slot and reuses `applyDropToSide`/`applySplitDrop` to dock it. Consumed by `useCanvasRailsStore` and the rail components (Phase 2 relocated this out of `components/canvas/rails/`) |
| FloatDockTarget, ResolveFloatDockOpts, resolveFloatDockTarget, nearestStubSlotId | utils/rail-dnd.ts | Float-drag hit-test for the float → rail dock gesture. React Flow (not dnd-kit) owns the floating-node drag, so there is no `@dnd-kit` collision pipeline to piggyback on. `resolveFloatDockTarget(pointer, opts)` walks `document.elementsFromPoint(x, y)` and collects every element carrying a `data-rails-drop-id`, then picks the most-specific kind: **slot > tab-strip > empty-side > collapsed-side > canvas** (real-browser-testing 6.5.2; collapsed-side inserted in triage-2026-04-26 P1 / T3). The priority ordering is load-bearing — `DockOverlay`'s corner sub-zones extend along the perpendicular rail's width and stack above the rail's slot stub / expanded slot body, so without the preference a float dropped on a collapsed-rail stub would resolve to corner-claim instead of slot-merge. Within a kind the first (topmost) hit wins (sibling slots / strips don't overlap, so the first match is unambiguous). For `slot` hits — disambiguates center-merge vs split via `pointerToSplitZone` against the element's bounding rect; for `tab-strip` hits, computes the insertion index from `[data-tab-id]` pill midpoints (no source-tab filter — a float always inserts a new tab); for `'collapsed-side'` hits, walks the aside's `[data-slot-id]` stubs via `nearestStubSlotId(asideEl, pointer, side)` and resolves to a center-merge into the closest slot — falls through to null when the rail has no stubs. **`nearestStubSlotId` is now exported (triage-2026-04-27 P5 / Item 2)** — it bisects child stubs by axis distance (vertical rails by Y, horizontal by X) and is consumed by both this resolver and `RailContainer`'s aside-level visual highlight, so the visible target and actual dock target can never diverge. Returns null when the pointer misses every rail hotspot so the caller falls through to the float's position-persist path; also returns null for `zone.kind === 'canvas'` (the canvas drop zone is for the reverse direction only). `opts.elementsFromPoint` is injectable for tests, `opts.getSlotOrientation(slotId)` resolves rail orientation (the slot's containing rail), returning null if the slot is gone |
| FloatDescriptor, tabFromFloat, slotFromFloat, applyDockFloatIntoSlot, applyDockFloatAsNewSlot | utils/rail-dnd.ts | Float-dock reducers (inverse of `popTabAtPosition`). `FloatDescriptor` is a discriminated union over the five float kinds (`note` / `calendar` / `taskboard` / `lens` / `horizons`) carrying the minimum data to seed a Tab — calendar threads `orientation` + `weekOffset` so the user's strip orientation survives the dock. Code-review-2026-04-25 P3 dropped the vestigial `taskboardId` field from the taskboard variant — the v33 migration collapsed `taskboards` to a singleton, so threading the id was redundant. `tabFromFloat(descriptor, tabId)` builds the `Tab` payload; `slotFromFloat(descriptor, slotId, tabId)` builds a fresh single-tab Slot and threads calendar slot-level fields (orientation / weekOffset only apply when a fresh slot is built — center-merge into an existing slot cannot override destination state). `applyDockFloatIntoSlot` dispatches on `target: 'center' \| SplitZone` — center appends a tab at `insertIndex ?? tabs.length` and activates on arrival, edge zones call `placeNewSlot` with a fresh slot adjacent to the target. `applyDockFloatAsNewSlot` handles the brand-new-slot paths (empty rail side or split off adjacent to an existing slot). Both take `genSlotId` + `genTabId` factories from `canvas-rails-store` so id generation stays co-located. The source float row is deleted by the *store* reducer after a successful rails update (detect via `result !== rails`); these pure helpers only shape the RailsState |
| FlowViewport, pointerToFlowPosition | utils/rail-dnd.ts | Tab-drag → canvas float-pop-out math. `pointerToFlowPosition(pointer, canvasRect, vp)` maps client-space coordinates to React Flow (canvas) coordinates — `(pointer - canvasRect.topLeft - vp.pan) / vp.zoom` — then subtracts `DEFAULT_FLOAT_WIDTH / 2` + `DEFAULT_FLOAT_HEIGHT / 2` (320 / 280 px, mirroring the four floating stores' `add` defaults) so the widget centres on the cursor instead of pinning its upper-left corner there. Caller supplies the canvas DOM rect (from `getBoundingClientRect` on the `rails:canvas` droppable) and the persisted viewport from `settings.canvasViewport`. Used by `useRailsDragMonitor`'s tab-drag `zone.kind === 'canvas'` branch; the menu pop-out path keeps using `computePopOutFlowPosition` (viewport-upper-left + jitter) in `RailsFrame.tsx` |
| findSlotKindById, describeFloatDockTarget, computeEmptySideCornerClaim, CornerAssignment | utils/float-dock-announce.ts | Pure helpers extracted from `CanvasPage` for the float-dock handler. `describeFloatDockTarget(target, rails)` builds the screen-reader announcement for a resolved dock (`"Dropped in <kind> slot"` / `"Dropped in <kind> tab strip"` / `"Dropped in <side> rail"`) mirroring `RailsFrame.useRailsDragMonitor`'s `describeDropZone` wording so rail-tab and float-dock announcements read identically. `computeEmptySideCornerClaim(side, claim)` mirrors `applyEmptySideCorners` in `RailsFrame` for the rail-tab path — a claimed corner goes to the dropping rail's axis, the non-claimed sibling corner is pinched to the opposite axis; owners of `'v'` emit as `null` so callers bias toward clearing entries over writing defaults (keeps persisted corner bag minimal). `findSlotKindById` is the announcer's slot-lookup helper |
| isValidCssColor | utils/css.ts | Validates hex color strings (#rgb or #rrggbb only); consumed by settings-store color overrides + import validation |
| parseHorizonSlots, normalizeHorizonSlots, resolveLegacySelectedHorizon, LEGACY_HORIZON_KEYS | utils/horizon-slots.ts | Settings parsers for the post-stats-widgets-2026-04-25 P6 horizons-slot model. `parseHorizonSlots(value): number[]` reads `settings.horizonSlots`; accepts both the new plain `number[]` shape AND the legacy `Partial<Record<HorizonKey, number>>` map (auto-flattens via `LEGACY_HORIZON_KEYS = ['thisweek','nextweek','thismonth','later','someday']` iteration order). `normalizeHorizonSlots(value)` is the same flatten over an already-parsed value (used by `import-validation`). Drops non-finite numbers + unknown legacy keys silently; returns `[]` for null / undefined / empty / invalid JSON / scalar inputs. `resolveLegacySelectedHorizon(legacySelected, legacyHorizonSlotsValue): number | null` resolves a pre-P6 `selectedHorizon: HorizonKey` string against the legacy map row to the matching defId — used only by `settings-store.load()` migration |
| readDateAnchor | utils/date-anchor.ts | Reads a persisted date-range endpoint from an untyped value. Accepts the current `DateAnchor` object shape and the legacy pre-DSL-extension ISO-string form (auto-upgrades to `{kind:'fixed', iso}`); unknown shapes return `null`. Lives in `utils/` (not `stores/filter-store`) so `data/saved-view-legacy.ts` + restore paths can call it without introducing a data→stores import edge |
| getSaveFilePicker, getOpenFilePicker, ShowSaveFilePicker, ShowOpenFilePicker, ShowSaveFilePickerOptions, ShowOpenFilePickerOptions, FilePickerAcceptType | utils/file-picker.ts | File System Access API wrappers (introduced in code-review-2026-04-25 P10). The standard DOM lib doesn't declare `showSaveFilePicker` / `showOpenFilePicker`, so this module declares the option shapes + return types and exposes typed accessors that return the picker function when supported or `undefined` otherwise. Hoisted from four `(window as unknown as { showSaveFilePicker })` cast sites in `views/SettingsPage.tsx` + `components/overlays/MigrationDialog.tsx` so file-picker access goes through one shape-checked path |
| translateSortBy, resolveSavedViewGrouping, encodeGroupSort | utils/list-view-encoding.ts | Runtime translators between persisted list-view shape (`groupBy` / `itemSortBy` / legacy `sortBy`) and `ListDefinition.{sort, grouping}`. Lives in `utils/` so `views/ListView.tsx` doesn't import from `data/`; `data/saved-view-legacy.ts` re-imports from here for the v39 migration's `savedViewToListDefinition` composer. Code-review-2026-04-25 P8 split this out from `data/saved-view-legacy.ts` to repair the layering violation. **Post-v46** (ui-consistency-2026-04-25 P4) `encodeGroupSort` is near-trivial — `ListDefinition.sort` / `.grouping` are flat `TodoSortBy` / `TodoGroupBy` literals on disk so the function just emits the literals directly; `translateSortBy` still translates legacy pre-v39 `sortBy` (`'priority'` / `'due'` / `'tag'`) for backup-import paths |
| defsEqual | utils/list-def-equal.ts | Pure structural equality check between two `PersistedListDefinition`s used by both `DashboardListsEditor.ListEditorBody` and `<StandaloneListEditor>` to drive their dirty / Save state. Compares `name` / `pinnedToDashboard` / `sort` / `grouping` / `runtimeFilter` / `membership` (set-equality on every `personIds` / `orgIds` / `projectIds` / `statusIds` / `tags` / runtime-filter-value array; structural compare on the `dateRangeStart` / `dateRangeEnd` `DateAnchor`s — fixed-iso vs relative-token shapes both honored); intentionally ignores `sortOrder` (reorder is saved independently) and `favorited` (toggled directly from the dialog header — never part of the draft). Extracted in triage-2026-04-26 P5 / T7 alongside `<StandaloneListEditor>` so the manager + standalone surfaces share one dirty-tracker |
