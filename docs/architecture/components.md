# Components

Detail reference for `src/components/` (task, canvas, overlays, shared, layout, settings, taskboard) and `src/views/` helpers. Load when touching UI, canvas nodes, overlays, editors, or view-level grouping/bucketing.

## Views

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| (DashboardView retired) | — | Removed in features-batch-2026-04 Phase 5. Horizon ribbon + selected-horizon list lives on canvas as the `horizons` widget kind (`FloatingHorizonsNode` / `HorizonsSlotContent`). `/dashboard` route redirects to `/`. Settings `dashboardTopOrder` / `dashboardUserLists` / `notesPinnedToDashboard` remain dormant for one release |
| truncateSections | views/ListView.tsx | Pure helper: walks ordered `Section[]` and slices the tail so total task count ≤ `maxTasks`; returns `{ displaySections, truncatedCount }`. Drives ListView's "hard" limit mode |
| buildDateSections, buildScheduledSections, buildDeadlineSections | views/ListView.tsx | Bucket todos by their date field into Overdue / Today / This Week / Later / (No Date\|Not Scheduled\|No Deadline). `buildDateSections` reads `effectiveDate(todo, today)`; `buildScheduledSections` reads `resolveScheduled(todo.scheduledDate, today)`; `buildDeadlineSections` reads `todo.dueDate`. Within a bucket order is `sortOrder` |

## Task Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| TaskEditPopup | components/task/TaskEditPopup.tsx | Centered modal for editing/creating tasks; project selector in create/edit mode. Hosts people / orgs / **tags** chip selectors driven by `ChipSelector`: tag props are `{assignedTags, allTags, onAssignTag, onUnassignTag, onCreateTag}`; create mode buffers picks into `pendingTagIds` and returns them through `onCreate(todo, {personIds, orgIds, tagIds})` so the caller can route tag assignments through `tag-store.assignTag` after insert. The chip selector is the canonical tag-management surface — rows never render tag chips |
| TaskEditHeader | components/task/TaskEditHeader.tsx | Title input + NLP autocomplete + close |
| TaskEditMetadata | components/task/TaskEditMetadata.tsx | Scheduled (SchedulePicker) + Deadline (DeadlinePicker) rows with combined helper line, recurrence select gated on deadline, project, people/orgs sections |
| TaskEditFooter | components/task/TaskEditFooter.tsx | Edit/create mode footer with timestamps, actions |
| MobileTaskRow | components/task/MobileTaskRow.tsx | Mobile two-line task row: checkbox + title + status icon + chevron (line 1), scheduled/deadline chips + `AvatarStack` (sm, max=3) + org/notes (line 2); 48px min touch targets; reads/writes `hoveredTodoId` on `useUIStore` for cross-surface hover sync. Renders **no tag chip** — assigned tags never appear on a row (tags-v2 display rule); manage tags from `TaskEditPopup` |
| TaskRow | components/task/TaskRow.tsx | Desktop task row: checkbox, inline-editable title (via `useInlineEdit`), status icon + `StatusIcon` picker, scheduled/deadline chips with intensity fade (`dateIntensity`), `AvatarStack` (people) + org chip, notes-icon → `TaskNotePopover`, project picker → `ProjectPickerPopup`. Right-click opens `CanvasContextMenu` with Open / Mark complete-incomplete / Add-or-Remove-from-Taskboard / Move-to-project… / (separator) / Delete — last item reads "Remove from Taskboard" when rendered inside a `TaskboardPanel`/`TaskboardNode` (via the `onTaskboard` boolean prop). No inline delete `×`, and **no tag chip** — assigned tags never render on the row (tags-v2 display rule); manage tags from `TaskEditPopup`. Host for the `PortalDropdown` helper (anchor tracking via `ResizeObserver` + pane scroll listener — no RAF polling post-Phase 1). All multi-select-aware mutations route through `useBulkActions` |

## Canvas Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| CanvasView | components/canvas/CanvasView.tsx | React Flow host. Registers node types (`project`, `listInset`, `floatingNote`, `floatingCalendar`, `taskboard`), memoizes node-data refs via `shallowEqualObject` + cache so memoized nodes don't re-render on pan/zoom, pipes `applyNodeChanges` through cascade-shift + alignment-guide computation, and owns the canvas-background `CanvasContextMenu` (New project / Add list / Add note / Add calendar / Add taskboard). The canvas-scoped `DragInsertContext` + `DragPreviewContext` providers live here so drag state is stable per drag rather than recomputed per move tick. Owns the float-dock drag bookkeeping: `handleNodesChange` watches `position` changes with `dragging:true` and publishes the dragging float to `ui-store.floatDrag` (decoding kind via the node-id prefix — `note-` / `calendar-` / `inset-` / `taskboard-`); attaches a window `pointermove` listener at the leading edge that stashes client coords for `resolveFloatDockTarget`; on release (`dragging:false`) invokes `onFloatDock` (wired to `CanvasPage.handleFloatDock`) when a rail hotspot resolves and suppresses the usual position-persist — otherwise the normal `onNoteDragStop` / `onCalendarDragStop` / `onInsetDragStop` / `onTaskboardDragStop` path runs. A `wasMultiDragRef` session flag gates the hit-test to single-drag releases (multi-select drags stay position-only). Cancel-safety: `pointerup` / `pointercancel` / `blur` / unmount-mid-drag all teardown the listener and clear `floatDrag` + `floatAnnouncement`, so a swallowed React Flow release can't leave `DockOverlay` stuck visible. Also registers a single full-viewport `useDroppable({ id: 'rails:canvas', data.type: RAILS_DRAG_TYPE })` behind React Flow (`pointer-events: none`) so a rail tab-pill drag released over the canvas resolves to `zone.kind === 'canvas'` in `useRailsDragMonitor` — dnd-kit's geometry-based collision naturally prefers the higher-z rail hotspots, so `rails:canvas` fires only when the release misses every rail zone; a local `useDndMonitor` tracks `tabDragActive` to drive the parent wrapper's `data-canvas-drop-active` dashed-border affordance while a tab-pill is in flight over the canvas. No on-canvas toolbar `<Panel>` anymore — the old zoom/fit/project-navigator/theme/minimap button cluster was removed (theme moved to `Sidebar`; fit/pan is keyboard / native scroll; minimap retired entirely) |
| ListInsetNode | components/canvas/ListInsetNode.tsx | Canvas node for a `ListInset`. Resolves `inset.listDefinitionId` against `useListDefinitionStore` and routes through `buildDashboardLists` (single-def call) — parity sorting, grouping, and predicate evaluation with the dashboard. Applies global filter first; renders a placeholder header when the referenced def was deleted. Rows wrap in the shared `DraggableTaskRow` (re-exported here for back-compat) — drag to taskboard / project / rail slot. `WidgetHeader` meta hosts a ⧉ copy button that pipes the currently-rendered todos through `copyTasksRich` (rich HTML + plain-text clipboard). When the resolved def declares `runtimeFilter`, threads `inset.runtimeFilterValue` + a setter through `ListDefinitionBody`; clearing the value strips the key so the inset round-trips cleanly |
| RuntimeFilterPicker | components/canvas/RuntimeFilterPicker.tsx | Native `<select>` picker mounted above the body by `ListDefinitionBody` when the definition declares a `runtimeFilter`. Options are read live from the matching store — people / orgs / projects / statuses — sorted by name; selecting the blank option clears the pick (parent strips `runtimeFilterValue` from the owning row). Compact `<select>` chrome keeps the picker to a single row above the list, unlike the richer `ChipSelector`/`ProjectPicker` used elsewhere |
| DraggableTaskRow | components/canvas/shared/DraggableTaskRow.tsx | Thin wrapper around `TaskDraggable` with `surface='inset'` (default) or `surface='lens'`. Emits ids `inset-todo-<id>` / `lens-todo-<id>` and `data: { type: TASK_DRAG_KIND.task, todo }`. Both floating list insets and rail lens rows render through this, so they ride the single canvas `DndContext` and drop into any taskboard surface. `ListInsetNode.DraggableTaskRow` is a re-export to avoid churning existing imports |
| FloatingNoteNode | components/canvas/FloatingNoteNode.tsx | Canvas note widget for `Note` rows with `canvasId` set. Drag/resize chrome + color-picker palette; body delegates to shared `NotesBody` (reads/writes the row via `activeIdOverride`). Replaces the retired `StickyNoteNode`; sticky-specific per-line task conversion + @/#/// autocomplete were dropped — NotesBody's Alt-T already converts the caret line. Spawn path is rail-slot pop-out only (canvas right-click "New Note" retired with notes-polish) |
| FloatingCalendarNode | components/canvas/FloatingCalendarNode.tsx | Canvas calendar widget for `FloatingCalendar` rows. Drag/resize chrome wrapping `CalendarStrip`; threads per-row `orientation` + `weekOffset` through `floating-calendar-store.updateOrientation` / `updateWeekOffset`; mounts `CalendarOrientationToggle` in `WidgetHeader`'s meta slot; wires `onReschedule` to `todo-store.update` via `buildRescheduleUpdate` |
| FloatingHorizonsNode | components/canvas/FloatingHorizonsNode.tsx | Canvas placement widget for `FloatingHorizons` rows — drag/resize chrome wrapping `HorizonsSlotContent`. Ribbon state (`settings.horizonSlots` + `selectedHorizon`) is global, so all rail-docked + floating instances stay in sync. Title-menu kind switch routes through `convertFloatingKind` like the other float nodes |
| HorizonsSlotContent | components/canvas/rails/HorizonsSlotContent.tsx | Rail/float body for the `horizons` widget kind. Renders `HorizonRibbon` on top + the currently-selected horizon's todos below as `DraggableTaskRow surface='lens'` so rows ride the canvas `DndContext`. Hero-list pipeline mirrors `DashboardView`'s retired interpreter (`buildDashboardLists` + filter-store `evalPredicate`). "Edit horizons…" in the ribbon footer opens `DashboardListsEditor filterIds={horizonDefIds}` — unmapped ribbon slots open `ListDefinitionPickerPopup` so users can wire up a horizon in-place |
| TaskboardNode | components/canvas/TaskboardNode.tsx | Canvas node for the singleton taskboard; resizable, closable, sortable drag reorder via dnd-kit. Entries are wrapped in `SortableTaskDraggable` with `surface='taskboard-float'` + `floatingId` (emitting `tb-<floatingId>-<todoId>` ids and `data: { type: TASK_DRAG_KIND.taskboardTask, todo, panelId }`); the outer `useDroppable` registers `TASK_DROP_KIND.taskboard`. Keyed by `floatingId` — one per `FloatingTaskboard` row; all surfaces share the same entries. Panel + each entry's drag data carry the same `panelId`, so `onDragMove` recognizes ownership without relying on `useDroppable.isOver` (which flips false when dnd-kit picks an inner sortable entry). Insertion index comes from dnd-kit's sortable data (`over.data.current.sortable.index`) — post-Phase 6 there is no DOM query and no silent "missing DOM → prepend" failure mode (F3). Drop dispatch flows through `dispatchTaskDrop` in `use-canvas-dnd.handleDragEnd` |
| ProjectNavigator | components/canvas/ProjectNavigator.tsx | Collapsible overlay panel listing all projects; click to fitView-navigate; toggled with P key |
| DragInsertContext | components/canvas/DragInsertContext.ts | React context for stable per-drag state (activeDragTodoId, dragExpandedProjectId, dragSelectionIds); consumed by CanvasView + ProjectNode |
| DragPreviewContext | components/canvas/DragInsertContext.ts | React context for rapidly-changing drag preview (insertTodoId, insertAtEnd, insertProjectId); consumed only by SortableTaskList so CanvasView/ProjectNode don't re-render on every drag-move tick |
| InsertTrigger | components/canvas/InsertTrigger.tsx | Controlled "+" button between tasks for inline task creation; editing state lifted to SortableTaskList for Enter-chaining (new task opens next trigger) |
| findAlignments, findAlignmentsScoped, findResizeSnap | components/canvas/alignment.ts | Snap-to-edge alignment for dragging/resizing nodes (5px threshold, guide lines) |
| computeCascadeShifts, CASCADE_GAP_THRESHOLD | components/canvas/cascade-shift.ts | Auto-shift stacked projects when a neighbor's height changes (40px gap threshold, BFS cascade) |
| drag-preview.css | components/canvas/drag-preview.css | Intentionally global CSS (not a module) so selectors can target React Flow's own drag-preview class names; imported once from `CanvasView.tsx` |
| CalendarStrip | components/canvas/rails/CalendarStrip.tsx | Shared calendar widget body used by both rail slots (via `CalendarSlotContent`) and floating nodes. 7-day window anchored on Monday of today's week + `weekOffset * 7`; `orientation` prop flips between vertical rows and a `repeat(7, minmax(0, 1fr))` horizontal grid. Renders a `RangeBar` with ‹ / › nav + Today button (only off-week) + "This wk" hint + month-aware range label when `onWeekOffsetChange` is provided. Post-Phase 7, rows are dnd-kit draggable (wrapped in `TaskDraggable` with `surface='calendar-strip'`); day cells are `useDroppable` with `data: { type: TASK_DROP_KIND.calendarDay, date }`. A per-instance `scope` (defaulted to a `useId` string) keeps each strip's day droppable ids distinct under a shared `DndContext`. Reschedule dispatch is owned by the `DndContext` host via `dispatchTaskDrop` → `calendar.reschedule`; the strip only registers draggables + droppables |
| EventRow | components/canvas/rails/calendar/EventRow.tsx | Single row inside `CalendarStrip`. Renders status `StatusIcon` (left) + scheduled (`calendar`) / deadline (`clock`) markers + recurrence `↻` glyph + title + people `AvatarStack` + org hollow `AvatarStack`; `compact` mode drops the org stack and caps people to 2 (used by horizontal columns). The caller (`CalendarStrip`) supplies drag wiring via render prop; post-Phase 7 there is no `dataTransfer` plumbing on this component |
| calendar-events | components/canvas/rails/calendar/calendar-events.ts | `buildEntries(todos, days, today, assignedPeopleMap, assignedOrgsMap, statuses)` bucket-and-enrich helper; returns a `Map<dayKey, EventRowEntry[]>` with real + virtual recurring instances, pre-resolved people/orgs/status. Virtual rows carry the parent todo so reschedule drags mutate the parent's anchor |
| CalendarOrientationToggle | components/canvas/rails/calendar/CalendarOrientationToggle.tsx | Shared ☰/☷ toggle button mounted in `WidgetHeader`'s `meta` slot by both `CalendarSlotContent` (wired to `canvas-rails-store.setSlotOrientation`) and `FloatingCalendarNode` (wired to `floating-calendar-store.updateOrientation`) |
| TabStrip | components/canvas/rails/TabStrip.tsx | Multi-tab slot header rendered by `RailsFrame.SlotRenderer` in place of `SlotHeader` when `slot.tabs.length >= 2`. Each tab pill shows `KIND_ICON` + label (lens → list name, others → kind label; pills grow to fit label width — no `max-width` cap — and still `min-width: 0` + `.label` `overflow:hidden; text-overflow: ellipsis` so the strip shrinks to an ellipsis when cramped; full label is the `title` tooltip) + `×` close; the active pill also carries a `⋯` menu (Pop out / Change type…). Trailing `+` button opens `WidgetKindMenu` to append a tab; trailing chrome buttons (pop-out ↗ / more ⋯ / close ×) mirror single-tab `SlotHeader`. A `⋮⋮` drag handle surfaces the whole-slot drag activator from `DraggableSlot`. Drag: pills are `useDraggable({ data: { kind: 'tab', slotId, tabId } })`; the strip is `useDroppable` on `encodeRailsDropId({ kind: 'tab-strip', slotId })` — drop onto another `TabStrip` reorders/merges via `reorderTab`/`moveTabToSlot`; drop onto a slot-level zone detaches + docks via `detachTabToNewSlot`. Keyboard: arrow-key roving (Left/Right/Home/End activates, Delete closes) + `role="tablist"` / `role="tab"` / `aria-selected` |
| RailsFrame | components/canvas/rails/RailsFrame.tsx | Outer rails shell. 3×3 CSS grid driven by `--left-size` / `--right-size` / `--top-size` / `--bottom-size` (0 when a rail is null); each rail is a grid item placed via `computeRailGridArea(rails, side)` so horizontal rails can extend into NW/NE/SW/SE corner cells when the corresponding `rails.corners[c] === 'h'`. `canvas-host` always occupies cell 2/2. Renders `DockOverlay` on top while **any** of: a rails slot/tab drag is in flight (via `useRailsDragMonitor`) or a float drag is in flight (via `ui-store.floatDrag !== null`) — the float-drag gate also flips `DockOverlay`'s `floatDragActive` prop so drop strips render on every side, not just empty ones (docking a float into an occupied side creates a new slot). Corner ownership is written at drop-time only (via `applyEmptySideCorners`) — there is no per-corner toggle UI. Hosts `useDefaultRails` (hydrates from `settings.canvasRails`, seeds a right-side `thisweek` lens on first run, debounce-persists back) and `useRailsDragMonitor` (routes `onDragEnd` to the matching store reducer + `applyEmptySideCorners(side, claim)` to wire the adjacent corners per the claimed/pinched semantics documented on `DockOverlay`; the tab-drag branch handles `zone.kind === 'canvas'` by reading the `data-rails-drop-id="rails:canvas"` DOM rect + `settings.canvasViewport`, calling `pointerToFlowPosition`, and dispatching `popTabAtPosition(tab, canvasId, x, y, {orientation, weekOffset})` + `closeTab` — the reverse of the float → rail dock gesture). Renders the rail-drag `announcement` and the float-drag `floatAnnouncement` (from `ui-store`) as two parallel `role=status aria-live=polite` regions. Also exports `popSlotToCanvas` / `popTabToCanvas` — both thin wrappers over the exported pure `popTabAtPosition(tab, canvasId, x, y, init?)` helper, which is the shared pop-out dispatcher used by the menu path (`computePopOutFlowPosition` coords) and `useRailsDragMonitor`'s tab-drag canvas branch (`pointerToFlowPosition` coords) |
| DockOverlay | components/canvas/rails/DockOverlay.tsx | Drag-time drop-strip overlay rendered by `RailsFrame` during any rails slot/tab drag or any float-widget drag (`ui-store.floatDrag !== null`). Returns null only when both flags are off AND no side is empty; otherwise iterates `emptySides`. The `floatDragActive` prop tells the overlay to render even with zero empty sides — when a float drags over a fully-populated rail, docking still creates a new slot, so the affordance stays useful. For each rendered side, emits three `useDroppable` sub-zones — `start` / `center` / `end` — encoded via `encodeRailsDropId({ kind: 'empty-side', side, claim? })` and tagged with `data-rails-drop-id={id}` on the outer `<div>` so the float-dock pointer hit-test (`resolveFloatDockTarget` via `document.elementsFromPoint`) can find them without going through dnd-kit's collision pipeline. `start` / `end` extend into the adjacent corner cell (size keyed off the perpendicular rail's `--*-size` var so they collapse to 0 when that rail is absent). `useRailsDragMonitor.applyEmptySideCorners` resolves the drop with two roles per adjacent corner: **claimed** (dropped rail extends into the corner → owner = dropped-rail axis: `'h'` for top/bottom, `'v'` for left/right) and **pinched** (dropped rail does NOT extend → owner = opposite axis, so perpendicular rails own when present, with `resolveCorner` falling back cleanly when absent). `claim='start'` claims start + pinches end; `claim='end'` mirrors; no claim pinches both (the dropped rail is pinched between perpendicular neighbors). Writes route through `setCornerOwner` when the target ≠ default (`'v'`) and `clearCornerOwner` when it ==  default, keeping the persisted bag minimal. The float-dock path uses `utils/float-dock-announce.computeEmptySideCornerClaim` + the same `setCornerOwner` / `clearCornerOwner` actions for parity. The strip wrapper is `display: contents` so each sub-zone positions absolutely against `.overlay` (a grid item spanning all 3×3 cells) rather than a 0-sized parent |
| RailContainer | components/canvas/rails/RailContainer.tsx | Rail shell. Renders an `<aside>` sized by the current `railSize` (width for verticals, height for horizontals; clamped to `[RAIL_SIZE_MIN=60, RAIL_SIZE_MAX=600]`). Two pointer-driven edge controls share a single rAF scheduler (`useThrottledResize`): `RailEdgeStrip` — an invisible 6 px drag strip spanning the full canvas-facing edge (drag-only, `col-resize` / `row-resize` cursor) — and `RailEdgeHandle` — a small neutral button (14×36 chip for vertical rails, 36×14 for horizontal) centered on the edge that click-toggles `toggleRailCollapsed(side)` and drag-resizes via `setRailSize` once the pointer has travelled past `DRAG_THRESHOLD_PX` (3 px). The handle uses `--color-surface` + `--color-border` + `--radius-sm` so it matches the rest of the app's chrome, hover brightens to `--color-surface-high`. Keyboard on the handle: Enter/Space toggle, arrow keys resize by 20 px. Collapse is an explicit action (button, keyboard) — dragging past the clamp minimum does not auto-collapse. When `collapsed === true` (from `rails.collapsed[side]`), the body content + edge strip are hidden (only the handle remains, rendered via `overflow: visible` so its half-outside chip stays fully visible against the narrow strip), and `railSize` returns `COLLAPSED_RAIL_PX` (28 px). The collapsed body renders one `CollapsedSlotStub` per slot — each shows the slot's `KIND_ICON` plus a semibold text label (the list definition name for lens slots via `useListDefinitionStore`, else the kind's title-cased `KIND_DISPLAY` label); labels are rotated with `writing-mode: vertical-rl` on vertical rails so they read top-to-bottom and stay horizontal on top/bottom rails |

## Overlays

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| BulkConfirmDialog | components/overlays/BulkConfirmDialog.tsx | Confirmation dialog for destructive bulk actions (delete, complete/uncomplete); supports custom messages, labels, and skipIds for two-option dialogs |
| UndoSnackbar | components/overlays/UndoSnackbar.tsx | Bottom-center toast after destructive actions with "Undo" button, auto-dismiss 5s |
| FilterSheet | components/overlays/FilterSheet.tsx | Mobile filter bottom sheet: search, priority, date range, toggles, people/orgs/statuses/tags accordion lists; reads/writes useFilterStore. Tag chips pull from `useTagStore().tags` (sorted alphabetically) with each chip tinted by `tag.color`; toggling writes a `Set<number>` of tag ids through `filter-store.setTags` (OR semantics via `assignedTagsMap`) |
| CanvasContextMenu | components/overlays/CanvasContextMenu.tsx | Reusable right-click context menu (canvas background, project, box) |
| FilteredListPopup | components/overlays/FilteredListPopup.tsx | On-demand floating list popup triggered by right-clicking person/org on any TaskRow; reads from stores directly |
| MigrationDialog | components/overlays/MigrationDialog.tsx | Confirmation dialog for data migrations; `schema-upgrade` mode (full-screen, Dexie upgrade) and `legacy-import` mode (overlay modal, file/import); export backup button + apply/cancel |
| ProjectPickerPopup | components/overlays/ProjectPickerPopup.tsx | Portal-rendered positioned popup wrapping `ProjectPicker`; closes on outside-click / Escape; used by TaskRow right-click "Move to project…" |
| ListDefinitionPickerPopup | components/overlays/ListDefinitionPickerPopup.tsx | Portal-rendered popover with two modes. `dashboard` (default): lists unpinned defs with a Pin action. `canvas`: lists all defs with an `onSelect(id)` action the caller wires to list-inset creation. Both show a "Create new list…" footer that opens `DashboardListsEditor`. Delegates the row rendering to `ListDefinitionPickerBody` so the popup + the `WidgetKindMenu` hover flyout share the same item layout |
| ListDefinitionPickerBody | components/overlays/ListDefinitionPickerBody.tsx | Headless row-renderer extracted from `ListDefinitionPickerPopup`. Same filter logic (`mode`/`excludeIds`/`showNotesEntry`) but chromeless — action-label per row is opt-in via `actionLabel`, optional header / empty-message / create-new footer, and `onPick(id)` is the single click handler. Used by the popup (with Pin/Add labels) and the `WidgetKindMenu` "Change list…" inline flyout (no labels) |

## Settings Editors

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| DashboardListsEditor | components/settings/DashboardListsEditor.tsx | Settings modal: dnd-kit reorderable list of list definitions, inline rename, pin-to-dashboard toggle, delete confirm, "Add list" footer. Per-row ⚙ toggle expands an inline `ConfigPanel` for live-edit of membership-kind / `warningWindowDays` / sort / sortBy / grouping / **runtime filter** (None + Person/Org/Project/Status — writes `def.runtimeFilter = { field }` or strips the key for None). For `kind:'custom'`, renders predicate-summary chips and an "Edit in ListView…" deep-link that applies the predicate to the live filter store, restores `listSortBy`, sets `editingListDefId`, and navigates to /list. Accepts `filterIds?: number[]` + `title?` so `HorizonsSlotContent`'s "Edit horizons…" action can scope the editor to the 5 horizon-mapped definitions |
| TagEditor | components/settings/TagEditor.tsx | Settings modal mirroring `OrgEditor`: list of `Tag` rows (sorted by name) with color swatch + name + assigned-count pulled from `tagRepository.getTodoCounts`. Add flow: name input + `ColorInput` (defaults to `DEFAULT_ENTITY_COLOR`; duplicate names rejected case-insensitively). Edit in place: name + color, Enter saves / Escape cancels. Delete flow: confirmation dialog showing `getTodoCount(tagId)` — delete is terminal, `tagRepository.delete` cascades `todoTags` rows inside a transaction but the top-level `tag-store.remove` wraps it in `undoable` (restore re-hydrates both the tag row and the cascaded joins via `restoreEntityWithJoins`). Search field filters the list. Rename is a single registry row update — no walking todos — so every consumer picks up the new name via the `assignedTagsMap` refresh |

## Shared Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| AvatarStack | components/shared/AvatarStack.tsx | Overlapping circle avatars with `+N` overflow (default max=3); fill-variant color derived from the person's first assigned org via `utils/person-color.resolvePersonColor` (subscribes to `useOrgStore`; falls back to `DEFAULT_ENTITY_COLOR`); hollow variant uses the org's own color. `sm` variant; click bubbles to `onClick` (opens picker in TaskRow), right-click on a visible avatar surfaces per-person context menu. Used by TaskRow + MobileTaskRow in place of the legacy `.personChip` row |
| ChipSelector | components/shared/ChipSelector.tsx | Reusable autocomplete dropdown for assigning people/orgs; search input, filtered list, create-new option |
| IconSelect | components/shared/IconSelect.tsx | Generic `<select>` replacement showing a per-option icon in the trigger and each menu row; handles click-outside, Enter/Space/Escape and ArrowUp/Down cycling. Used by ListView's Group / Sort dropdowns |
| groupByIcons, itemSortByIcons | components/shared/list-option-icons.tsx | Icon registry for `ListGroupBy` / `ListItemSortBy` values; reuses `StatusIcon` (calendar/clock/flag/person/circle) + inline SVGs for project/org/none/manual |
| ColorInput | components/shared/ColorInput.tsx | Shared color picker: native swatch + editable hex text input with validation, 3-digit expansion, auto-# prefix, blur revert |
| ProjectPicker | components/shared/ProjectPicker.tsx | Shared project search + list UI (with "No project" option); self-contained search state |
| StatusIcon | components/shared/StatusIcon.tsx | Inline SVG icon registry for statuses (15 icons: person, message-bubble, circle, star, stop-sign, exclamation, clock, check, question, flag, eye, bookmark, snooze, arrow, calendar); returns null for unknown/missing icon |
| SchedulePicker | components/shared/SchedulePicker.tsx | Trigger chip + inline popover for `scheduledDate` (wraps `ScheduledValueMenu`); expired-fuzzy state shown with overdue marker |
| ScheduledValueMenu | components/shared/ScheduledValueMenu.tsx | Shared menu body for scheduled-value editing: 3×2 fuzzy-token grid (Day/Week/Month × now/next), action footer with "Pick a specific day…", optional "Add deadline…", conditional "Clear" |
| DeadlinePicker | components/shared/DeadlinePicker.tsx | Danger-tinted chip that opens native date picker for `dueDate`; inline clear button; precise-only (no fuzzy) |
| DateAnchorInput | components/shared/DateAnchorInput.tsx | Shared filter-predicate anchor picker: native `<input type="date">` for fixed anchors paired with a `<select>` of the 12 `RelativeDateToken`s (yesterday, today, tomorrow, start/end-of-week, start/end-of-month, etc.) plus an explicit `None` sentinel that clears the anchor to null; mutually exclusive — selecting a token clears the date and vice versa. `Custom…` is only rendered in the select when the value is a fixed anchor. Used by TopBar `DateRangeDropdown` and mobile `FilterSheet` |
| ErrorBoundary | components/shared/ErrorBoundary.tsx | Generic React error boundary (class component, documented exception); catches render errors, shows scoped fallback with "Try again" / "Reload"; wired at App level and around Canvas route |
| NlpAutocomplete | components/shared/NlpAutocomplete.tsx | Floating dropdown for autocomplete suggestions; renders people / orgs / projects with color dots |
| WidgetHeader | components/shared/WidgetHeader.tsx | Unified slot/widget chrome used by rails (`SlotHeader`), floating nodes (`FloatingNoteNode`, `FloatingCalendarNode`, `TaskboardNode`), and `ListInsetNode`. Renders kind icon (via `KIND_ICON`) + drag handle + title + meta + action buttons (pop-out ⇱, dock ↙, more ⋯, close ×). `floating` prop switches the button row to hover-reveal + applies React Flow's `nopan nodrag` classes. `onTitleClick` makes the title a `▾`-caret button that fires anchor coords — the carrier for the `WidgetKindMenu` opener |
| WidgetKindMenu | components/shared/WidgetKindMenu.tsx | Shared title-click popover: four kind radios (List/Notes/Calendar/Taskboard) + optional "Change list…" secondary row (lens only — taskboard has no secondary because the board is a singleton; label overridable via `secondaryLabel`). Rails consume it via `canvas-rails-store.setSlotKind`; floating nodes consume it via `convertFloatingKind` (in `services/float-kind-switch`). The secondary row is a hover flyout (not a separate popup): `pickListForLens(id)` fires `canvas-rails-store.updateSlot` (rails) or `list-inset-store.update` (floats) on click and closes the whole menu — the flyout renders `ListDefinitionPickerBody` inline, portal-anchored to the row with fallback left-flip when it would overflow the viewport, closes on pointer-leave with a short grace timer. Keyboard: arrow-key roving, Enter selects, ArrowRight opens flyout, ArrowLeft / Escape closes it then the menu |
| TaskDraggable, SortableTaskDraggable | components/task/dnd/TaskDraggable.tsx | Shared render-prop wrapper — **the** single `useDraggable` / `useSortable` call for every task row in the app. Owns id generation via `taskDragId(surface, todoId, extras?)` and the drag data payload shape `{ type, todo, ...extraData }` so every surface that renders a draggable task row emits identical ids + identical `data.type`. Consumers: `DraggableTaskRow` (inset + lens), `TaskList.DraggableRow` (ListView, with `extraData.sectionKey`), `DashboardDraggableRow` (dashboard cards, with `listKey`), `SortableTaskList.SortableTaskRow` (canvas project rows — `SortableTaskDraggable`), `TaskboardPanel.SortableEntry` + `TaskboardNode.SortableTaskboardEntry` (`SortableTaskDraggable` with `kind: 'taskboard-task'`), `CalendarView` + `CalendarStrip` (Phase 7 migrated to dnd-kit), `TopBar.SearchResultRow` (surface `'search'`, emits `search-todo-<id>` into the TopBar-local `DndContext`). Extracted in dnd-unification Phase 4 |
| DropIndicator, dropCellClassName | components/shared/DropIndicator.tsx | Shared drop-indicator visuals. `<DropIndicator kind="line">` renders a layout-neutral 2 px insertion bar via negative vertical margins (`-1 + 2 + -1 = 0` net flow impact, overlaps 1 px into adjacent rows' padding) so cached droppable rects stay valid for the rest of the drag — used by `SortableTaskList` between task rows + by `TaskboardPanel` / `TaskboardNode` for sortable-index insertion. `<DropIndicator kind="cell">` + `dropCellClassName(active)` handle whole-container glow (list insets, lens slots, calendar day cells). One CSS module (`DropIndicator.module.css`) owns the styling so every surface looks identical. Extracted in dnd-unification Phase 3; layout-neutral margins added in the post-P11 sortable-index drift fix (cursor was offsetting from the indicator as the indicator's mount shifted entries below it) |

## Layout

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| Sidebar | components/layout/Sidebar.tsx | Desktop vertical icon sidebar: Canvas (grid), List (lines), Calendar (calendar) at top; `bottomIcons` container pushes the theme-mode toggle (☀/☾) + Settings (gear) icons to the bottom — theme button reads `useSettingsStore().themeMode` + `useResolvedTheme()` and flips between `light` / `dark` on click, title shows `system (resolved)` hint when mode is system; hidden on mobile. Dashboard nav icon retired in features-batch-2026-04 Phase 5 alongside the view itself |
| TopBar | components/layout/TopBar.tsx | Desktop filter + search bar. Grouped search dropdown (`role=listbox` with per-field `role=group` sections for Title/Notes/Project/Person/Org/Status/Tag). Each search row is a `SearchResultRow` — a `TaskDraggable surface='search'` (emits `search-todo-<id>` + `data.type='task'`) wrapped by a TopBar-local `DndContext` + `DragOverlay`. On drop-end, `handleSearchDragEnd` finds the hovered taskboard panel (`[data-tbp-container]`), bisects `[data-tbp-entry]` rects by Y via `computeSearchDropIndex`, and calls `useTaskboardStore.addAt(todoId, idx)` — the scoped context isolates search drags from the canvas/dashboard `DndContext`s without needing hoisting. Right-click on a row opens `CanvasContextMenu` with the same item set as `TaskRow` (Open / Mark complete / Add or Remove from Taskboard / Move to project… / Delete); menu state + `ProjectPickerPopup` live on TopBar (not the row) so they survive the dropdown closing when the menu opens. `SearchResultsGroups` arrow-key roving + `Show all {n}` expansion unchanged from pre-P3 |
| BottomTabBar | components/layout/BottomTabBar.tsx | Mobile bottom tab navigation (List, Filters, Settings); shows filter active indicator dot |
| FileSyncBanner | components/layout/FileSyncBanner.tsx | Dismissible banner suggesting file sync when no file handle saved; dismissal persisted in localStorage |

## Taskboard

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| TaskboardPanel | components/taskboard/TaskboardPanel.tsx | Dashboard + rail card for the singleton taskboard. **No nested `DndContext`** — the outer `SortableContext` rides the surrounding canvas (rail slot) or dashboard `DndContext`. Entries are `SortableTaskDraggable` with `surface='taskboard-panel'` (emitting `tbp-<todoId>` ids + `data: { type: TASK_DRAG_KIND.taskboardTask, todo, panelId }`); the outer container is `useDroppable` with `data: { type: TASK_DROP_KIND.taskboard, panelId }`. `use-canvas-dnd.ts` / `DashboardView.handleDragEnd` both route drops through `dispatchTaskDrop`, which owns reorder / move-to-end / remove-on-drag-off / external-task-add. Panel + entry drag data both carry the same `panelId` so `onDragMove` recognizes ownership without leaning on `useDroppable.isOver`. Insertion index comes from dnd-kit sortable data (Phase 6); indicator rendered via shared `DropIndicator` — no DOM query, no silent "missing DOM → prepend" failure mode (F3) |

## UI Constants

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| DEFAULT_ENTITY_COLOR | constants.ts | Default color '#537FE7' for new people and orgs |
| UNAFFILIATED_PERSON_COLOR | constants.ts | Neutral grey '#9CA3AF' used as fallback for people with no assigned org (PeopleEditor swatch + AvatarStack fill) |
