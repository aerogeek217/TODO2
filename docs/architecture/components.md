# Components

Detail reference for `src/components/` (task, canvas, overlays, shared, layout, settings, taskboard) and `src/views/` helpers. Load when touching UI, canvas nodes, overlays, editors, or view-level grouping/bucketing.

## Views

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| (DashboardView retired) | — | Removed in features-batch-2026-04 Phase 5. Horizon ribbon + selected-horizon list lives on canvas as the `horizons` widget kind (`FloatingHorizonsNode` / `HorizonsSlotContent`). `/dashboard` route redirects to `/`. Post-code-review-2026-04-25 P8 the dormant `dashboardUserLists` / `notesPinnedToDashboard` settings fields + setters are removed; import-validation still accepts the legacy keys (one-release grace), and `restore` strips both keys from IndexedDB on import |
| ListView (Save / Load / Favorites) | views/ListView.tsx | Post-v39 unified save surface. Toolbar uses the shared `<SortGroupToolbar density='comfortable'>` (ui-consistency-2026-04-25 P3 — collapsed the prior inline `IconSelect` pair (Group + Sort) plus asc toggle into one toolbar; sort/group options are `LIST_SORT_VALUES` / `LIST_GROUP_VALUES` from `models/todo-sort-group.ts`). Toolbar action order (post-list-bugs P1): **Load** → **Save** → ⧉ copy. `Save` opens `ListDefinitionSelector` in `'save'` mode (leading "+ New list" entry + row per def; picking a def routes through `showBulkConfirmation` for overwrite, picking "+ New list" prompts for a name and `add`s a `favorited: true` def). `Load` opens the same selector in `'load'` mode (no "+ New"; selecting a def calls `applyAndMarkLoaded`, which writes predicate + grouping + `maxTasks` / `limitMode` + `runtimeFilter` spec back into `filter-store` + local UI state and highlights the chip). Each row carries a `×` that routes through `showBulkConfirmation` → `removeListDefinition`. The Favorites chip bar renders every def where `favorited === true`, sorted by `sortOrder`, via `FavoriteChip`. The desktop `TopBar` is the single filter UI — the post-dashboard-lists-editor inline `ListFilterEditor` mount was removed in list-bugs P1 (mobile keeps `FilterSheet`). **Sort options (lists-consistency P3):** `itemSortByOptions` exposes `manual` / `name` / `date` / `scheduled` / `deadline`. `'name'` sorts by `title.localeCompare` with `sortOrder` + `id` as tiebreakers, honouring asc/desc. `ListItemSortBy` widened to include `'name'` (also `ListSortBy` for round-trip with saved defs); `applyDefinition` coerces an incoming `'name'` *grouping* (which would be meaningless — `'name'` is sort-only) to `'none'`. `'name'` outside ListView falls back to sortOrder via `compareBySortBy`'s default branch. **Prompt on `/list` (list-bugs P3 + lists-consistency P2/P5):** the toolbar's `Prompt` `<select>` (label `aria-label="Prompt field"`) sets `runtimeFilterSpec.field` (`none`/`person`/`org`/`project`/`status`/`tag`) on a local `runtimeFilterSpec: RuntimeFilterSpec \| null` + `runtimeFilterValue: number[] \| undefined` pair; when the spec is set, a `RuntimeFilterPicker` renders above the task grid and the list applies it by round-tripping `filter-store` criteria through `criteriaToPredicate` → `applyRuntimeFilter(pred, spec, values)` → `predicateToCriteria` before `applyFilter`. Empty / undefined value short-circuits back to the unfiltered list (no narrowing). The spec round-trips onto the loaded def via `writeCurrentStateToDef` / `handleCreateNew` + hydrates back via `applyDefinition`; the value is transient (mirrors widget surfaces — it is *not* persisted on the def). Dirty tracking: the `applyingRef` effect watches `runtimeFilterSpec` only (not the value), so picking a value doesn't flag the loaded chip dirty. Empty-state copy swaps to `"Pick a {label}…"` when the spec is set but no value is picked. Selector + chip CSS keys reuse the legacy `savedViewChip` / `savedViewsBar` module names for continuity — the names are historical, the semantics are now list-definition Favorites |
| truncateSections | views/ListView.tsx | Pure helper: walks ordered `Section[]` and slices the tail so total task count ≤ `maxTasks`; returns `{ displaySections, truncatedCount }`. Drives ListView's "hard" limit mode. `maxTasks` + `limitMode` (`'hard'` vs `'scroll'`) round-trip onto saved `ListDefinition`s via `encodeGroupSort` + the toolbar Save flow |
| buildDateSections, buildScheduledSections, buildDeadlineSections | views/ListView.tsx | Bucket todos by their date field into Overdue / Today / This Week / Later / (No Date\|Not Scheduled\|No Deadline). `buildDateSections` reads `effectiveDate(todo, today)`; `buildScheduledSections` reads `resolveScheduled(todo.scheduledDate, today)`; `buildDeadlineSections` reads `todo.dueDate`. Within a bucket order is `sortOrder` |

## Task Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| TaskEditPopup | components/task/TaskEditPopup.tsx | Centered modal for editing/creating tasks; project selector in create/edit mode. Hosts people / orgs / **tags** chip selectors driven by `ChipSelector`: tag props are `{assignedTags, allTags, onAssignTag, onUnassignTag, onCreateTag}`; create mode buffers picks into `pendingTagIds` and returns them through `onCreate(todo, {personIds, orgIds, tagIds})` so the caller can route tag assignments through `tag-store.assignTag` after insert. As of triage-2026-04-27-batch2 P2, `TaskRow` / `MobileTaskRow` also expose an inline tag chip (the shared `<TagChipSelector>`); the popup remains the canonical detail surface but is no longer the only path to add tags. Edit-mode footer (`TaskEditFooter`) carries an Add/Remove-from-Taskboard toggle alongside Duplicate / Delete / Done (bugs-ui P3) |
| TaskEditHeader | components/task/TaskEditHeader.tsx | Title input + NLP autocomplete + close |
| TaskEditMetadata | components/task/TaskEditMetadata.tsx | Scheduled (SchedulePicker) + Deadline (DeadlinePicker) rows with combined helper line, recurrence select gated on deadline, project, people/orgs sections |
| TaskEditFooter | components/task/TaskEditFooter.tsx | Edit/create mode footer with timestamps + actions. Edit mode hosts an `Add to Taskboard` / `Remove from Taskboard` toggle (mirrors `TaskRow`'s right-click item) before `Duplicate`; `onBoard` is read via `useTaskboardStore((s) => s.has(todo.id))` so the label flips after toggle without remount. The button reuses `.duplicateButton` styling — `Done` stays the only primary action (bugs-ui P3) |
| MobileTaskRow | components/task/MobileTaskRow.tsx | Mobile two-line task row: checkbox + title + status icon + chevron (line 1), `<TaskPillBar density='small'>` (scheduled/deadline + `AvatarStack` (sm, max=3) + org/status — ui-consistency-2026-04-25 P2 replaced the inline chip block) + `<TagChipSelector>` (triage-2026-04-27-batch2 P2 — `#tag` chips inline; empty `#` trigger always visible since touch surfaces have no row-hover state) + notes (line 2); 48px min touch targets; reads/writes `hoveredTodoId` on `useUIStore` for cross-surface hover sync. Code-review-2026-04-25 P6 brought feature parity with `TaskRow`: notes-icon button → `TaskNotePopover`, scheduled chip → `SchedulePicker`, deadline chip → `DeadlinePicker`, status `StatusIcon` picker, people `AvatarStack` → `ChipSelector` popover, long-press / right-click `CanvasContextMenu` (built via `buildTaskRowMenuItems`), Add/Remove from Taskboard. Routes mutations through the shared `useTaskRowActions` hook so multi-select semantics + Taskboard-surface "Delete = remove from board" stay identical between desktop and mobile |
| TaskRow | components/task/TaskRow.tsx | Desktop task row: checkbox, inline-editable title (via `useInlineEdit`), `<TaskPillBar>` (people → dates → status — ui-consistency-2026-04-25 P2 replaced the inline `AvatarStack` + scheduled/deadline chip + status icon block; uses `<TaskPillDates layout='stack'>` for the vertical date column), `<TagChipSelector>` mounted after the pill bar (triage-2026-04-27-batch2 P2 — `#tag` chips render inline; empty `#` trigger is hover-revealed via `[class*="tagChipEmpty"]` cross-module rule on `.row:hover`), notes-icon → `TaskNotePopover`, project picker → `ProjectPickerPopup`. Right-click opens `CanvasContextMenu` with Open / Mark complete-incomplete / Add-or-Remove-from-Taskboard / Move-to-project… / (separator) / Delete — last item reads "Remove from Taskboard" when rendered inside a `TaskboardPanel`/`TaskboardNode` (via the `onTaskboard` boolean prop). No inline delete `×`. Host for the `PortalDropdown` helper (anchor tracking via `ResizeObserver` + pane scroll listener — no RAF polling post-Phase 1). All multi-select-aware mutations route through `useTaskRowActions` (which wraps `useBulkActions`); the context-menu items come from the shared `buildTaskRowMenuItems` builder so TaskRow / MobileTaskRow / TopBar search results render an identical menu. Dead `compact` / `extraLabel` props + `TaskList.ghostIds` were removed in code-review-2026-04-25 P6 |
| TagChipSelector | components/shared/TagChipSelector.tsx | Inline tag chip row + lookup-or-create dropdown (triage-2026-04-27-batch2 P2). Pure presentational: receives pre-resolved `assignedTags` + the `allTags` registry + `onToggle` / `onCreate` callbacks, no store subscriptions. Empty state renders a `#` trigger inside a wrapper marked `tagChipEmpty` (substring-matched by surface CSS for hover-reveal — TaskRow opacity-gates on `:hover`, MobileTaskRow leaves it visible). Populated state renders one `#name` chip per assigned tag (`color: tag.color, borderColor: tag.color`). Click any chip / trigger opens a `PortalDropdown(anchorRef)` hosting the existing `<ChipSelector>` over the registry — Enter on a novel name fires `onCreate(name)`, which TaskRow / MobileTaskRow wire to `useTagStore.add` + `useBulkActions.quickAssignTag`. `disabled` (used for ghost rows) suppresses popover opening |

## Canvas Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| CanvasView | components/canvas/CanvasView.tsx | React Flow host. Post-code-review-2026-04-25 P5 reduced from ~1018 LOC to ~600 LOC: registers node types (`project`, `listInset`, `floatingNote`, `floatingCalendar`, `taskboard`, `horizons`), memoizes node-data refs via `shallowEqualObject` + cache so memoized nodes don't re-render on pan/zoom, owns the canvas-background `CanvasContextMenu` (New project / Add list / Add note / Add calendar / Add taskboard), registers the full-viewport `useDroppable({ id: 'rails:canvas' })` behind React Flow for rail-tab → canvas pop-out, and mounts the `<CanvasToolbar>` (triage-2026-04-26 P3 / T5) as a sibling inside `.canvasWrapper`. **Pop-out drop indicator (triage-2026-04-26 P1 / T2):** while a tab-pill drag is over the canvas drop zone, a `.popOutIndicator` overlay div renders at the projected landing rect — `DEFAULT_FLOAT_WIDTH × DEFAULT_FLOAT_HEIGHT` (320 × 280 px) positioned via inline `transform: translate(pointer.x - canvas.left - W/2, pointer.y - canvas.top - H/2)` with `will-change: transform` to keep the per-pointermove cost on the compositor. Pointer is tracked via a window-level `pointermove` listener gated on `tabDragActive` (dnd-kit doesn't surface a stream of pointer coords mid-drag, so the listener attaches at drag-start and detaches at drag-end / cancel). Replaces the prior full-viewport `[data-canvas-drop-active]::after` border so the affordance communicates *where* the widget will land instead of *that* docking will fire. The full-viewport `.canvasDropZone` `useDroppable` rect stays for collision purposes — only the visual outline shrinks. The canvas-scoped `DragInsertContext` + `DragPreviewContext` providers live here so drag state is stable per drag. `handleNodesChange` is now a thin sequencer over two extracted hooks: **`useFloatDragLifecycle`** (drag-id tracking, multi-drag flag, `ui-store.floatDrag` publication, window `pointermove` capture for `resolveFloatDockTarget`, position-persist callbacks on release, rAF-deferred cleanup of all listeners so DockOverlay survives the release hit-test — float-dock-bugs-2026-04-25 P1) and **`useCascadeShifts`** (per-node height cache, cascade-shift detection / dispatch, alignment-guide computation, debounced cascade persist). Cancel-safety: `pointerup` / `pointercancel` / `blur` / unmount-mid-drag all teardown the float-drag listener and clear `floatDrag` + `floatAnnouncement` via the lifecycle hook's unmount effect. **`<ReactFlow autoPanOnNodeDrag>` is gated on `useUIStore.floatDrag === null`** (float-dock-bugs-2026-04-25 P2) so float-widget drags don't pan the canvas — the gesture pulls the widget toward a rail (off-canvas) and RF's auto-pan rAF loop otherwise accumulates ~1.2k px of viewport drift over a typical ~700 ms occupied-slot dwell; project-node drags keep auto-pan because the gate flips only while a float is in flight. No on-canvas toolbar `<Panel>` anymore (theme moved to `Sidebar`; fit/pan is keyboard / native scroll; minimap retired) |
| CanvasToolbar | components/canvas/CanvasToolbar.tsx | Lower-left floating canvas toolbar (triage-2026-04-26 P3 / T5 / Q5=A). Floating pill (`.toolbar`) anchored bottom-left of `.canvasWrapper` at `--z-fab`, mounted as a sibling inside `CanvasView` so it z-orders above the canvas + the projected pop-out indicator but below modals. Two icon-only 36 × 36 buttons: **Fit all to view** (always rendered; dispatches the existing `canvas-fit-view` `CustomEvent` that `App.fitView` / `Ctrl+0` / the command palette already use, handled by `CanvasPage`'s window listener — `rfInstance.fitView`) and **Collapse / Expand all rails** (rendered only when `RAIL_SIDES.some(side => rails[side] != null)`; reads + writes `useCanvasRailsStore.rails` + `setAllRailsCollapsed` directly — same logic the retired `Sidebar.bottomIcons` button used; icon swaps between two collapse / expand glyphs). `role="toolbar"` + `aria-label="Canvas toolbar"` on the wrapper; per-button `title` + `aria-label` for screen-reader parity. Reads no props — pure UI on top of the canvas-rails store |
| ListInsetNode | components/canvas/ListInsetNode.tsx | Canvas node for a `ListInset`. Resolves `inset.listDefinitionId` against `useListDefinitionStore` and routes through `buildDashboardLists` (single-def call) — parity sorting, grouping, and predicate evaluation with the dashboard. Applies global filter first; renders a placeholder header when the referenced def was deleted. Rows wrap in the shared `DraggableTaskRow` (re-exported here for back-compat) — drag to taskboard / project / rail slot. `WidgetHeader` meta hosts a ⧉ copy button that pipes the currently-rendered todos through `copyTasksRich` (rich HTML + plain-text clipboard). The kind-menu (title-click) carries an "Edit list" item (lens-only) that fires `useUIStore.openListEditorDialog(inset.listDefinitionId)` (triage-2026-04-26 P5 / T7 / Q7=A; switched from `openListsEditor`) so `<StandaloneListEditor>` mounts the per-list editor dialog directly without `DashboardListsEditor` underneath. When the resolved def declares `runtimeFilter`, threads `inset.runtimeFilterValue: number[] \| undefined` + a setter through `ListDefinitionBody`; an empty array or `undefined` strips the key so the inset round-trips cleanly (lists-consistency P5 widened the value from a scalar to an OR-combined array). Post-code-review-2026-04-25 P3 chrome plumbing (kind-menu state, dock / close, kind change) lives in shared `useFloatingWidget`; the three resize handles (right edge / bottom edge / corner) consume shared `<ResizeHandle>` with `axis: 'x'/'y'/'xy'` — close-handler is composed on top of the hook's `handleClose` to also clear runtime-filter state |
| RuntimeFilterPicker | components/canvas/RuntimeFilterPicker.tsx | Multi-value picker mounted above the body by `ListDefinitionBody` when the definition declares a `runtimeFilter`. Visual shape mirrors `ListFilterEditor`'s `FilterDropdown` chip+typeahead (lists-consistency P5 retired the prior native `<select>` per "don't reinvent controls that already exist"). Always-visible text input + selected-chip rail + anchored option list with a checkbox per option; typeahead filters by label as the user types. Options are read live from the matching store — people / orgs / projects / statuses / **tags** (P5 added `'tag'`) — sorted by name; deleted entities silently drop from the chip rail rather than rendering dangling chips. Backspace on an empty input quick-deletes the most-recent chip; Escape blurs and closes the option list; chips' `×` removes individually; outside-click closes the panel. `value: number[] \| undefined`, `onChange: (value: number[]) => void` — empty array is "explicit no narrowing" (helper passes the predicate through unchanged); `ListInsetNode` / `canvas-rails-store` normalize empty → undefined so the placeholder reappears. Dropdown panel renders via `createPortal` to `document.body` (triage-2026-04-25 P3) — placement computed from the input row's `getBoundingClientRect()`: `top = rect.bottom + 4`, flipped to `rect.top - panelHeight - 4` when the panel would clip below the viewport (8 px viewport margin); horizontal clamp keeps a right-edge field's panel inside the viewport. Tracks scroll (capture phase, so ancestor `overflow:auto` like an inset body recomputes) + resize while open. Outside-click dismiss tracks both `wrapperRef` and `panelRef`. Mirrors `ProjectPickerPopup` / `WidgetKindMenu`; escapes ancestor `overflow: hidden` clipping on float insets / rail lens slots / `/list` toolbar (replaces the prior `useRightEdgeFlip` X-only logic — that hook is no longer imported here) |
| DraggableTaskRow | components/canvas/shared/DraggableTaskRow.tsx | Thin wrapper around `TaskDraggable` with `surface='inset'` (default) or `surface='lens'`. Emits ids `inset-todo-<id>` / `lens-todo-<id>` and `data: { type: TASK_DRAG_KIND.task, todo }`. Both floating list insets and rail lens rows render through this, so they ride the single canvas `DndContext` and drop into any taskboard surface. `ListInsetNode.DraggableTaskRow` is a re-export to avoid churning existing imports |
| FloatingNoteNode | components/canvas/FloatingNoteNode.tsx | Canvas note widget for `Note` rows with `canvasId` set. Color-picker palette; body delegates to shared `NotesBody` (reads/writes the row via `activeIdOverride`). Replaces the retired `StickyNoteNode`; sticky-specific per-line task conversion + @/#/// autocomplete were dropped — NotesBody's Alt-T already converts the caret line. Spawn path is rail-slot pop-out only (canvas right-click "New Note" retired with notes-polish). Post-code-review-2026-04-25 P3 the chrome plumbing (kind-anchor state, dock / close / kind-change handlers, pointer-capture resize) lives in shared `useFloatingWidget` + `<ResizeHandle>` |
| FloatingCalendarNode | components/canvas/FloatingCalendarNode.tsx | Canvas calendar widget for `FloatingCalendar` rows wrapping `CalendarStrip`. Threads per-row `orientation` + `weekOffset` through `floating-calendar-store.updateOrientation` / `updateWeekOffset`; mounts `CalendarOrientationToggle` in `WidgetHeader`'s meta slot; wires `onReschedule` to `todo-store.update` via `buildRescheduleUpdate`. Post-code-review-2026-04-25 P3 chrome plumbing lives in shared `useFloatingWidget` + `<ResizeHandle>` |
| FloatingHorizonsNode | components/canvas/FloatingHorizonsNode.tsx | Canvas placement widget for `FloatingHorizons` rows wrapping `HorizonsSlotContent`. Ribbon state (`settings.horizonSlots: number[]` + `selectedHorizonDefId: number | null` — post-stats-widgets-2026-04-25 P6) is global, so all rail-docked + floating instances stay in sync. Title-menu kind switch routes through `convertFloatingKind` like the other float nodes. Post-code-review-2026-04-25 P3 chrome plumbing lives in shared `useFloatingWidget` + `<ResizeHandle>` |
| FloatingStatusNode | components/canvas/FloatingStatusNode.tsx | Canvas placement widget for `FloatingStatus` rows wrapping `StatusSlotContent`. Title is "Open by status"; default size 380×240. Post-stats-widgets-2026-04-25 Phase 1 chrome parity with the other float nodes via shared `useFloatingWidget` + `<ResizeHandle>`. Body is derived live from `useTodoStore.todos` + `todosVersion` + `useStatusStore.statuses` via `selectStatusBreakdown` — no per-widget settings, no event-store dependency |
| FloatingScoreboardNode | components/canvas/FloatingScoreboardNode.tsx | Canvas placement widget for `FloatingScoreboard` rows wrapping `ScoreboardSlotContent`. Title is "Discipline scoreboard"; default size 720×280. Post-stats-widgets-2026-04-25 Phase 1 chrome parity via shared `useFloatingWidget` + `<ResizeHandle>`. Body subscribes to `useTodoStore.todos` + `useTodoEventStore` (`loadInRange` on mount + on every `todos` ref change — covers field-only edits via `bulkUpdateField`) and renders `selectDisciplineMetrics` over a 12-week window |
| FloatingSnoozeGraveyardNode | components/canvas/FloatingSnoozeGraveyardNode.tsx | Canvas placement widget for `FloatingSnoozeGraveyard` rows wrapping `SnoozeGraveyardSlotContent`. Title is "Snooze graveyard"; default size 380×240; `<ResizeHandle>` min 280×160. Post-stats-widgets-2026-04-25 Phase 1 chrome parity via shared `useFloatingWidget`. Body subscribes to `useTodoStore.todos` + `todosVersion` and calls `useTodoEventStore.getState().loadAll()` on mount + on every todos change; renders `selectMostDeferred` (top-N=5 most-rescheduled open todos); rows are `<button>`s that open `TaskEditPopup` on click |
| HorizonRibbon, HorizonRow | components/dashboard/HorizonRibbon.tsx | Sortable rows + stacked-bar layout for the `horizons` canvas widget (rewritten in stats-widgets-2026-04-25 P6 — replaces the prior 5-cell chart; `HorizonCell` deleted from disk). Input shape: `rows: HorizonRow[]` with `{ defId, label, scheduled: PersistedTodoItem[], due: PersistedTodoItem[], total }`. Each row renders as a grid cell carrying a `DragHandle` (dnd-kit `useSortable` + `verticalListSortingStrategy`) · label `<button>` (click → `onSwap(defId, anchor)` opens `ListDefinitionPickerPopup` to swap that row's list) · stacked bar `<button>` (scheduled segment in `--color-accent`, due segment in `--color-text-muted` at 0.7 opacity; widths = `(perSegmentCount / row.total) * (row.total / max(rows.total)) * 100%`; `aria-pressed={selected}`, `aria-label="<label>: N task[s] (X scheduled, Y due)"`; click → `onSelect(defId)`) · count chip. **Right-click context menu (triage-2026-04-27 P2 / Item 6):** the row `<div>` carries `onContextMenu` that fires `onRowContext(defId, { x: clientX, y: clientY })`; parent (`HorizonsSlotContent`) renders a portaled `CanvasContextMenu` with Edit list / Insert below / Remove — the prior per-row inline `×` button + its `.removeBtn` CSS are gone, and `Props.onRemove` retired (removal flows through the menu's Remove item now). Selected row carries `.rowSelected` class so `HorizonsSlotContent` can style the inset list accent. Footer: `+ Add list` renders only when `rows.length === 0` (empty-state-only — triage-2026-04-27 P2; click → `onAdd(anchor)` opens `ListDefinitionPickerPopup` with `excludeIds={horizonSlots}`). Empty state copy ("Add a horizon to start.") still renders when `rows.length === 0`. `onReorder(fromIndex, toIndex)` dispatched on dnd-kit `dragEnd`. v1 has no animation, no per-segment hover tooltip, no per-row collapse, no max-task limit (lists already carry `maxTasks` / `limitMode` and continue to apply when set on the underlying def) |
| HorizonsSlotContent | components/canvas/rails/HorizonsSlotContent.tsx | Rail/float body for the `horizons` widget kind. Renders the rewritten `HorizonRibbon` on top (sortable rows + per-row stacked bars — see HorizonRibbon row above) + the currently-selected row's todos below as `DraggableTaskRow surface='lens'` so rows ride the canvas `DndContext`. Iterates `settings.horizonSlots: number[]`, resolves each defId to a `ListDefinition`, runs `buildDashboardLists`, splits each list's todos into `scheduled`/`due` segments via `classifyByDateSource`. Selected list pulls from `rows.find(r => r.defId === selectedHorizonDefId)?.scheduled.concat(due)`. `ListDefinitionPickerPopup` is reused for add (`excludeIds={horizonSlots}`), swap (no exclude), and the new "Insert below" path. **Row right-click menu (triage-2026-04-27 P2 / Item 6):** local `rowContextMenu: { defId, index, x, y } \| null` state + `handleRowContext` open a portaled `<CanvasContextMenu>` with three items — **Edit list** dispatches `useUIStore.openListEditorDialog(defId)` (post-triage-2026-04-26 P5 / T7 path; `<StandaloneListEditor>` mounts the per-list editor without a `DashboardListsEditor` underneath), **Insert below** sets `picker = { kind: 'add-at', index: idx + 1, x, y }` (a new `PickerMode` branch added in this phase; `handlePick` calls `addHorizon(defId, picker.index)` against the existing `atIndex` arg), **Remove** is a danger item that calls `removeHorizon(index)`. The portal target is `document.body` so the menu escapes ancestor `overflow: hidden` clipping on rail / float surfaces. "Edit horizons…" in the ribbon footer (when present) still opens `DashboardListsEditor filterIds={defIds}` for editing the underlying list-defs — distinct from "swap which list this row points to" |
| StatusSlotContent | components/canvas/rails/StatusSlotContent.tsx | Rail/float body for the `status` widget kind (stats-widgets-2026-04-25 Phase 2). Hero stacked bar across the top (one `<div>` per status entry, `flex-grow` proportional to count, 1 px between-segment border) above legend rows (`<StatusIcon icon=… filled />` + label + count + percent, with the `--color-text-muted` "No status" fallback color when `id === null`). Empty state ("No open tasks") when total is 0. Reads live from `useTodoStore.todos` + `useTodoStore.todosVersion` + `useStatusStore.statuses` so counts reflow on every CRUD / field-only edit; no history dependency |
| ScoreboardSlotContent | components/canvas/rails/ScoreboardSlotContent.tsx | Rail/float body for the `scoreboard` widget kind (stats-widgets-2026-04-25 Phase 4). Three-card grid (Defer rate / On-time completion / Schedule lag) with 12-week SVG sparklines + ↑/↓/· delta arrows colored by `lowerIsBetter`. Container queries (`@container`) collapse the grid to one column under ~520 px with a `@media` fallback. Driven by `selectDisciplineMetrics({ events, todos, now, weekStartsOn, weeks: 12 })`; subscribes to `useTodoStore.todos` + `useTodoEventStore` (mounting `loadInRange(startOfFirstBucket, end)` and re-pulling on every `todos` ref change). Range label is hardcoded to "12 weeks" (D11) |
| SnoozeGraveyardSlotContent | components/canvas/rails/SnoozeGraveyardSlotContent.tsx | Rail/float body for the `snoozeGraveyard` widget kind (stats-widgets-2026-04-25 Phase 5). Top-N rows (default 5) — each row is a `<button>` with title (ellipsis) · count (in `--color-danger`, semibold tabular-nums) · "since {oldestScheduled}" (formatted via `formatDateShort`, omitted when `oldestScheduled === null`) · proportional bar (track over `--color-surface`, fill `--color-danger`, max width = top row's count). Empty state ("No deferred tasks") when no rows. Click → `useUIStore.openEditPopup(todoId)`. Driven by `selectMostDeferred({ events, todos, limit: 5 })`; subscribes to `useTodoStore.todos` + `todosVersion` and calls `useTodoEventStore.getState().loadAll()` on mount + on every todos change (last-caller-wins race with `ScoreboardSlotContent.loadInRange` is acceptable for v1; both re-pull on todos change so steady state converges) |
| TaskboardNode | components/canvas/TaskboardNode.tsx | Canvas node for the singleton taskboard; sortable drag reorder via dnd-kit. Entries are wrapped in `SortableTaskDraggable` with `surface='taskboard-float'` + `floatingId` (emitting `tb-<floatingId>-<todoId>` ids and `data: { type: TASK_DRAG_KIND.taskboardTask, todo, panelId }`); the outer `useDroppable` registers `TASK_DROP_KIND.taskboard`. Keyed by `floatingId` — one per `FloatingTaskboard` row; all surfaces share the same entries. Panel + each entry's drag data carry the same `panelId`, so `onDragMove` recognizes ownership without relying on `useDroppable.isOver` (which flips false when dnd-kit picks an inner sortable entry). Insertion index comes from dnd-kit's sortable data (`over.data.current.sortable.index`) — post-Phase 6 there is no DOM query and no silent "missing DOM → prepend" failure mode (F3). Drop dispatch flows through `dispatchTaskDrop` in `use-canvas-dnd.handleDragEnd`. Post-code-review-2026-04-25 P3 chrome plumbing (kind-menu state, dock / close handlers, pointer-capture resize) lives in shared `useFloatingWidget` + `<ResizeHandle>` |
| ProjectNode | components/canvas/ProjectNode.tsx | Canvas node for a `Project`. Title bar carries the shared `<SortGroupToolbar density='compact' className={styles.toolbar} showSort={todos.length>1}>` (ui-consistency-2026-04-25 P3 — collapsed the prior parallel sort/group menu blocks (~46 lines of JSX + `showSortMenu` / `showGroupMenu` state + two outside-click effects + two helper handlers) into one toolbar; sort options are `PROJECT_SORT_VALUES` (`manual` / `name` / `date` / `created`), group options are `PROJECT_GROUP_VALUES` (eight dimensions including None and **Tag** from lists-consistency P4) — both subsets imported from `models/todo-sort-group.ts`); same-sort-click → asc-toggle semantic preserved by `handleSort` reading `lastSort` state. Hover-reveal stays on the consumer — single `.toolbar { opacity:0 } .titleBar:hover .toolbar { opacity:1 }` rule replaces the prior per-button rules. ProjectNode's `null` group value rides through a `'none'` sentinel (`PROJECT_NULL_GROUP`) at the consumer boundary; the toolbar API is generic over any `S extends string` / `G extends string`. ⧉ copy button, color picker, and delete `×` retained. `GROUP_OPTIONS` is exported for the test harness. Body delegates to `SortableTaskList` with `groupBy={project.groupBy ?? null}`. Resize handle uses pointer events + `setPointerCapture` (no `window` listeners) with an unmount-time cleanup ref |
| SortableTaskList | components/canvas/SortableTaskList.tsx | Body of `ProjectNode`. Two render paths chosen by `groupBy`. **Flat (`groupBy == null`):** single `SortableContext` over all `displayItems`, insert-triggers between every row. **Grouped (`groupBy` set):** `partitionByGroup(displayItems, groupBy, { assignedPeopleMap, assignedOrgsMap, assignedTagsMap, statuses, orgs, personOrgMap, today, weekStartsOn })` returns `{ ungrouped, groups }`; the renderer emits one `RenderBlock` per non-empty section (each named block also gets a `block.color` derived via `getGroupColor(g.key, groupBy, ctx)` — the color is threaded into `<TaskGroup>` so status / people / org / tag headers render an 8 px swatch; date-bucket / ungrouped blocks stay colorless — triage-2026-04-26 P2 / T4) and renders one `SortableContext` per block, each id-keyed via `blockContextId(projectId, blockKey)` from `utils/cross-group-drag` so two ProjectNodes on the same canvas don't emit colliding container ids (ungrouped block first, headerless, keyed by `UNGROUPED_GROUP_KEY`; named groups follow, each wrapped in `<TaskGroup label count color>`). The grouping context now reads `orgs` + `personOrgMap` off `useOrgStore` in addition to the previous slices, so person color resolution doesn't need extra threads at the call site. Insert-triggers stay inside each block — the after-row trigger of a block's last row carries the next block's first todo as its `beforeId`, so "insert after the last row of group A" lands at the A→B boundary without a trigger that lives between groups. **Cross-group drag (lists-consistency P6) mutates the grouped field with replace semantics**: `useCanvasDnD.handleDragEnd` reads source + target container ids via `parseBlockContextId(active/over.data.current?.sortable?.containerId)`, calls `resolveCrossGroupMutation(project.groupBy, sourceKey, targetKey, todoId)`, and dispatches `bulkSetStatus` / `unassignPerson+assignPerson` / `unassignOrg+assignOrg` / `unassignTag+assignTag` accordingly. Date dimensions (`date` / `scheduled` / `deadline`) skip mutation — buckets are derived, not assigned (Q5). Synthetic ungrouped buckets ("(no people)" / "(no org)" / "(no tag)") map to a `null` removeId/addId so the helper skips the corresponding side of the replace. Same-key drops short-circuit (pure reorder). Multi-drag is skipped to keep semantics unambiguous when the selection spans multiple source groups. FLIP animation (`data-stl-row` attribute on every row wrapper) works in both layouts — measurement happens against the outer container, no `:scope >` query, so grouped wrappers don't break it. **Enter-chain focus handoff (real-browser-testing P3)** is imperative: `triggerRefs: Map<number, InsertTriggerHandle \| null>` keys the new InsertTrigger handle, populated by a memoized `getTriggerRefCb(id)` (recomputing the callback on every render would race the t50 schedule by calling the ref with `null` first); `useLayoutEffect` on `activeInsertAfterId` schedules `setTimeout(() => triggerRefs.get(id)?.focusInput(), 50)` after commit — the 50 ms wait is the post-Phase-4 trace's measured contention window (likely React Flow's ResizeObserver firing as the project node grows). Wired on all three `<InsertTrigger>` rendering sites (BEFORE_FIRST, after-row, empty state). Cross-project `openTriggerAfterInsert` routes through `triggerInlineCreate(newId)`, which sets the target project's `activeInsertAfterId` so the same `useLayoutEffect → setTimeout` path focuses the target's input |
| TaskGroup | components/canvas/shared/TaskGroup.tsx | Visual-only group header used by `SortableTaskList`'s grouped render path. Renders `<section aria-label={label}>` + `<header>` with up to four pieces — optional 8 px circle swatch (when `color` prop is set), small-caps label, count chip, thin rule — followed by a `.rows` wrapper around the children. v1 owns no state: no collapse affordance, no per-group action slots, no drag, no edit. Spacing uses literal px (`gap: 8px`, `padding: 4px 12px 2px`, `margin-top: 16px`) instead of `--space-*` tokens, mirroring the original handoff design (the project's tokens differ from the handoff's assumed 8/12/16 scale and rebasing them globally would ripple). **Color swatch (triage-2026-04-26 P2 / T4 / Q4=A):** `color?: string` is optional; when set, an `aria-hidden="true"` 8 × 8 px round `.swatch` span renders before `.label` with `background: color` (status / people / org / tag headers — colors resolved by the caller via `getGroupColor`). Date-bucket / ungrouped headers leave it undefined so they stay colorless. The swatch is the only `aria-hidden` decorative child the header has had — tests querying header decorative children now need to account for it |
| ProjectNavigator | components/canvas/ProjectNavigator.tsx | Collapsible overlay panel listing all projects; click to fitView-navigate; toggled with P key |
| DragInsertContext | components/canvas/DragInsertContext.ts | React context for stable per-drag state (activeDragTodoId, dragExpandedProjectId, dragSelectionIds); consumed by CanvasView + ProjectNode |
| DragPreviewContext | components/canvas/DragInsertContext.ts | React context for rapidly-changing drag preview (insertTodoId, insertAtEnd, insertProjectId); consumed only by SortableTaskList so CanvasView/ProjectNode don't re-render on every drag-move tick |
| InsertTrigger | components/canvas/InsertTrigger.tsx | Controlled "+" button between tasks for inline task creation; editing state lifted to `SortableTaskList` for Enter-chaining (new task opens next trigger). Post-real-browser-testing P3 the component is `forwardRef<InsertTriggerHandle, Props>` exposing `useImperativeHandle({ focusInput })` — `focusInput` is a single `inputRef.current?.focus()` with the already-on-input / committed / no-input early-bail conditions preserved. The Enter-chain focus reclaim chain (rAF / t0 / t50 / t150 / t300 / `focusout` listener) is gone — Phase 2's debug-focus trace proved each is 0/40 in real Chromium and `setTimeout(_, 50)` scheduled by the parent post-commit is the only reliable path. `autoFocus` on the `<input>` is retained for the click-activate / Insert-key mount path (no row insertion → no React Flow ResizeObserver → no contention; MCP-confirmed 10/10 in P5). `useLayoutEffect(editing)` is trimmed to autocomplete dismiss + `titleRef`/`committedRef` reset on open. `DEBUG_FOCUS` instrumentation (gated on `?debug-focus=1`) stays — load-bearing for `e2e/focus-trace.spec.ts`'s per-label tally; retirement is tracked in `TODO.md`'s Deferred section |
| findAlignments, findAlignmentsScoped, findResizeSnap | components/canvas/alignment.ts | Snap-to-edge alignment for dragging/resizing nodes (5px threshold, guide lines) |
| computeCascadeShifts, CASCADE_GAP_THRESHOLD | components/canvas/cascade-shift.ts | Auto-shift stacked projects when a neighbor's height changes (40px gap threshold, BFS cascade) |
| drag-preview.css | components/canvas/drag-preview.css | Intentionally global CSS (not a module) so selectors can target React Flow's own drag-preview class names; imported once from `CanvasView.tsx` |
| CalendarStrip | components/canvas/rails/CalendarStrip.tsx | Shared calendar widget body used by both rail slots (via `CalendarSlotContent`) and floating nodes. 7-day window anchored on Monday of today's week + `weekOffset * 7`; `orientation` prop flips between vertical rows and a `repeat(7, minmax(0, 1fr))` horizontal grid. Renders a `RangeBar` with ‹ / › nav + Today button (only off-week) + "This wk" hint + month-aware range label when `onWeekOffsetChange` is provided. Post-Phase 7, rows are dnd-kit draggable (wrapped in `TaskDraggable` with `surface='calendar-strip'`); day cells are `useDroppable` with `data: { type: TASK_DROP_KIND.calendarDay, date }`. A per-instance `scope` (defaulted to a `useId` string) keeps each strip's day droppable ids distinct under a shared `DndContext`. Reschedule dispatch is owned by the `DndContext` host via `dispatchTaskDrop` → `calendar.reschedule`; the strip only registers draggables + droppables |
| EventRow | components/canvas/rails/calendar/EventRow.tsx | Single row inside `CalendarStrip`. Ui-consistency-2026-04-25 P2 replaced the prior inline avatar/marker code with the `<TaskPillStatus>` + `<TaskPillDates compact>` + `<TaskPillPeople compact>` atoms from the shared `<TaskPillBar>` family — EventRow puts dates *before* the title, so it consumes the atoms directly rather than the composed bar. Renders the recurrence `↻` glyph between dates and title; `compact` mode drops the org stack and caps people to 2 (used by horizontal columns). Receives `people` / `orgs` / `status` pre-resolved on `EventRowEntry` from `CalendarStrip`'s `buildEntries` (matches the `SearchResultPills` perf pattern — no per-row store subscriptions). The caller (`CalendarStrip`) supplies drag wiring via render prop; post-Phase 7 there is no `dataTransfer` plumbing on this component |
| calendar-events | components/canvas/rails/calendar/calendar-events.ts | Thin wrapper around `services/calendar-entries.buildEntries` (post-code-review-2026-04-25 P2) that locks the strip's `sortMode` to `'sortOrder'` and exposes a `StripEntry` type alias matching the strip's `EventRowEntry` shape. Virtual recurring rows carry the parent todo so reschedule drags mutate the parent's anchor. The shared helper handles bucketing + enrichment for both this strip and CalendarView's grid (CalendarView uses `sortMode: 'effective'`) |
| CalendarOrientationToggle | components/canvas/rails/calendar/CalendarOrientationToggle.tsx | Shared ☰/☷ toggle button mounted in `WidgetHeader`'s `meta` slot by both `CalendarSlotContent` (wired to `canvas-rails-store.setSlotOrientation`) and `FloatingCalendarNode` (wired to `floating-calendar-store.updateOrientation`) |
| TabStrip | components/canvas/rails/TabStrip.tsx | Multi-tab slot header rendered by `RailsFrame.SlotRenderer` in place of `SlotHeader` when `slot.tabs.length >= 2`. Each tab pill shows `KIND_ICON` + label (lens → list name, others → `KIND_LABEL[tab.type]` directly — triage-2026-04-27 P1 / Item 5 retired the per-kind switch in `tabLabel` so stats kinds (`status` / `scoreboard` / `snoozeGraveyard`) no longer fall through to "Calendar"; pills grow to fit label width — no `max-width` cap — and still `min-width: 0` + `.label` `overflow:hidden; text-overflow: ellipsis` so the strip shrinks to an ellipsis when cramped; full label is the `title` tooltip) + `×` close. **Per-pill kind change is right-click only (triage-2026-04-27 P1 / Item 4):** `onContextMenu` on the pill `<div>` calls `e.preventDefault()` then dispatches `onOpenChangeType(tab.id, { x: e.clientX, y: e.clientY })` so the parent's `WidgetKindMenu` opens at the click coords (matches OS context-menu convention); the prior `▾` caret button + its `.caretBtn` CSS are gone, and `aria-haspopup="menu"` / `aria-expanded` moved onto `pillButton`. Trailing `+` button opens `WidgetKindMenu` to append a tab; trailing chrome buttons (pop-out ↗ / more ⋯ / close ×) mirror single-tab `SlotHeader` — the `↗` is wired to `onPopOut` (added in triage-2026-04-25 P4) which routes through `popTabToCanvas(slot, activeTab.id)`. A `⋮⋮` drag handle surfaces the whole-slot drag activator from `DraggableSlot`. Drag: pills are `useDraggable({ data: { kind: 'tab', slotId, tabId } })`; the strip is `useDroppable` on `encodeRailsDropId({ kind: 'tab-strip', slotId })` — drop onto another `TabStrip` reorders/merges via `reorderTab`/`moveTabToSlot`; drop onto a slot-level zone detaches + docks via `detachTabToNewSlot`. Keyboard: arrow-key roving (Left/Right/Home/End activates, Delete closes) + `role="tablist"` / `role="tab"` / `aria-selected` |
| RailsFrame | components/canvas/rails/RailsFrame.tsx | Outer rails shell. Post-code-review-2026-04-25 P5 reduced from ~805 LOC to ~130 LOC: holds the 3×3 CSS grid (driven by `--left-size` / `--right-size` / `--top-size` / `--bottom-size`, **emitted only when the matching rail exists** so the var is unset when null — `RailsFrame.module.css`'s grid uses `var(--…-size, 0px)` to collapse the cell, while `DockOverlay`'s corner sub-zones use `var(--…-size, 80px)` to keep the corner-claim hit-target available when the perp rail is absent (real-browser-testing 6.5.2); each rail placed via `computeRailGridArea(rails, side)` so horizontal rails extend into NW/NE/SW/SE corner cells when the matching `rails.corners[c] === 'h'`), renders `DockOverlay` on top while a rails slot/tab drag is in flight (via `useRailsDragMonitor`) or a float drag is in flight (`ui-store.floatDrag !== null` — the float-drag gate also flips `DockOverlay`'s `floatDragActive` prop so drop strips render on every side, not just empty ones), maps `RailContainer` per side, and renders the dual `aria-live=polite` announcer regions. Each slot's chrome + body is now rendered by extracted `SlotRenderer` (not inlined). Hosts the hydrate-on-mount `useDefaultRails` hook and the drag-monitor wiring via `useRailsDragMonitor`. Pop-out helpers (`popSlotToCanvas` / `popTabToCanvas` / `popTabAtPosition` / `computePopOutFlowPosition`) live in `services/rail-pop-out.ts`; corner-claim helpers (`applyEmptySideCorners`) live in `utils/rail-corner-claim.ts`; drop-zone description / slot-kind / tab-label / tab-insert-index helpers live in `utils/rail-dnd-monitor-helpers.ts` |
| SlotRenderer | components/canvas/rails/SlotRenderer.tsx | Per-slot chrome + body switch extracted from `RailsFrame` in code-review-2026-04-25 P5 (~252 LOC). Reads the slot's active tab kind, picks `SlotHeader` or `TabStrip` (when `slot.tabs.length >= 2`), and dispatches the body component — `LensSlotContent` / `NotesSlotContent` / `CalendarSlotContent` / `TaskboardSlotContent` / `HorizonsSlotContent` — based on the active tab's `type`. Collapses 11 store-slice subscriptions to two `useShallow` reads off the canvas-rails store + the floating-* stores |
| DockOverlay | components/canvas/rails/DockOverlay.tsx | Drag-time drop-strip overlay rendered by `RailsFrame` during any rails slot/tab drag or any float-widget drag (`ui-store.floatDrag !== null`). Returns null only when both flags are off AND no side is empty; otherwise iterates `emptySides`. The `floatDragActive` prop tells the overlay to render even with zero empty sides — when a float drags over a fully-populated rail, docking still creates a new slot, so the affordance stays useful. For each rendered side, emits three `useDroppable` sub-zones — `start` / `center` / `end` — encoded via `encodeRailsDropId({ kind: 'empty-side', side, claim? })` and tagged with `data-rails-drop-id={id}` on the outer `<div>` so the float-dock pointer hit-test (`resolveFloatDockTarget` via `document.elementsFromPoint`) can find them without going through dnd-kit's collision pipeline. `start` / `end` extend into the adjacent corner cell. **Corner sub-zones key their perpendicular dimension off `var(--{left,right,top,bottom}-size, 80px)`** — `RailsFrame.tsx` only emits the `--{side}-size` var when the perpendicular rail exists (real-browser-testing 6.5.2), so the 80 px fallback fires only on the perp-rail-absent case (preserving corner-claim hit-target — `CORNER_HIT_MIN_PX` in `src/utils/rail-dnd.ts`). When the perp rail exists (collapsed at `COLLAPSED_RAIL_PX = 28 px` or expanded at any width) the corner sub-zone matches the rail's size exactly, replacing the pre-fix `max(var(--…-size, 0px), 80px)` floor that overshot collapsed rails by ≥52 px and occluded their slot stubs from `document.elementsFromPoint`. The matching center sub-zone's start/end edges follow the same `var(...)` so corner + center never overlap. The CSS narrowing is paired with a resolver-side preference (slot > tab-strip > empty-side in `resolveFloatDockTarget`) so even when the corner overlay still covers a slot stub vertically, the resolver picks the slot. (bugs-ui P2 fixed top + bottom; code-review-2026-04-25 P3 mirrored the prior floor onto left + right; real-browser-testing 6.5.2 replaced the floor with the var-fallback contract.) `useRailsDragMonitor.applyEmptySideCorners` resolves the drop with two roles per adjacent corner: **claimed** (dropped rail extends into the corner → owner = dropped-rail axis: `'h'` for top/bottom, `'v'` for left/right) and **pinched** (dropped rail does NOT extend → owner = opposite axis, so perpendicular rails own when present, with `resolveCorner` falling back cleanly when absent). `claim='start'` claims start + pinches end; `claim='end'` mirrors; no claim pinches both (the dropped rail is pinched between perpendicular neighbors). Writes route through `setCornerOwner` when the target ≠ default (`'v'`) and `clearCornerOwner` when it ==  default, keeping the persisted bag minimal. The float-dock path uses `utils/float-dock-announce.computeEmptySideCornerClaim` + the same `setCornerOwner` / `clearCornerOwner` actions for parity. The strip wrapper is `display: contents` so each sub-zone positions absolutely against `.overlay` (a grid item spanning all 3×3 cells) rather than a 0-sized parent |
| RailContainer | components/canvas/rails/RailContainer.tsx | Rail shell. Renders an `<aside>` sized by the current `railSize` (width for verticals, height for horizontals; clamped to `[RAIL_SIZE_MIN=60, RAIL_SIZE_MAX=600]`). Two pointer-driven edge controls share a single rAF scheduler (`useThrottledResize`): `RailEdgeStrip` — an invisible 6 px drag strip spanning the full canvas-facing edge (drag-only, `col-resize` / `row-resize` cursor) — and `RailEdgeHandle` — a small neutral button (14×36 chip for vertical rails, 36×14 for horizontal) centered on the edge that click-toggles `toggleRailCollapsed(side)` and drag-resizes via `setRailSize` once the pointer has travelled past `DRAG_THRESHOLD_PX` (3 px). The handle uses `--color-surface` + `--color-border` + `--radius-sm` so it matches the rest of the app's chrome, hover brightens to `--color-surface-high`. Keyboard on the handle: Enter/Space toggle, arrow keys resize by 20 px. Collapse is an explicit action (button, keyboard) — dragging past the clamp minimum does not auto-collapse. When `collapsed === true` (from `rails.collapsed[side]`), the body content + edge strip are hidden (only the handle remains, rendered via `overflow: visible` so its half-outside chip stays fully visible against the narrow strip), and `railSize` returns `COLLAPSED_RAIL_PX` (28 px). The collapsed body renders one `CollapsedSlotStub` per slot — each shows the slot's `KIND_ICON` plus a semibold text label (the list definition name for lens slots via `useListDefinitionStore`, else the kind's title-cased `KIND_DISPLAY` label); labels are rotated with `writing-mode: vertical-rl` on vertical rails so they read top-to-bottom and stay horizontal on top/bottom rails. Each `CollapsedSlotStub` registers the same `rails:slot:<id>` drop zone the expanded `DraggableSlot` would (`useDroppable` + `data-rails-drop-id`) so float / slot / tab drags can dock onto a collapsed rail via the small stub rect; the over-state highlight (`.iconStubOver`) reuses `--color-accent` at 18% opacity, matching the expanded slot's drag-over treatment (triage-2026-04-25 P5 — pre-fix, collapsed rails silently rejected drops because the slot drop zone disappeared with the body). React-Flow-driven float drags bypass dnd-kit, so `droppable.isOver` never fires while a floating widget is being dragged over a stub; the stub mirrors `DraggableSlot`'s workaround — subscribes to `ui-store.floatDrag`, runs a global `pointermove` listener gated on the stub's `getBoundingClientRect()` while a float drag is in flight, and ORs the rect-hit with `droppable.isOver` to drive `.iconStubOver` (float-dock-bugs-2026-04-25 P1 — pre-fix, the stub had no visible dock target during float drags so users released off the small rect). **Whole-aside catch-all (triage-2026-04-26 P1 / T3):** when collapsed, the `<aside>` itself carries `data-rails-drop-id={ kind: 'collapsed-side', side }` (encoded as `rails:collapsed-side:<side>`), so a release that lands inside the aside but misses every individual stub (margin / gap between stubs / top-bottom padding) still resolves. `resolveFloatDockTarget` ranks slot-id drop zones above collapsed-side, so the per-stub `rails:slot:<id>` zones still take priority — the catch-all only fires on the leftover area, where the resolver bisects child `[data-slot-id]` stubs by axis distance (vertical rails by Y, horizontal by X) and routes the dock to the nearest stub via center-merge. **Aside-level nearest-stub visual highlight (triage-2026-04-27 P5 / Item 2):** `RailContainer` lifts the float-drag pointer subscription from per-stub up to the aside scope. While `useUIStore.floatDrag !== null` AND the rail is collapsed, a single window-level `pointermove` listener gated on the aside's `getBoundingClientRect()` runs the now-exported `nearestStubSlotId(asideEl, pointer, side)` from `utils/rail-dnd.ts` and stores the result in local `nearestStubId` state, threaded down to each `CollapsedSlotStub` as `highlightFromAside={floatDragActive && nearestStubId === slot.id}`. Each stub ORs that with `droppable.isOver` to drive `.iconStubOver`, so the user sees one stub highlighted continuously over the entire aside (margin / between-stub gaps / aside padding included). The per-stub `pointerInside` shortcut is gone — aside-level "nearest" covers the inside-stub case (nearest = the one you're inside). `nearestStubSlotId` was renamed-and-exported (was internal to `resolveFloatDockTarget` only) so resolver-side hit-test and visual highlight share one bisection helper — the visible target and the actual dock target can never diverge. `droppable.isOver` still drives the dnd-kit slot/tab drag highlight unchanged |

## Overlays

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| BulkConfirmDialog | components/overlays/BulkConfirmDialog.tsx | Confirmation dialog for destructive bulk actions (delete, complete/uncomplete); supports custom messages, labels, and skipIds for two-option dialogs. Code-review-2026-04-25 P7 migrated to the shared `<Dialog>` primitive — focus trap, Esc dismissal, autoFocus on the primary action are inherited. **Triage-2026-04-27 batch2 P3 / item 2:** routes through `<Dialog nested>` so backdrop + dialog land at `--z-dialog-nested` (1003) — keeps the singleton confirm above any open list-editor / dashboard-lists modal at `--z-dialog-overlay` (1002) instead of disappearing behind |
| UndoSnackbar | components/overlays/UndoSnackbar.tsx | Bottom-center toast after destructive actions with "Undo" button, auto-dismiss 5s |
| FilterSheet | components/overlays/FilterSheet.tsx | Thin mobile wrapper around `<FilterChipBar predicate={...} onChange={...} density="mobile" />` (post-code-review-2026-04-25 P4). The accordion sections (search, date range, toggles, people / orgs / statuses / tags) all live in the shared primitive at `density="mobile"`; the sheet contributes the bottom-sheet chrome + close button + reads/writes `useFilterStore` via `criteriaToPredicate` / `predicateToCriteria` |
| CanvasContextMenu | components/overlays/CanvasContextMenu.tsx | Reusable right-click context menu (canvas background, project, box). Code-review-2026-04-25 P7 added `role="menu"` + `role="menuitem"` on rows, arrow-key navigation (Up/Down + Home/End), autoFocus on the first item, Esc dismissal — a11y parity with the new `<Dialog>` primitive |
| FilteredListPopup | components/overlays/FilteredListPopup.tsx | On-demand floating list popup triggered by right-clicking person/org on any TaskRow; reads from stores directly |
| MigrationDialog | components/overlays/MigrationDialog.tsx | Confirmation dialog for data migrations; `schema-upgrade` mode (full-screen, Dexie upgrade) and `legacy-import` mode (overlay modal, file/import); export backup button + apply/cancel. Code-review-2026-04-25 P7 migrated to the shared `<Dialog>` primitive in legacy-import mode (the schema-upgrade mode keeps its own full-screen layout because it must render before any router context is up) |
| ProjectPickerPopup | components/overlays/ProjectPickerPopup.tsx | Portal-rendered positioned popup wrapping `ProjectPicker`; closes on outside-click / Escape; used by TaskRow right-click "Move to project…" |
| ListDefinitionPickerPopup | components/overlays/ListDefinitionPickerPopup.tsx | Portal-rendered "Add list to canvas" popover. Post-code-review-2026-04-25 P8 the body is now a thin wrapper around `<ListDefinitionPickerBody>` (chrome + portal positioning + outside-click + the "Add" action label) so the two surfaces share one rendering path; the popup contributes the floating-popover chrome only. Callers wire `onSelect` to list-inset / lens creation |
| ListDefinitionPickerBody | components/overlays/ListDefinitionPickerBody.tsx | Single source of truth for "list of saved list-defs with optional action button per row". Filter logic (`excludeIds` hides listed ids, otherwise shows every def), action-label per row opt-in via `actionLabel`, optional header / empty-message / create-new footer, and `onPick(id)` as the single click handler. Used by the `WidgetKindMenu` "Change list…" inline flyout (no action label) and — post-code-review-2026-04-25 P8 — by `ListDefinitionPickerPopup` |
| QuickAddBar | components/overlays/QuickAddBar.tsx | Keyboard-first task capture surface; primary entry point for new tasks (post-quickadd-bar P5). Top-anchored single-row bar at 120 px, no scrim, opened via `Ctrl+Space` (`use-keyboard-shortcuts.ts:90-94` → `useUIStore.openQuickAdd`), the FAB (`App.tsx`), or `command-registry.ts:openQuickAdd`. **Redesign-2026-04-26 layout**: 18 px title input + outlined `Details ⇥` button + muted `↵ create · esc close` cluster — no `+` glyph, no filled-accent Create button, no in-bar expanded panel, no Notes textarea, no footer "Open full editor →" link. The wrapper `AppQuickAddBar` (`App.tsx`) subscribes to `ui-store.quickAddOpen` / `quickAddDraft` and threads `defaultProject` (current `useSettingsStore().defaultProjectId`), `onSubmit` (mirrors `CanvasPage.handleAddTask` — reads `useCanvasStore.getState().selectedCanvasId` and aborts when null, resolves project via `submitted.project?.id ?? (await ensureDefaultProject(canvasId))` from `services/ensure-default-project.ts` (triage-2026-04-27 P6 / Q7=A — first canvas project if any exist, else creates an "Inbox" project at (0, 0) seeded with `settings.defaultProjectGroupBy` and persists `settings.defaultProjectId`; pre-fix `add(title, undefined, projectId)` passed `canvasId=undefined` unconditionally, leaving blank-canvas adds invisible), then `todoStore.add(title, canvasId, projectId)` + `applyNlpMetadata`), and `onOpenFullEditor` (stashes `{ rawTitle }` on `ui-store.quickAddDraft` — notes field dropped in the redesign, `QuickAddDraft` no longer carries it — flips `quickAddOpen=false` without clearing the draft, dispatches `openCreatePopup`; `closeEditPopup` clears the draft). The bar runs the live `parseInput` → `resolveInput` pipeline against the person / org / project / status / tag stores on every keystroke and renders read-only chips for every metadata field the resolver produces — `StatusChip` / `PersonChip` / `OrgChip` / `ProjectChip` / `TagChip` / `DateChip` / `RecurrenceChip`. Auto-derived chips have no `×` (clear by editing the title text). Empty-state hint row covers every NLP token: `@person` · `/project` · `#tag` · `:status` · `!deadline (mon, fri…)` · natural dates (`tomorrow, fri 3pm`) · recurrence (`every week`). Autocomplete reuses `useNlpAutocomplete` (the same hook `InsertTrigger` consumes); the popup is `createPortal`'d to `document.body` with the `RuntimeFilterPicker` flip-above-when-clipping + clamp-on-right-edge math + scroll/resize tracking. **`Tab` and the visible `Details` button both call `onOpenFullEditor?.(draft)`** — same handoff path; the bar deliberately does not call `onClose` here (parent owns the close-vs-handoff sequencing). Title input carries `data-shortcut-scope="none"` so `use-keyboard-shortcuts.ts:34-44` skips global shortcuts while typing. The legacy `TaskEditPopup` create modal stays reachable via the bar's Details / Tab handoff and via the canvas right-click "New Task" menu (deliberate — that flow already picks a target project) |

## Popover patterns

Every floating panel anchored to a click-coord or a trigger element routes positioning + dismissal through the shared `usePopoverAnchor` hook (`hooks/use-popover-anchor.ts` — see services-and-utils.md). The hook owns flip-on-clip + clamp + scroll/resize/Escape/outside-click; the caller owns the panel + its `createPortal` wrapper. Built ui-consistency P1; the seven popovers covered are `RuntimeFilterPicker`, `ListDefinitionPickerPopup`, `WidgetKindMenu`, `FilteredListPopup`, `SlotMenu`, `CanvasContextMenu`, and `ProjectPickerPopup`.

**When to add a new popover, do this and only this:**

1. Choose an anchor: `{kind:'ref', ref}` for an element-anchored popover (panel tracks the trigger on scroll); `{kind:'point', x, y}` for click-coord popovers (right-click / contextmenu).
2. Call `usePopoverAnchor({ anchor, open, onClose })`. Defaults handle scroll-close / resize-close / Escape / outside-click.
3. Render the panel inside `createPortal(<div ref={panelRef} style={style}>…</div>, document.body)` — the hook is the *only* place that should compute panel position; consumers should not also write a separate clamp / flip `useEffect`.
4. For nested flyouts whose drop target sits outside the panel (e.g. `WidgetKindMenu`'s Stats / Change-list submenus), pass `extraInsideRefs: [flyoutRef]` so outside-click ignores them.
5. To preserve a popover's "no behavior change" path during migration: ref-anchored popovers that previously *repositioned* on scroll (`RuntimeFilterPicker`) pass `closeOnScroll: false, closeOnResize: false` — the hook reposition-mode kicks in for `kind: 'ref'` anchors, while `kind: 'point'` anchors stay static.
6. Popovers that own special Escape logic (e.g. `WidgetKindMenu`'s "close flyout first, then menu") pass `closeOnEscape: false` and keep their own keyboard handler on the panel.

**What NOT to do:**

- Do not write per-component `useEffect` for `getBoundingClientRect()` math, scroll listeners, resize listeners, or document-level `mousedown` / `keydown` outside-click handlers. Every one of those is folded into the hook.
- Do not portalize a popover from the *call site* (`createPortal(<MyPopover/>, body)` at the consumer). All the popovers above portal *internally* — adding a wrapping portal at the call site double-portals harmlessly but obscures intent and leaves stale parallel `createPortal` patterns the audit explicitly retired (`CanvasContextMenu` / `ProjectPickerPopup` previously had split call-site portal patterns; ui-consistency P1 collapsed them).

## DnD diagnostic flag (`?debug-dnd=1`)

Append `?debug-dnd=1` to the URL to instrument every guard branch and resolution path across the rails / float / taskboard / calendar drag-drop subsystem. Built ui-consistency P5 in response to triage P5's forensic diagnosis effort: the next time a drag silently misses (no console error, no visible feedback, the gesture just appears to do nothing), the user or reviewer flips the flag, redoes the drag, and the console emits the full ladder — source slot/tab → collision rule chosen → resolved `over.id` → guard outcomes → final dispatch.

The flag is read once at module load via `utils/debug-flags.ts` (mirrors `DEBUG_FOCUS` / `?debug-focus=1` from `InsertTrigger.tsx`); each call site uses the gated `dndLog(label, payload?)` helper so production console stays quiet when the flag is absent.

**Wrap sites:**
- `utils/task-dnd/dispatch.ts` — every `return false` / `return true` branch (no-active-todo, calendar reschedule, taskboard reorder / move-to-end / remove-on-drop-off, taskboard add, unhandled).
- `utils/task-dnd/collision.ts` — `buildTaskCollision` rule selection (no-active fallback, rule matched with index + algorithm + droppable counts, no-rule-matched fallback).
- `utils/rail-dnd.ts` — `resolveFloatDockTarget` resolution (miss / empty-side / tab-strip / collapsed-side center-merge / collapsed-side no-stub / canvas-fallthrough / slot.no-orientation / slot).
- `hooks/use-rails-drag-monitor.ts` — `useRailsDragMonitor` `onDragStart` + `onDragEnd` (non-rails-drag bail, no-over, unknown-drop-id, resolved zone, all five canvas-branch silent aborts — no-pointer / no-canvas-el / no-src-slot / no-src-tab / no-canvas-id — and the slot-drag fallback).

**Not wrapped:** stats widgets (`FloatingStatusNode` / `FloatingScoreboardNode` / `FloatingSnoozeGraveyardNode`) consume the shared `useFloatingWidget` hook and dock via the dock-button click path, NOT dnd-kit drag events. They participate in the rails-monitor float-dock flow when the user drags them, but don't initiate any new DnD entry-points — no new wrap points needed for them.

## Settings Editors

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| ThemeColorsEditor | components/settings/ThemeColorsEditor.tsx | Settings modal for the per-theme color overrides (triage-2026-04-27 P4 / Q1=A). Reads `useResolvedTheme()` on mount to seed a local `workingTheme: 'dark' \| 'light'` state and renders a Light/Dark segmented toggle (`role="tablist"` + `aria-selected`) at the top of the modal — switching the toggle flips which `colors[theme]` bag the inputs bind to *without* changing the user's actual theme preference (that lives in SettingsPage's theme-mode control). Header copy reads "Theme Colors — <Working theme>". The same eight `colorLabels` keys (accent / canvasBg / surface / danger / warning / star / scheduled / deadline) bind to `setColor(workingTheme, key, value)`; the Reset button targets only the working bag via `resetColors(workingTheme)`. Companion `ThemeColorsEditor.module.css` carries the toggle pill styling. The store-side legacy migration (`color.<key>` rows lifted into the `dark` bag on `load()`) is in `settings-store`; this editor only routes per-theme writes |
| DashboardListsEditor | components/settings/DashboardListsEditor.tsx | Settings modal (default title **"Lists"**, width override `modalWide` → 760 px so `ListFilterEditor`'s chip row + expanded `DateRangeDropdown` fit without horizontal scroll; the modal body also enforces `overflow-x: hidden` per bugs-ui P1 so even an un-flipped picker degrades to clipping rather than producing a scrollbar that closes the picker on touch): dnd-kit reorderable list of list definitions, per-row Favorite toggle (writes `listDefinitionStore.setFavorited`; after features-batch-2026-04 P5 this is the only pin-shaped affordance — the separate "pin to dashboard" toggle retired with DashboardView and `settings.dashboardUserLists` became a dormant key), delete confirm, "+ Add List" footer. **Modal-on-modal pattern (lists-editor-modal P2)**: per-row ⚙ button OR clicking the row's name fires `handleEdit(id)` → sets `editingListId` → mounts `<ListEditorDialog>` portaled to `document.body` over the Lists modal (Lists modal stays mounted, dimmed by the editor scrim). The dialog body is the shared `ListEditorBody` (`components/shared/`, extracted in triage-2026-04-26 P5 / T7 alongside `<StandaloneListEditor>` so manager + standalone flows share one form body) — Name input + `ListFilterEditor` (`predicate` from `membership.custom.predicate`) + the shared `<SortGroupToolbar density='comfortable'>` over flat `TodoSortBy` / `TodoGroupBy` (ui-consistency-2026-04-25 P4 — the prior single-`<select>` over a discriminated-union value + its `encodeSortValue` / `decodeSortValue` / `encodeGroupingValue` / `decodeGroupingValue` helpers + the `requiresSortBy` cross-field disabled rule were retired when the on-disk shape flattened) + **Prompt** single-`<select>` (lists-consistency P2 renamed the "Runtime filter" label; the underlying field is still `runtimeFilter` and the picker still calls `applyRuntimeFilter`; `RUNTIME_FILTER_OPTIONS` lists `none`/`person`/`org`/`project`/`status`/`tag`). The body owns no chrome — header eyebrow ("Edit list ·" + frozen list-name snapshot), header `Add to favorites` toggle, header `×`, footer `Cancel` / `Save` all live on `ListEditorDialog`. Row inline-rename retired in lists-editor-modal P2 — the `editing` state + `startEdit` / `saveEdit` / `handleEditKeyDown` triple is gone; Name editing happens in the body draft, committed on dialog Save (`update(draft)` after a `name.trim()` non-empty check; empty name keeps the editor open). Draft state is held on the editor parent (`useState<PersistedListDefinition \| null>`) and synced to the upstream def via an effect that adopts a fresh def when the draft is clean and forwards externally-managed fields (`favorited` / `pinnedToDashboard` / `sortOrder`) when the draft is dirty so a header-fired favorite or a reorder doesn't get reverted on Save. `defsEqual` (extracted to `utils/list-def-equal.ts` in triage-2026-04-26 P5 / T7) powers the dirty check (ignores `sortOrder` + `favorited`). `guardDirty(perform)` wraps every editor-dismissing path (Esc / scrim click / `×` / `Cancel` / clicking another row's ⚙ / closing the outer Lists modal); when dirty it routes through `showBulkConfirmation('custom', [], { title: 'Discard changes?', message, confirmLabel: 'Discard', onConfirm: perform })` matching other destructive prompts. `Add to favorites` button in the dialog header fires `setFavorited` immediately — never part of the draft, no Save needed. Accepts `filterIds?: number[]` + `title?` + `initialSelectedId?`: `filterIds` scopes the list and hides Add / Delete for the "Edit horizons…" entry point; `initialSelectedId` seeds `editingListId` on first mount so the dialog opens on a specific row (used by the canvas list widget's "Edit list" affordance — lists-consistency P1 routes through `useUIStore.listsEditorInitialId`). The "Edit in ListView…" deep-link of the pre-dashboard-lists-editor-P4 editor is gone — filter editing happens inside the dialog |
| TagEditor | components/settings/TagEditor.tsx | Settings modal mirroring `OrgEditor`: list of `Tag` rows (sorted by name) with color swatch + name + assigned-count pulled from `tagRepository.getTodoCounts`. Add flow: name input + `ColorInput` (defaults to `DEFAULT_ENTITY_COLOR`; duplicate names rejected case-insensitively). Edit in place: name + color, Enter saves / Escape cancels. Delete flow: confirmation dialog showing `getTodoCount(tagId)` — delete is terminal, `tagRepository.delete` cascades `todoTags` rows inside a transaction but the top-level `tag-store.remove` wraps it in `undoable` (restore re-hydrates both the tag row and the cascaded joins via `restoreEntityWithJoins`). Search field filters the list. Rename is a single registry row update — no walking todos — so every consumer picks up the new name via the `assignedTagsMap` refresh |
| ListFilterEditor | components/settings/ListFilterEditor.tsx | Thin wrapper around `<FilterChipBar predicate={draft} onChange={onChange} density="desktop" />` — the editor's full chip-row vocabulary (people / orgs / projects / statuses / tags / date / toggles / search) lives in the shared primitive (post-code-review-2026-04-25 P4). Consumed only by `DashboardListsEditor.ConfigPanel` (editing a draft `membership.custom.predicate`); ListView's inline mount was removed because TopBar + mobile `FilterSheet` already cover `/list`'s filter chrome. Originally extracted in dashboard-lists-editor P2 to retire the "Edit in ListView…" deep-link |

## Shared Components

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| AvatarStack | components/shared/AvatarStack.tsx | Overlapping circle avatars with `+N` overflow (default max=3); fill-variant color derived from the person's first assigned org via `utils/person-color.resolvePersonColor` (subscribes to `useOrgStore`; falls back to `DEFAULT_ENTITY_COLOR`); hollow variant uses the org's own color. `sm` variant; click bubbles to `onClick` (opens picker in TaskRow), right-click on a visible avatar surfaces per-person context menu. Ui-consistency-2026-04-25 P2 added `readOnly` (renders as plain `<span>`s instead of `<button>`s — used by the search dropdown so chips never intercept the row click or dnd-kit drag listener) and `colorContext` (`{ personOrgMap, orgs }` — bypasses the internal `useOrgStore` subscription so dense per-row consumers like the search dropdown stay perf-bounded). Consumed by `TaskRow` / `MobileTaskRow` (via `<TaskPillBar>`) + canvas calendar `EventRow` (via `<TaskPillPeople>`) |
| TaskPillBar, TaskPillPeople, TaskPillDates, TaskPillStatus | components/shared/TaskPillBar.tsx | Shared task-pill primitive (ui-consistency-2026-04-25 P2). `<TaskPillBar>` composes the people → dates → status layout used by `TaskRow` / `MobileTaskRow` / TopBar `SearchResultRow`; the three atoms (`<TaskPillPeople>` / `<TaskPillDates>` / `<TaskPillStatus>`) are exposed for surfaces with a different layout — canvas calendar `EventRow` puts dates before the title and renders the atoms directly. Props: `todo` + pre-resolved `people` / `orgs` / `status` + `today` + `weekStartsOn` + optional `interactive` (default true; SearchResultRow + EventRow pass false to render non-interactive `<span>`s so the row's outer click target / drag activator stays unobstructed) + `compact` (icon-only date markers + smaller avatars for canvas calendar columns) + `density: 'default' \| 'small'` (mobile parity) + `personColorContext` (`{ personOrgMap, orgs }` threaded down to `<AvatarStack>` to bypass its internal `useOrgStore` subscription on dense surfaces). `<TaskPillDates>` accepts `layout: 'inline' \| 'stack'` — `'stack'` is `TaskRow`'s vertical date column. Tag-display rule baked into the shape — no `tags` prop exists; rows never render tag chips (manage tags from `TaskEditPopup`). Perf invariant: the primitive does NOT subscribe to assignment stores — every consumer threads pre-resolved data and resolves once at the surface boundary. Replaces the prior parallel chip implementations across the four consumers (the inline `SearchResultPills` block in TopBar + the inline avatar/marker code in EventRow + the per-surface chip / avatar / status CSS in `TaskRow.module.css` / `MobileTaskRow.module.css` / `EventRow.module.css` / `TopBar.module.css` are gone) |
| SortGroupToolbar, SortGroupOption | components/shared/SortGroupToolbar.tsx | Shared sort + group toolbar (ui-consistency-2026-04-25 P3). Generic over `S extends string, G extends string` so each consumer threads its own per-surface subset constant (`PROJECT_SORT_VALUES` / `LIST_SORT_VALUES` / etc. from `models/todo-sort-group.ts`). Two densities: `'comfortable'` wraps two `IconSelect` triggers (Group + Sort) inside labelled fields — used by `ListView`'s toolbar (collapses the prior `IconSelect` pair + asc toggle into one toolbar); `'compact'` renders icon-only anchored buttons (`↕` / `↑` / `↓` for sort, `⊟` for group) with per-field menus dismissed via `useClickOutside` + Escape — used by `ProjectNode` (collapses ~46 lines of parallel sort/group menu blocks + four hooks into one `<SortGroupToolbar density='compact' showSort={todos.length>1}>`). Props: `sortBy` / `groupBy` (current values) + `sortOptions` / `groupOptions` (`SortGroupOption<T>[]` = `{ value, label, icon? }[]`) + `onSortChange` / `onGroupChange` + optional `sortAsc` / `onToggleAsc` (asc-only toggle button when `onToggleAsc` is provided; ProjectNode reads `lastSort` state to decide same-sort-click → asc-toggle vs new sort) + `showSort` / `showGroup` (false-y hides the field) + `className` (consumer-controlled CSS for hover-reveal etc) + `groupLabel` / `sortLabel` / `ariaLabelSort` / `ariaLabelGroup`. Same-sort-click → asc-toggle semantic owned by the consumer (toolbar fires `onSortChange` with the same value; consumer flips asc). Consumed by `ListView` + `ProjectNode` + `ListEditorBody` (the latter folded in via P4 once the on-disk shape flattened — see ListEditorBody row below) |
| ChipSelector | components/shared/ChipSelector.tsx | Reusable autocomplete dropdown for assigning people/orgs; search input, filtered list, create-new option |
| IconSelect | components/shared/IconSelect.tsx | Generic `<select>` replacement showing a per-option icon in the trigger and each menu row. Triage-2026-04-27 batch2 P3 / item 3 migrated the menu to `usePopoverAnchor` + `createPortal(document.body)` so it escapes the parent dialog's `body { overflow: auto }` and flips/clamps when it would clip the viewport bottom. Inline style threaded from the hook is **only** `position`/`left`/`top` — applying the hook's `maxHeight` would collapse the panel under INITIAL_STYLE and disable flip math (the hook fires its single `compute` on attach). Trigger `onKeyDown` handles Enter/Space/ArrowDown to open + ArrowUp/Down cycling; Escape is owned by the hook's document-level listener. Used by `<SortGroupToolbar density='comfortable'>` (ListView + ListEditorBody) |
| groupByIcons, itemSortByIcons | components/shared/list-option-icons.tsx | Icon registry for `ListGroupBy` / `ListItemSortBy` values; reuses `StatusIcon` (calendar/clock/flag/person/circle) + inline SVGs for project/org/none/manual |
| ColorInput | components/shared/ColorInput.tsx | Shared color picker: native swatch + editable hex text input with validation, 3-digit expansion, auto-# prefix, blur revert |
| ProjectPicker | components/shared/ProjectPicker.tsx | Shared project search + list UI (with "No project" option); self-contained search state |
| StatusIcon | components/shared/StatusIcon.tsx | Inline SVG icon registry for statuses (15 icons: person, message-bubble, circle, star, stop-sign, exclamation, clock, check, question, flag, eye, bookmark, snooze, arrow, calendar); returns null for unknown/missing icon |
| SchedulePicker | components/shared/SchedulePicker.tsx | Trigger chip + inline popover for `scheduledDate` (wraps `ScheduledValueMenu`); expired-fuzzy state shown with overdue marker |
| ScheduledValueMenu | components/shared/ScheduledValueMenu.tsx | Shared menu body for scheduled-value editing: 3×2 fuzzy-token grid (Day/Week/Month × now/next), action footer with "Pick a specific day…", optional "Add deadline…", conditional "Clear" |
| DeadlinePicker | components/shared/DeadlinePicker.tsx | Danger-tinted chip that opens native date picker for `dueDate`; inline clear button; precise-only (no fuzzy) |
| DateAnchorInput | components/shared/DateAnchorInput.tsx | Shared filter-predicate anchor picker: native `<input type="date">` for fixed anchors paired with a `<select>` of the 12 `RelativeDateToken`s (yesterday, today, tomorrow, start/end-of-week, start/end-of-month, etc.) plus an explicit `None` sentinel that clears the anchor to null; mutually exclusive — selecting a token clears the date and vice versa. `Custom…` is only rendered in the select when the value is a fixed anchor. Used by TopBar `DateRangeDropdown` and mobile `FilterSheet` |
| ErrorBoundary | components/shared/ErrorBoundary.tsx | Generic React error boundary (class component, documented exception); catches render errors, shows scoped fallback with "Try again" / "Reload"; wired at App level and around Canvas route |
| NlpAutocomplete | components/shared/NlpAutocomplete.tsx | Floating dropdown for autocomplete suggestions; renders people / orgs / projects with color dots |
| WidgetHeader | components/shared/WidgetHeader.tsx | Unified slot/widget chrome used by rails (`SlotHeader`), floating nodes (`FloatingNoteNode`, `FloatingCalendarNode`, `TaskboardNode`), and `ListInsetNode`. Renders kind icon (via `KIND_ICON`) + drag handle + title + meta + action buttons (pop-out ⇱, dock ↙, more ⋯, close ×). `floating` prop switches the button row to hover-reveal + applies React Flow's `nopan nodrag` classes. `onTitleClick` makes the title a `▾`-caret button that fires anchor coords — the carrier for the `WidgetKindMenu` opener |
| Dialog, DialogProps | components/shared/Dialog.tsx | Shared modal primitive (code-review-2026-04-25 P7). Owns focus trap (Tab cycles focusables inside the dialog body), Esc dismissal, autoFocus (first focusable inside the dialog OR caller-supplied `initialFocusRef`), `role="dialog"` + `aria-modal="true"` + `aria-labelledby={titleId}`, restore-focus-on-unmount (returns focus to the element that was focused when the dialog opened). Backdrop click closes by default (`blockBackdropClose` opts out). `size: 'sm' \| 'md' \| 'lg'` width preset. Children own the body; `title` slot renders into the chrome's labelled title bar. Consumed by `BulkConfirmDialog` / `ReassignDialog` / `MigrationDialog` (legacy-import mode) / `DashboardListsEditor` per-row delete confirm + the four entity-editor delete confirm strips (`PeopleEditor` / `OrgEditor` / `StatusEditor` / `TagEditor`) |
| ListEditorDialog | components/shared/ListEditorDialog.tsx | Modal-on-modal shell for editing a single saved list definition (lists-editor-modal P1). Portals scrim + dialog to `document.body` at `--z-dialog-overlay` (1002) so it lays above the Lists modal at `--z-dialog` (1001) without being clipped by the outer modal's stacking context (popovers inside the body — runtime-filter picker, date picker — already portal to body, so they layer cleanly above). Props: `{ open, list: { id: number; name: string; favorited: boolean } \| null, onClose, onSave, onToggleFavorite, children }` — caller supplies the form body (today only `DashboardListsEditor.ListEditorBody`; the dialog itself is body-agnostic). Header eyebrow reads "Edit list ·" + the dialog title shows `list.name` as a **frozen snapshot** (not live-updated as the user types in the body's Name input — avoids title jitter); `Add to favorites` star toggle fires `onToggleFavorite` immediately, no Save needed (matches the row checkbox semantics). Footer is `Cancel` / `Save`. Esc handler is registered on `window` with `capture: true` + `stopPropagation()` so Esc closes the editor without bubbling to the Lists modal underneath; scrim click → `onClose`; click inside the dialog → `stopPropagation` (so it doesn't reach the scrim). Focus drops to the dialog root on open; **no focus trap** in v1 — matches existing modal practice. Hard short-circuits to `null` when `!open \|\| !list` (the `initialSelectedId` race where the def list hasn't loaded yet falls into this branch). No focus-restore on unmount in v1 — picked up if/when this folds into the shared `<Dialog>` primitive (out-of-scope future). Tokens: `--color-favorite` (#e6c34d) for the star fill, `--color-border-strong` (aliased to `--color-border` in v1) for the dialog frame, `--z-dialog-overlay` for the portal layer |
| ListEditorBody | components/shared/ListEditorBody.tsx | Shared form body for editing a single saved list definition. Owns the Name input + `ListFilterEditor` (`predicate` from `membership.custom.predicate`) + the shared `<SortGroupToolbar density='comfortable'>` over flat-literal `TodoSortBy` / `TodoGroupBy` options (`LIST_EDITOR_SORT_VALUES` / `LIST_EDITOR_GROUP_VALUES` from `models/todo-sort-group.ts`) + **Prompt** single-`<select>` (`runtimeFilter.field`). Ui-consistency-2026-04-25 P4 dropped the prior `encodeSortValue` / `decodeSortValue` / `encodeGroupingValue` / `decodeGroupingValue` helpers + the `requiresSortBy` cross-field disabled rule — both went away when the discriminated-union `ListDefinition.sort` / `.grouping` shape flattened to flat literals on disk via the v46 Dexie migration; the editor now reads/writes the persisted shape directly. Stateless predicate adapter shape `{ draft, onChange }`; renders no chrome (no header eyebrow, no `Cancel` / `Save`, no `Add to favorites` toggle — those live on `<ListEditorDialog>`). Extracted in triage-2026-04-26 P5 / T7 from `DashboardListsEditor` so the lists-manager flow (`DashboardListsEditor` → `ListEditorDialog` → `ListEditorBody`) and the standalone flow (`<StandaloneListEditor>` → `ListEditorDialog` → `ListEditorBody`) share one form body |
| StandaloneListEditor | components/shared/StandaloneListEditor.tsx | Direct list-editor surface (triage-2026-04-26 P5 / T7 / Q7=A). Mounted once by `CanvasPage` (sibling of `<DashboardListsEditor>`), subscribes to `useUIStore.listEditorDialogId`, resolves the target def from `useListDefinitionStore`, owns its own draft + dirty + save / close / favorite state via `defsEqual` (in `utils/list-def-equal`), and renders `<ListEditorDialog list={…} onSave onClose onToggleFavorite>` over `<ListEditorBody>` *without* a `DashboardListsEditor` Lists modal underneath. Drives the "Edit list" entry from a tab pill / `ListInsetNode`'s widget-kind menu (`SlotRenderer` + `ListInsetNode` both fire `useUIStore.openListEditorDialog(defId)`); SettingsPage's "Lists" button + the on-canvas list-create-then-edit flow stay on the lists-manager surface (`openListsEditor`). Hard short-circuits to `null` while no `editingList` is resolved (the standard `initialSelectedId`-style race). Draft sync effect adopts a fresh def when clean, forwards externally-managed fields (`favorited` / `pinnedToDashboard` / `sortOrder`) on dirty so a header-fired favorite or a reorder doesn't get reverted on Save. `guardDirty(perform)` routes Cancel / Esc / scrim through `showBulkConfirmation('custom', [], { title: 'Discard changes?', confirmLabel: 'Discard', onConfirm: perform })`, mirroring the manager flow |
| ResizeHandle | components/shared/ResizeHandle.tsx | Shared pointer-capture resize primitive consumed by every floating canvas widget (note / calendar / horizons / taskboard / list-inset — three handles in list-inset alone). Props: `axis: 'x' \| 'y' \| 'xy'`, `width` / `height` / `minW` / `minH` (clamp), `bodySelector` (CSS selector queried relative to `.react-flow__node` for the live-preview DOM-write target — defaults the host node's outer chrome), `onPreview(w, h)` (callback that may return `{ w, h }` overrides for snapping — used by ListInset's snap-aware paths), `onResize(w, h)` (commit on pointerup), `onEnd()` (cleanup hook for clearing alignment lines etc). Listeners attach to the handle element via `setPointerCapture` so a mid-drag unmount can't leak them; `cleanupRef` belt-and-suspenders fallback runs on unmount. Zoom-aware via `useReactFlow().getZoom()`. Extracted in code-review-2026-04-25 P3 from the inline pointer-capture blocks in each floating node |
| DragHandle | components/shared/DragHandle.tsx | Shared 6-dot drag-handle SVG (8×14 viewBox, 6 circles). Optional dnd-kit `attributes` + `listeners` props spread onto the wrapper span when this glyph IS the activator (e.g. `StatusEditor` / `DashboardListsEditor` reorder rows); decorative-only when `ariaHidden`. Extracted in code-review-2026-04-25 P6 from triplicate inline SVGs in `TaskRow` / `StatusEditor` / `DashboardListsEditor` |
| PortalDropdown | components/shared/PortalDropdown.tsx | Portal-rendered dropdown anchored below a trigger element. `anchorRef` + `onClickOutside`; positions panel via `getBoundingClientRect`, tracks anchor via `ResizeObserver` + capture-phase `scroll` + `react-flow__viewport` `MutationObserver` so transforms re-position the panel; viewport-clamps so the panel stays inside. Used by `TaskRow`'s status / people / scheduled menus and by `MobileTaskRow`'s long-press context menu + chip popovers (code-review-2026-04-25 P6 mobile parity) |
| FilterChipBar, FilterChipDensity | components/shared/filters/FilterChipBar.tsx | Shared filter-chip primitive consumed by `TopBar` (desktop), `ListFilterEditor` (desktop draft predicate), and `FilterSheet` (mobile). Single primitive owns every dropdown — `FilterDropdown` chrome + `EntityDropdownItems` (people / orgs / projects / statuses / tags) + `TriStateRow` (`hasScheduled` / `hasDeadline` / `showCompleted` / `showHiddenStatuses`) + `DateRangeDropdown` (`DateAnchorInput` × 2 + `dateField` `<select>`) + search input + Clear-all button. Props: `{ predicate, onChange, density }` — stateless predicate adapter shape so caller decides the state target (`filter-store`, draft on a `ListDefinition`, anything). `density="desktop"` renders the chip row + popover dropdowns; `density="mobile"` renders the accordion bottom-sheet shape that `FilterSheet` used to own inline. `useRightEdgeFlip` is wired in by default at desktop density so any chip near the viewport edge flips the dropdown via `data-align="end"`. Extracted in code-review-2026-04-25 P4 from ~1.8k LOC of duplication across TopBar / ListFilterEditor / FilterSheet |
| WidgetKindMenu | components/shared/WidgetKindMenu.tsx | Shared title-click popover: primary kind radios (List/Notes/Calendar/Taskboard) + a "Stats ▸" hover flyout (always visible, both add + change modes) containing horizons / status / scoreboard / snoozeGraveyard — post-stats-widgets-2026-04-25 P1 reorganization. The Stats row marks itself active when the current widget kind is one of the four stats kinds; ArrowRight on the row opens the flyout, ArrowLeft / Esc closes it (one Escape closes only the flyout — the menu stays open until the user dismisses it). The Stats and "Change list…" flyouts are mutually exclusive — they share a single `flyout: { kind: 'list' | 'stats' }` slot; opening one closes the other. + optional "Edit list" item (lens-only; lists-consistency P1; triage-2026-04-26 P5 / T7 redirected the dispatch) + optional "Change list…" secondary row (lens only — taskboard has no secondary because the board is a singleton; label overridable via `secondaryLabel`). Rails consume it via `canvas-rails-store.setSlotKind`; floating nodes consume it via `convertFloatingKind` (in `services/float-kind-switch`). The Edit list item only renders when `currentKind === 'lens'` and an `onEditList` callback is provided; clicking fires the callback — `SlotRenderer` and `ListInsetNode` now wire it to `useUIStore.openListEditorDialog(defId)` (Q7=A) so `<StandaloneListEditor>` mounts the per-list editor dialog directly with no `DashboardListsEditor` Lists modal underneath, and closes the menu. The secondary row is a hover flyout (not a separate popup): `pickListForLens(id)` fires `canvas-rails-store.updateSlot` (rails) or `list-inset-store.update` (floats) on click and closes the whole menu — the flyout renders `ListDefinitionPickerBody` inline, portal-anchored to the row with fallback left-flip when it would overflow the viewport, closes on pointer-leave with a short grace timer. Keyboard: arrow-key roving, Enter selects, ArrowRight opens flyout, ArrowLeft / Escape closes it then the menu |
| TaskDraggable, SortableTaskDraggable | components/task/dnd/TaskDraggable.tsx | Shared render-prop wrapper — **the** single `useDraggable` / `useSortable` call for every task row in the app. Owns id generation via `taskDragId(surface, todoId, extras?)` and the drag data payload shape `{ type, todo, ...extraData }` so every surface that renders a draggable task row emits identical ids + identical `data.type`. Consumers: `DraggableTaskRow` (inset + lens), `TaskList.DraggableRow` (ListView, with `extraData.sectionKey`), `DashboardDraggableRow` (dashboard cards, with `listKey`), `SortableTaskList.SortableTaskRow` (canvas project rows — `SortableTaskDraggable`), `TaskboardPanel.SortableEntry` + `TaskboardNode.SortableTaskboardEntry` (`SortableTaskDraggable` with `kind: 'taskboard-task'`), `CalendarView` + `CalendarStrip` (Phase 7 migrated to dnd-kit), `TopBar.SearchResultRow` (surface `'search'`, emits `search-todo-<id>` into the TopBar-local `DndContext`). Extracted in dnd-unification Phase 4 |
| DropIndicator, dropCellClassName | components/shared/DropIndicator.tsx | Shared drop-indicator visuals. `<DropIndicator kind="line">` renders a layout-neutral 2 px insertion bar via negative vertical margins (`-1 + 2 + -1 = 0` net flow impact, overlaps 1 px into adjacent rows' padding) so cached droppable rects stay valid for the rest of the drag — used by `SortableTaskList` between task rows + by `TaskboardPanel` / `TaskboardNode` for sortable-index insertion. `<DropIndicator kind="cell">` + `dropCellClassName(active)` handle whole-container glow (list insets, lens slots, calendar day cells). One CSS module (`DropIndicator.module.css`) owns the styling so every surface looks identical. Extracted in dnd-unification Phase 3; layout-neutral margins added in the post-P11 sortable-index drift fix (cursor was offsetting from the indicator as the indicator's mount shifted entries below it) |

## Layout

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| Sidebar | components/layout/Sidebar.tsx | Desktop vertical icon sidebar: Canvas (grid), List (lines), Calendar (calendar) at top; `bottomIcons` container pushes the theme-mode toggle (☀/☾) + Settings (gear) icons to the bottom — theme button reads `useSettingsStore().themeMode` + `useResolvedTheme()` and flips between `light` / `dark` on click, title shows `system (resolved)` hint when mode is system; hidden on mobile. Dashboard nav icon retired in features-batch-2026-04 Phase 5 alongside the view itself. Rails-collapse button + `useCanvasRailsStore` import + `RAIL_SIDES` const + `presentSides` / `hasAnyRail` / `allCollapsed` derivation removed in triage-2026-04-26 P3 / T5 (lives on the new `<CanvasToolbar>` instead — canvas-scoped actions belong on the canvas, not on the sidebar) |
| TopBar | components/layout/TopBar.tsx | Desktop filter + search bar. Filter chip row delegates to `<FilterChipBar density="desktop">` (post-code-review-2026-04-25 P4 — the dropdown chrome / entity items / tri-state rows / date-range editor that used to live inline are gone, replaced with a thin predicate adapter to `filter-store`). Grouped search dropdown (`role=listbox` with per-field `role=group` sections for Title/Notes/Project/Person/Org/Status/Tag). Each search row is a `SearchResultRow` — a `TaskDraggable surface='search'` (emits `search-todo-<id>` + `data.type='task'`) wrapped by a TopBar-local `DndContext` + `DragOverlay`. On drop-end, `handleSearchDragEnd` finds the hovered taskboard panel (`[data-tbp-container]`), bisects `[data-tbp-entry]` rects by Y via `computeSearchDropIndex`, and calls `useTaskboardStore.addAt(todoId, idx)` — the scoped context isolates search drags from the canvas/dashboard `DndContext`s without needing hoisting. Right-click on a row opens `CanvasContextMenu` with the same item set as `TaskRow` (Open / Mark complete / Add or Remove from Taskboard / Move to project… / Delete); menu state + `ProjectPickerPopup` live on TopBar (not the row) so they outlive row unmounts, and `handleOpenSearchContextMenu` no longer blurs the input — the dropdown stays open under the menu so the user can pick another row without retyping. `SearchResultsGroups` arrow-key roving + `Show all {n}` expansion unchanged from pre-P3. **Pills (ui-consistency-2026-04-25 P2):** each `SearchResultRow` renders `<TaskPillBar interactive={false}>` after the title + notes snippet — the inlined `SearchResultPills` block + its CSS (`miniListPills` / `miniListAvatar*` / `miniListChip*` / `miniListStatus`) retired. `interactive={false}` makes every chip a non-interactive `<span>` so chips never intercept the row click or the dnd-kit drag listener. The bar is wrapped in a `pointer-events: none` parent (`barReadOnly` class) so even hover effects don't bubble. Pill context (`peopleByTodoId` / `orgsByTodoId` / `statusesById` / `personOrgMap` / `orgs`) is resolved once on `TopBar` and threaded down via a single `SearchResultPillContext` prop so each row doesn't re-subscribe to assignment stores; the same context provides `personColorContext` so `<AvatarStack>` skips its internal `useOrgStore` subscription on the densest read-only surface in the app |
| BottomTabBar | components/layout/BottomTabBar.tsx | Mobile bottom tab navigation (List, Filters, Settings); shows filter active indicator dot |
| FileSyncBanner | components/layout/FileSyncBanner.tsx | Dismissible banner suggesting file sync when no file handle saved; dismissal persisted in localStorage |

## Taskboard

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| TaskboardPanel | components/taskboard/TaskboardPanel.tsx | Dashboard + rail card for the singleton taskboard. **No nested `DndContext`** — the outer `SortableContext` rides the surrounding canvas (rail slot) or dashboard `DndContext`. Entries are `SortableTaskDraggable` with `surface='taskboard-panel'` (emitting `tbp-<todoId>` ids + `data: { type: TASK_DRAG_KIND.taskboardTask, todo, panelId }`); the outer container is `useDroppable` with `data: { type: TASK_DROP_KIND.taskboard, panelId }`. `use-canvas-dnd.ts` / `DashboardView.handleDragEnd` both route drops through `dispatchTaskDrop`, which owns reorder / move-to-end / remove-on-drag-off / external-task-add. Panel + entry drag data both carry the same `panelId` so `onDragMove` recognizes ownership without leaning on `useDroppable.isOver`. Insertion index comes from dnd-kit sortable data (Phase 6); indicator rendered via shared `DropIndicator` — no DOM query, no silent "missing DOM → prepend" failure mode (F3) |

## UI Constants

| Abstraction | Location | Purpose |
|-------------|----------|---------|
| DEFAULT_ENTITY_COLOR | constants.ts | Default color '#537FE7' for new people and orgs |
| UNAFFILIATED_PERSON_COLOR | constants.ts | Neutral grey '#9CA3AF' used as fallback for people with no assigned org (PeopleEditor swatch + AvatarStack fill) |
